# Координация агентов LedgerLens

Канал связи между двумя автономными «разработчиками» проекта:
- **Оркестратор** — интерактивная сессия с владельцем (может спавнить субагентов
  в worktree, делает живые проверки на общем стеке, мержит).
- **Регулярная сессия** — `ledgerlens-backlog-progress` (cron каждые 3 ч,
  `~/.claude/scheduled-tasks/`): продолжает бэклог с места остановки.

Обе видят этот файл через git. **Читать его ПЕРВЫМ делом каждой сессии, писать в
него сводку в конце.** Он не заменяет BACKLOG.md (там ТЗ и приёмка) — здесь
оперативная координация: кто что делает сейчас, свежие находки, план.

---

## Протокол против коллизий (обязательно)

1. **Старт сессии:** `git pull` → прочитать раздел «Активные зоны» ниже → `git
   log --oneline -8` → **сверить состояние eval с БД, а не с памятью**
   (`SELECT id, profile, git_sha, summary->'metrics' FROM eval_runs ORDER BY id
   DESC LIMIT 6`). Числа в памяти/сносках могут устареть.
2. **Правило 30 минут:** если в `git status` есть незакоммиченные изменения, а
   файлы менялись < 30 мин назад (`stat -c %y <file>`) — вероятно, другая сессия
   активна. НЕ трогать эти файлы, заняться непересекающейся задачей или доложить
   и выйти.
3. **Застолбить зону:** перед работой над задачей — дописать строку в «Активные
   зоны» (задача, какие каталоги трогаешь, время). Убрать/обновить в конце.
4. **Коммить часто и мелко** — это главный механизм передачи. Незакоммиченная
   работа = невидима другой сессии и рискует быть перезатёртой. Перед выходом —
   закоммитить всё готовое (или явно отметить «WIP, не трогать» здесь).
5. **Не push в main из worktree субагента** — субагенты коммитят в свою ветку,
   мержит оркестратор.
6. **Общие файлы-хотспоты** (частые конфликты): `prompts/worker_react.md`,
   `config/app.yaml`, `docker-compose.yml`, `orchestrator/api.py`,
   `BACKLOG.md`. Правя их — закоммить сразу, не копи.

---

## Активные зоны (кто что делает СЕЙЧАС)

_Формат: `[роль] задача — каталоги — время начала — статус`_

- `[оркестратор] интерактив с владельцем: web_search reliability — Search-API бэкенд (Tavily/Brave) поверх скрапера — tools/web_search/core.py + common/config.py + .env.example + tests/unit/test_web_search.py — 2026-07-15 ~19:00 — ЗАВЕРШЕНО, закоммичено+запушено (main=`365df8d`), live-верифицировано. Зона СВОБОДНА.` **Причина:** владелец заметил, что web_search находил AMD, но не NVIDIA — no-key DDG-скрап отдаёт HTTP 202 bot-challenge с серверного IP (0 результатов → DeepSeek-fallback → uncited-ответ дропался агентом). **Фикс:** reliable-first Search-API слой в `_search`: Brave → Tavily → no-key скрап → DeepSeek (какой ключ задан). `parse_brave`/`_brave_search` (`683daf3`), `parse_tavily`/`_tavily_search` (`a36806b`); `tavily_api_key`/`brave_api_key` в Settings + `.env.example`. **Brave оказался платным** → Tavily = рекомендуемый бесплатный дефолт (`365df8d`). **Владелец дал Tavily-ключ; прописал в локальный `.env` (gitignored; НЕ в .env.example — там пусто, проверено git status).** **Live end-to-end (workstation, из РФ БЕЗ VPN):** api.tavily.com 401-reachable (первый TLS-timeout — разовый флап), keyed NVIDIA-поиск → 200, top = **sec.gov** (аудированный SEC-филинг) + официальные IR; через `web_search()`: `source:web`, `trust_summary:high`, 4 строки в `web_documents`, повторный вызов `source:db cached:True` (кэш работает), цитаты `sec.gov[high] type:web`. **Follow-up тюнинг тиров СДЕЛАН (`e49835d`):** офиц. IR-сабдомены раньше падали в `low` (как блог). Новое leftmost-label правило `ir_subdomains` (investor/investors/ir → medium) + ленты релизов (globenewswire/businesswire/prnewswire → tier2) в `config/web_search.yaml`+`trust_for_domain`. Ньюсрум-хост (nvidianews.nvidia.com) не IR → остаётся low (редундантно покрыт SEC+IR). Юнит-тест + live: `investor.nvidia.com` теперь medium, sec.gov high, стейл-кэш очищен+перескорен. Тронул tools/web_search/core.py + common/config.py + config/web_search.yaml + .env.example + tests/unit/test_web_search.py (15 web_search-тестов зелёных, ruff+mypy чисто). Образы mcp-websearch+worker пересобраны, mcp-websearch пересоздан с ключом+новым скорингом. **⚠️ ХОТСПОТ ТРОНУТ (`prompts/worker_react.md` v10→v11, `1c7936d`):** владелец: «NVIDIA vs AMD» → бот не искал AMD в вебе, выдал частичный ответ. Диагноз (live-трейс через `/api/chat`): web_search-фолбэк был привязан к all-or-nothing NO_DATA-пути — при СРАВНЕНИИ, где NVIDIA есть в БД, а AMD нет, воркер НЕ отдаёт `NO_DATA:` (данные NVIDIA на руках) → выдаёт частичный ответ, минуя веб. Недетерминированно (мой pre-fix прогон web_search вызвал, скриншот владельца — нет). Фикс: правило 3 теперь ТРЕБУЕТ web_search для КАЖДОЙ отсутствующей сущности в comparison/multi-entity задаче. **Gate-safety: правка инертна для голдена** (41 кейс проверен): comparison-кейсы грузят ОБЕ сущности (нет missing→не триггерит), 5 no_data — одиночные (существующее pre-NO_DATA правило 3 их и так покрывает, моё добавление comparison-specific). Live: 3/3 прогона после фикса web-ищут AMD → NVIDIA ~65.5% vs AMD ~34.1% с [web:]-цитатами. **Рекомендация регулярной/релизной сессии: worker_react.md — eval-hotspot; при следующем full-eval подтвердить гейт зелёным (ожидаемо инертно, но правило меняет worker-поведение).** eval-конфиг/пороги/eval.yml НЕ тронуты.

- `[регулярная] sql_query defense-in-depth: parse-барьер пропускал data-modifying CTE — tools/sql/core.py + tests/unit/test_sql_tool_validation.py (НЕ хотспот, cold-файл) — 2026-07-15 ~18:10 — ЗАВЕРШЕНО, закоммичено+запушено (main=`887869e`). Зона СВОБОДНА.` Оркестратор был АКТИВЕН на web_search/web/plumbing (683daf3 в 17:58, <30мин) — НЕ трогала ни один из их файлов (tools/web_search/*, web/*, orchestrator/graph.py, workers/react_worker.py, docker-compose.yml, prompts/worker_react.md, config/{router,workers,web_search}.yaml, common/config.py, .env.example, tests/unit/test_web_search.py). **Находка:** `_validate_and_rewrite` подтверждал `isinstance(statement, exp.Query)` но НЕ инспектировал вложенные ноды → writable-CTE (`WITH t AS (INSERT/UPDATE/DELETE … RETURNING *) SELECT * FROM t`) проскакивал parse-барьер (реальный барьер = роль app_ro на execution, но документированный «double barrier» был пробит на parse-слое + агент получал opaque permission-denied вместо самокоррект-observation). Живьё подтверждено ДО фикса: 3 writable-CTE ALLOWED. **Фикс:** reject при `statement.find(Insert/Update/Delete/Merge)` в любом месте дерева → честный hint «read-only, including inside CTEs»; benign read-only CTE не задет. 4 регресс-теста (3 writable-варианта + 1 benign-guard), git-stash-верифицированы (3 падают без фикса, доходя до `_ExplodingFactory`; benign проходит в обоих). ruff+mypy чисто, **385 non-slow зелёных** (381+4). Тронула ТОЛЬКО tools/sql/core.py + tests/unit/test_sql_tool_validation.py. Бюджет $0 (без LLM, тесты на фейках).

- `[оркестратор] интерактив с владельцем: T-042 переработка UI демо (web/) + T-043 веб-поиск (tools/web_search + backend) — 2026-07-15 — ЗАВЕРШЕНО, обе фичи задеплоены+верифицированы live (main=db4cdaa). Зона СВОБОДНА.` **T-042 ГОТОВО (33d5186):** переработан UI (markdown-ответ, чат-пузыри, живой нарратор+прогресс, таймлайн-степпер, footer-сводка cost/tokens/LLM/tools, тёмная/светлая тема, EN/RU); событийный пайплайн (agent.ts/RunView) не тронут; +react-markdown/remark-gfm; браузер-верифицировано, Pages-задеплоено. **T-043 ГОТОВО (db4cdaa):** новый `tools/web_search` (cache-first→DDG-скрап→DeepSeek-fallback, доменные trust-тиры + кросс-проверка), миграция `005 web_documents` (отдельно от аудируемых financial_facts), обвязка воркер/graph/router/compose (сервис mcp-websearch:8768)/prompt-rule-3c, расширен `_CITATION_MARKER` под `[web: domain]`, trust-бейдж на карточках цитат. 10 юнит-тестов (respx). **Живьё:** «Кто CEO OpenAI?» → SQL no_data → web_search ×4 → ответ «Сэм Альтман» с `[web: cnbc.com/wikipedia.org/forbes.com]` + бейджи ПЕРЕПРОВЕРЕН/НЕ ПРОВЕРЕН; 18 строк в web_documents (кэш). eval под `dev`/пороги НЕ трогал — гейт цел. **⚠️ Живой демо-стек пересоздан** (app/worker/mcp-websearch на новых образах, миграция 005 применена, BUDGET_PROFILE=demo сохранён). Ниже — исходная запись зоны: **Ф1 зона: `web/`** — рерайт `web/src/components/{Chat,AnalysisPanel,Header,Examples}.tsx` + `App.tsx` + `styles.css` + `i18n.ts`, новые `web/src/narration.ts`/иконки, `web/package.json` (+react-markdown/remark-gfm). Событийный пайплайн (`agent.ts`/`types.ts`/`config.ts`/`RunView`) НЕ трогаю. **Ф2 (позже) зона:** новый `tools/web_search/*`, `config/web_search.yaml`, `db/versions/005_*`, правки `workers/react_worker.py`/`orchestrator/graph.py`/`config/{workers,router}.yaml`/`docker-compose.yml`/`prompts/worker_react.md`/`common/config.py`. **eval под `dev`/пороги/eval.yml/eval-thresholds НЕ трогаю — гейт цел.** BACKLOG: T-042/T-043 заведены. Коммичу мелко и часто.

- `[регулярная] migration-staleness accuracy-fix: user-facing доки описывали выведенный CF-Tunnel + `app.ledgerlens.space` (NXDOMAIN) — CHANGELOG.md + deploy/demo/README.md + docs/release/v1.0-dod.md (docs-only, НЕ хотспот) — 2026-07-15 — ЗАВЕРШЕНО, закоммичено+запушено (main=`a1062a9`). Зона СВОБОДНА.` Найдено при live-сверке инфры: сайт+`ledgerlens.space/app/`+`api.ledgerlens.space` все 200, но старый `app.ledgerlens.space` = NXDOMAIN (миграция `c46bac3` ушла на Pages-фронт `/app/` + VPS-дверь `api.`). Три user-facing дока асертили выведенную топологию как текущую: (1) CHANGELOG `[Unreleased]`→v1.0 T-036-entry «live demo at app.ledgerlens.space behind a Cloudflare Tunnel» → переписан на актуальную топологию + линк migration-doc; (2) `deploy/demo/README.md` — оставила CF-Tunnel-рунбук как валидный ДЖЕНЕРИК-вариант self-host, но добавила migration-note над §4 + исправила «current demo domain» строку (app.ledgerlens.space ВЫВЕДЕН); (3) `docs/release/v1.0-dod.md` header — датированный аддендум 2026-07-15 (историчные «HTTP 200»-снимки от 2026-07-14 НЕ трогала — они дата-корректны). Оставшиеся `app.ledgerlens.space` в BACKLOG.md — дата-историчные acceptance-сноски (²⁹/³¹/³²), НЕ трогала (переписать = фальсифицировать запись). НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web/пороги/код/тесты. Бюджет $0 (docs-only, без LLM). Живьё 2026-07-15: сайт+фронт+API 200, app.ledgerlens.space NXDOMAIN; раннер `epyc-home` online+idle.

- `[оркестратор] интерактивная сессия с владельцем: SSE-транспорт демо + agui tool-call фикс + demo-бюджет + доки/сайт под новую топологию — 2026-07-15 — ЗАВЕРШЕНО, всё закоммичено+запушено (main=9dca58c). Зона СВОБОДНА.` Тронул хотспоты `orchestrator/api.py`, `orchestrator/agui.py`, `config/budgets.yaml` — все закоммичены сразу. НЕ трогал prompts/graph/worker/eval.yml/eval-thresholds/пороги/scoring. **(1) SSE-транспорт демо:** браузер висел/падал, curl проходил — CF-прокси дважды ломал SSE в браузере (h2 `Accept-Encoding`→буферизация-под-сжатие → фикс `no-transform` в `_sse_headers`, `fd9fd46`; h3/QUIC рвал совсем, alt-svc кэш 24ч). Развязка — **API вынесен из-под CF-прокси: `api.ledgerlens.space` → DNS-only + Let's Encrypt на VPS + HTTP/1.1** (webroot HTTP-01 через CF ДО переключения = нулевой простой; certbot на VPS уже был — apt заклинен сломанным `amneziawg-dkms`, **AWG-mesh владельца НЕ трогал**). Браузер теперь напрямую в VPS-nginx (`proxy_buffering off`). Живьё проверено: сложный multi-step стримит до конца. **(2) agui `Cannot send RUN_FINISHED while tool calls active` (`b7515a1`):** шаг с `budget_exceeded` оставлял открытый tool-call (name-keyed трекер терял параллельные одноимённые rag_search); адаптер теперь трекает открытые id в set'е и закрывает END'ами перед терминалом. 3 регресс-теста в `tests/contract/test_agui_contract.py`, git-stash-верифицированы, ruff+mypy чисто. **(3) budget (`dd21450`):** живой демо-стек шёл под `dev` (BUDGET_PROFILE пуст в базовом compose) — тяжёлый narrative-rag упирался в dev-лимиты (180с/8-итер), а `dev` делит eval → правка dev двинула бы гейт. Перевёл стек на изолированный `demo`-профиль (`.env` `BUDGET_PROFILE=demo`, gitignored, recreate) + поднял `config/budgets.yaml` demo (240с/12-итер/8-шагов/2-реплана/0.50$/250k/600с), `runs_per_hour_per_ip:null` (после ухода от CF детект client-IP не работает → concurrency+daily-cap защищают). **eval под `dev` НЕ тронут — гейт цел** (eval CI ставит BUDGET_PROFILE в своём ci-.env, не demo). Живьё: Apple-vs-MSFT+риски теперь досчитывает полностью. **(4) доки/сайт (`93bd1dd`,`9dca58c`):** ARCHITECTURE §7.1 + README EN/RU + site (index.html + app.js EN/RU) переписаны под воркстейшн-бэкенд + VPS-дверь + Pages-фронт; EPYC-нода/two-node/AmneziaWG/CF-tunnel помечены выведенными, A2A-мультинода оставлена как дизайн-возможность (config/workers.yaml). Полный разбор миграции + SSE-сага — `docs/migration-workstation.md`. **Для регулярной сессии:** демо-стек теперь под BUDGET_PROFILE=demo (локальный `.env`); eval-раннер на EPYC (Phase 5) ещё не перенесён — при eval-нуждах сверять живьём.

- `[регулярная] empty-answer симметрия: guardrail-ресинтез-путь тоже нормализует пустой/whitespace ответ (латентный gap, парный к `005b96e`) — orchestrator/graph.py + tests/unit/test_orchestrator_graph.py — 2026-07-15 ~14:10 — ЗАВЕРШЕНО, закоммичено+запушено (main=`bfb2e4b`). Зона СВОБОДНА.` graph.py хотспот — коммитила сразу. Оркестратор был активен на `orchestrator/api.py`+`docs/migration-workstation.md` (SSE no-transform, uncommitted, >30мин — их зона, НЕ трогала; их диф в дереве цел, pre-commit stash/restore не задел, 21+/1- как было). **Находка:** empty-answer-фикс `005b96e` нормализовал пустой/whitespace синтез в `_synthesize` + бэкстоп в `_finalize`, но **guardrail advice-ресинтез-путь** (`answer = response.text`, graph.py:591) остался БЕЗ нормализации — симметричный gap. На нём whitespace-only ресинтез проскакивает ОБА guard'а: `_synthesize` отработал раньше, а `_finalize` проверяет только `.strip()`-пустоту — после того как guardrail дописал дисклеймер, строка непустая → near-blank ответ (`"   \n\n_дисклеймер_"`) доходит до run_finished. **Фикс:** нормализую ресинтез так же как `_synthesize` (strip → честный fallback при пустоте) + DRY: 3 fallback-места сведены в модульный `_empty_answer_fallback()`. Поведение непустых ответов байт-в-байт. Регресс-тест `test_empty_guardrail_resynthesis_yields_honest_fallback_not_blank` git-stash-верифицирован: падает pre-fix ровно с near-blank `"   \n\n_…not investment advice._"`, проходит с фиксом. ruff+mypy чисто (130 files), **363 non-slow зелёных** (362+1; единственный red — orchestrator-owned `test_sse_headers_disable_buffering` от их незакоммиченного no-transform, не мой). НЕ трогала api.py/prompts/config/compose/worker/eval.yml/web/пороги/eval-логику. Бюджет $0 (без LLM; тесты на фейках).

- `[регулярная] gate-safety регресс-тесты (транзиентный сбой прогона НЕ даёт false-green) — tests/unit/test_eval_run.py (НЕ хотспот, test-only) — 2026-07-15 ~12:10 — ЗАВЕРШЕНО, закоммичено+запушено (main=d536943). Зона СВОБОДНА.` Оркестратор был АКТИВЕН на `orchestrator/api.py`+`docs/migration-workstation.md` (SSE `no-transform` split-origin фикс, <30мин, незакоммичен) → НЕ трогала их (ни их падающий `tests/unit/test_sse_heartbeat.py`); их uncommitted-диф в дереве цел (pre-commit stash/restore не задел). Test-only слайс в eval-домене: 2 регресс-теста пинят инвариант gate-safety `_aggregate`/`_check_thresholds`. **Находка:** `numeric_accuracy`/`guardrail_block`/`nodata_honesty` считаются из `r.passed` → non-succeeded/network_error кейс (`passed=False`, `scores={}`, ветка `status not in (...)` в `_score_case`) гонит метрику→0.0→red-violation. Это ПРАВИЛЬНО (gate-safe): будущая «оптимизация» исключать транзиенты из метрики превратила бы полностью-лежачий app (все метрики→None→skip в `_check_thresholds`) в **false-green** гейт — строго хуже false-red, который просто переганяется на чистом раннере. **Задокументированная АСИММЕТРИЯ (surfaced владельцу, НЕ фикшена):** narrative `faithfulness`/`citation_coverage` берутся из `r.scores` (пустой при сбое → **исключается**, не зануляется) — в full-eval это ловят pass-based категории, но narrative-only-прогон с тотальным network_error оставил бы faithfulness=None и проскочил гейт (краевой, приемлемо т.к. реальный гейт всегда микс категорий). Поведение НЕ менялось. Baseline: ruff+mypy чисто (130 files), **362 non-slow зелёных** (360+2; единственный red — orchestrator-owned `test_sse_headers_disable_buffering` от их незакоммиченного `no-transform`, не мой). НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web/пороги/eval-логику. Бюджет $0.

- `[регулярная] full-eval на release-HEAD (`af5c6d1`) — валидация empty-answer робастность-фикса (`005b96e`) на живом гейте — ЗАВЕРШЕНО ✅ ЗЕЛЁНЫЙ, промоушен доков сделан (main=`190cd3b`). Зона СВОБОДНА.` Прогон **`29390701909` (workflow_dispatch, profile=full) завершился success, gate exit 0** «no blocking thresholds violated»: citation 1.0 / faithfulness 0.862 / guardrail 1.0 / numeric 0.923 / nodata **1.0**, 37-of-41. **`nodata_ford_net_income` теперь PASS (74.4s, непустой)** — тот самый флейк-кейс, что ронял 1-й reference `29375595788` пустым answer. Второй подряд зелёный full-eval на release-HEAD и ПЕРВЫЙ с включённым empty-answer-фиксом → транзиент снят на гейте. Промотировала оба docs-трекера (DoD §3-аддендум + BACKLOG T-040 §3-аддендум); эталонная §3-запись с фиксом = `29390701909`. Тронула ТОЛЬКО `docs/release/v1.0-dod.md` + `BACKLOG.md` (не хотспоты) + COORDINATION + память. НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web/пороги. Baseline на HEAD: 361 non-slow зелёных.

- `[регулярная] empty-answer робастность-фикс (пустой синтез → run_finished с пустым answer → nodata_honesty=0.0 red-гейт, флаг из DoD §3 addendum) — orchestrator/graph.py (_synthesize+_finalize) + tests/unit/test_orchestrator_graph.py — 2026-07-15 — ЗАВЕРШЕНО, закоммичено+запушено (main=005b96e). Зона СВОБОДНА.` Хотспот graph.py — коммитила сразу (3 коммита: `_finalize` бэкстоп, `_synthesize` нормализация+тесты, DoD-аддендум). НЕ трогала prompts/config/compose/api.py/worker/eval.yml/web/пороги. **Корень (из DoD §3 addendum п.(а)):** редкий пустой/whitespace-only синтез-комплишн распространялся до `run_finished` как пустой `answer` (молчаливый не-ответ; на no_data → `nodata_honesty=0.0` red чистого прогона — ровно `29375595788`/`nodata_ford_net_income`). **Фикс:** (1) `_synthesize` нормализует пустой/whitespace ответ в источнике → честное локализованное «нет данных»-признание (guardrail дальше штатно вешает дисклеймер); (2) `_finalize` — бэкстоп на пустоту после guardrail-ре-синтеза; 2 модуль-константы EN/RU. 2 регресс-теста (EN empty, RU whitespace) git-stash-верифицированы, поведение для непустых ответов байт-в-байт. ruff+mypy чисто, **361 non-slow зелёных** (359+2). Пункт DoD §3 (а) закрыт кодом; (б) допуск ≥0.75/серверный ретрай — опциональный defense-in-depth за владельцем, не блокер.

- `[регулярная] T-040 §3 эталонный full-eval на release-HEAD — ЗАВЕРШЕНО (green run подтверждён), промоушен сделан ОРКЕСТРАТОРОМ (`a587a20`), моя зона СВОБОДНА.` Подтверждающий full-eval **`29378924962` (HEAD `5f63b3d`, рантайм-идентичен release-HEAD `df71baa`) ЗЕЛЁНЫЙ** (1h10m, gate exit 0): citation **1.0** / faithfulness **0.885** / guardrail **1.0** / nodata **1.0** / numeric 0.923, 37/41, «no blocking thresholds violated». nodata-флейк 1-го прогона (`nodata_ford_net_income → answer=""`) на повторе PASS (25.5s) — не регрессия. **Коллизия:** оркестратор мониторил тот же прогон параллельно и промотировал §3 первым (`a587a20` @ 01:35:54 UTC — DoD §3 block-D аддендум + BACKLOG-сноска +15 строк). Мой дубль-edit DoD-трекера откатан (`git checkout`), не запушен. Я лишь финализирую эту строку + журнал. Baseline на HEAD зелёный (ruff+mypy чисто, 359 non-slow passed). **1-й full-eval `29375595788` (release-HEAD df71baa) — все шаги up SUCCESS, «Run eval» SUCCESS, но gate red (exit 1) по ЕДИНСТВЕННОМУ нарушению `nodata_honesty=0.8 < 1.0`.** Разбор report-артефакта: `nodata_ford_net_income` → `answer=""` (ПУСТОЙ ответ), structural passed (`invented:[]`), judge 0.0 «ANSWER is empty» — ровно известная транзиентная nodata-хрупкость (журнал: RemoteProtocolError/пустой ответ обнуляет метрику при строгом гейте ≥1.0), НЕ регрессия. **ГЛАВНОЕ: все метрики, которых касались правки +27h…+30h, чистые — faithfulness 0.983, citation 1.0, guardrail 1.0, numeric 0.923 (37/41 pass) → рантайм-правки (agui tool_call_id, advice-gate, eval-scoring, CORS) гейт НЕ сдвинули.** §3 нужен ЗЕЛЁНЫЙ reference-прогон → передиспетчила `29378924962` (nodata-флейк washes out на повторе, прецедент в журнале). Промоушен §3 + BACKLOG — по зелёному прогону. НЕ трогаю prompts/config/compose/api.py/graph/worker/eval.yml/web.

- `[регулярная] eval-scoring latent-bug fix (_sig_digits для sub-unit/дробных значений) — eval/scoring.py + tests/unit/test_eval_scoring.py (НЕ хотспот, НЕ пороги/гейт-конфиг) — 2026-07-14 ~+30ч — ЗАВЕРШЕНО кодом+тестами, закоммичено+запушено (main=0ae7966). Зона СВОБОДНА.` Инфра live: раннер `epyc-home`=OFFLINE, демо=530 (EPYC-нода снова down), сайт=200 — все live/hardware-критерии заблокированы, взят чисто-локальный слайс. **Баг:** `_sig_digits` считал значащие цифры через `int(round(abs(value)))` → для `value<0.5` даёт `"0"` (answer-text фолбэк `"0" in digits(answer)` → ложный PASS на любом ответе с нулём), а для дробных >1 округление сдвигает младшие цифры (`1234.56`→`"1235"` вместо `"123456"` → ложный FAIL). Затрагивает `score_numeric` (absolute value) и `score_multi_step_structural` (key_numbers). **Фикс:** значащие цифры через точную десятичную развёртку `_digits(format(Decimal(str(abs(value))),"f")).lstrip("0")[:n]`. **Все текущие golden — крупные целые → поведение байт-в-байт (гейт НЕ двигается).** 2 регресс-теста git-stash-верифицированы (падают на старом round-коде: sub-unit `"0" in "040"`=спурьёзный pass; fractional `"1235"∉"123456"`=ложный fail). ruff+mypy чисто, **359 non-slow зелёных** (357+2). НЕ трогала thresholds/eval.yml/prompts/config/compose/api.py/graph/worker/web.

- `[регулярная] T-038 крит «все ссылки README живые» (live link-checker) + fresh-reader accuracy-fix (стейл Project-status/Known-limitations) — README.md, README.ru.md, BACKLOG³¹ — 2026-07-14 ~+27ч — ЗАВЕРШЕНО, закоммичено+запушено (main=8582db5). Зона СВОБОДНА.` Docs-only, не хотспот. НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web. **(1)** Полный live link-check обоих README: 16 внутренних файлов/картинок — все есть; 4 anchor-ссылки — резолвятся к реальным заголовкам (GitHub-slug); 4 внешних GitHub-URL/бейджа — все HTTP 200 (curl -L). Крит T-038 «все ссылки живые» ✅ закрыт живьём. **(2)** Fresh-reader pass вскрыл РЕАЛЬНУЮ stale: «Project status» говорил «Gates G1 G2» при закрытом **G3** + числил two-node в remaining; «Known limitations» держал 2 факт-устаревших пункта — `faithfulness non_blocking «while T-041 closes»` (T-041 ЗАКРЫТ, faithfulness = blocking, `eval-thresholds.yaml non_blocking: []`) и `two-node not live yet` (G3 живой). Оба пункта удалены; статус → «G1 G2 G3 ✅», delivered → T-001…T-034+T-041; EN+RU симметрично (`062bd47`). Baseline: 333 non-slow зелёных на HEAD, release-gate инвариант держится. Бюджет $0 (без LLM).

- `[регулярная] T-039/T-040 site OG/canonical fix — site/index.html (не хотспот) — 2026-07-14 ~+21ч — ЗАВЕРШЕНО, закоммичено+запушено (main=98f9d5f), ЖИВО проверено над публичным URL. Зона СВОБОДНА.` Соц-превью/canonical указывали на стейл `zzlawlzz.github.io/ledgerlens/`, сайт же канонически отдаётся с custom-домена `https://ledgerlens.space/` → `og:url`/`og:image`/`twitter:image` перенаправлены на custom-домен + добавлен `<link rel="canonical">`. После Pages-redeploy `29335200230` (success): `og:image` резолвится 200 с **0 редиректов** (было 301 через github.io → соц-краулеры не тянули превью). Тронула ТОЛЬКО `site/index.html`+BACKLOG³². НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web.

- `[регулярная] DoD §2 остаток: SSE keep-alive vs CF-idle-timeout (замер над публичным демо) — docs/release/v1.0-dod.md (docs-only) — 2026-07-14 ~+18ч — ЗАВЕРШЕНО, закоммичено+запушено (main=aa8dc76). §2 промотирован 🟡→✅ done. Зона СВОБОДНА.` Docs-only. НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web. **Измерено живьём** (4 тяжёлых multi-step запроса над публичным HTTPS-демо через реальный CF-туннель, клиентский таймштампер curl→python): max byte-пауза = **10.4 c** на 200-с непрерывном SSE-соединении; крупные data-паузы (шаг плана `deepseek-v4-pro` 22.6 c) перекрыты `: keep-alive` каждые ~10 c → CF-idle (дефолт 100 c ≫ 10 c heartbeat) не может сработать. Все 4 прогона обрезаны КЛИЕНТСКИМ `--max-time` (`curl 28`), ни разу не CF-524. Отклонение: единый завершённый >100-с трейс не снят (CPU-тир демо-ноды медленный; сквозной `run_finished` уже доказан 38-с acceptance-прогоном `d73056f9`). DoD-итог 7→**8 из 11 done**.

- `[регулярная] CHANGELOG.md accuracy-fix (стейл `[Unreleased]`: T-031/G3 и T-041 числились in-progress, а закрыты) — CHANGELOG.md + BACKLOG³¹ — ЗАВЕРШЕНО, закоммичено+запушено (main=<см. журнал>). Зона СВОБОДНА.` Docs-only, не хотспот. НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web. Перенесла T-031(G3)+T-041 из «In progress» в `[G3] Added`; «In progress» подрезан до T-037/T-040; добавлен опубликованный сайт T-039. Также сверила release-gate инвариант на HEAD: ruff+mypy чисто, 333 non-slow зелёных.

- `[регулярная] DoD §2 acceptance-прогон незнакомцем + §5 perimeter-sanity над публичным демо — docs/release/v1.0-dod.md, BACKLOG²⁹ — ЗАВЕРШЕНО, закоммичено+запушено (main=a65ad90). Зона СВОБОДНА.` БЕЗ правок кода (только 2 docs-файла). Live через публичный HTTPS `app.ledgerlens.space` (демо up, раннер online не-busy, eval не идёт → окно чистое): анонимный `/api/chat` «AAPL revenue FY2025» → верный $416.161B, guardrail pass, non-advice, cost $0.0015, run_id `d73056f9`; SSE `: keep-alive` через реальный CF-туннель (§4); dealias v4-flash+v4-pro живьё. §5: 600-симв. вопрос→413, 4 security-заголовка present, `/docs`+`/openapi.json`=SPA-fallback (docs-gating держится), healthz 200. НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web.

- `[регулярная] T-040 release-hygiene: Makefile (стейл-стаб `make eval` + пропуски .PHONY) — ЗАВЕРШЕНО, закоммичено+запушено (main=701190b). Зона СВОБОДНА.` Тронула ТОЛЬКО `Makefile` (не хотспот) + BACKLOG-сноска³⁴ + COORDINATION. НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web. **(1)** цель `make eval` была стейл-заглушкой `@echo "TODO(T-029): eval harness"` при давно готовом+CI-зелёном харнессе → прошита на `uv run python -m eval.run --profile $(EVAL_PROFILE) --base-url $(EVAL_BASE_URL) --concurrency $(EVAL_CONCURRENCY)` (дефолты ci/localhost:8000/1, как в eval.yml). Проверено: `eval.run --help` = дословная сигнатура, рецепт tab-индентирован, grep подтвердил что иных TODO-стабов в рецептах нет. **(2)** `up-no-local`+`reindex` были реальными целями вне `.PHONY` → добавлены (comm-проверка: все цели теперь phony-declared). Онбординг-путь §1 доп. сверен по РЕАЛЬНОСТИ: env-переменные Quick Start (DEEPSEEK_API_KEY/POSTGRES_PASSWORD/POSTGRES_RO_PASSWORD/A2A_TOKEN) все есть в `.env.example`; scripts smoke_test/eval_snapshot/fetch_demo_snapshot/eval_baseline_compare + флаг `--auto-ingest` + модуль `ingestion.reindex` — все существуют. Расхождений документированного пути с репо больше НЕ найдено. 333 non-slow зелёные (baseline не тронут).

- `[регулярная] T-041 ЗАКРЫТ (`[wip]`→`[done]`) — БЕЗ правок кода-рантайма, только gate-config+доки — ЗАВЕРШЕНО, закоммичено+запушено (main=<см. журнал>). Зона СВОБОДНА.` **ДВА подряд чистых full-eval на self-hosted EPYC-раннере зелёные:** `29297896263` (faithfulness 0.985 / citation 1.0 / 39-of-41) и `29303631947` (faithfulness 1.0 / citation 1.0 / 38-of-41), оба 41 кейс, gate exit 0 «no blocking thresholds violated». Критерий приёмки T-041 («≥0.7 стабильно на ≥2 последовательных full-прогонах на чистом стенде») ВЫПОЛНЕН. Citation-«потолок» и faithfulness-«шум» = артефакты голодания БЕСПЛАТНОГО 2-vCPU GH-раннера, не кода — на EPYC обе метрики железно на максимуме. **Сделано:** `config/eval-thresholds.yaml` → `non_blocking: []` (faithfulness промотирован в blocking, порог 0.7 НЕ занижался, комментарий-обоснование обновлён); валидатор гейта проверен инлайн (faithfulness-нарушение → exit≠0); BACKLOG T-041 `[done 2026-07-14]`+сноска²⁰-аддендум; Q-16 и Q-22 закрыты в OWNER_QUESTIONS; DoD-трекер §3/§5/block-C/header синкнут; память. 333 non-slow зелёных. **Тронула:** `config/eval-thresholds.yaml` (НЕ хотспот), BACKLOG, OWNER_QUESTIONS, docs/release/v1.0-dod.md, COORDINATION, память. НЕ трогала prompts/api.py/graph/worker/compose/eval.yml/web. **Заметка:** 1-й confirm-dispatch `29302691531` ушёл дефолтным профилем `ci` (default eval.yml=ci) — зелёный, но ci-сигнал слабый, не засчитан; передиспетчила `-f profile=full`.

- `[регулярная] §3 эталонный full-eval РАЗБЛОКИРОВКА (MCP host-port коллизия на EPYC-раннере) — ЗАВЕРШЕНО кодом+ЖИВОЙ верификацией, закоммичено+запушено (main=4c1ee7d). Зона СВОБОДНА.` Тронула ТОЛЬКО новый `docker-compose.ci.yml` + `.github/workflows/eval.yml` (+BACKLOG³⁴ addendum, отдельным коммитом). НЕ трогала base docker-compose.yml/prompts/config/api.py/graph/deploy/eval-код. **Корень:** прогон `29292555409` падал через 4м не по метрикам, а на `docker compose up`: `Bind for 10.9.0.1:8765 failed: port is already allocated` — base compose публикует MCP на host-портах 8765/8766/8767 (dev-only), а always-on demo/AWG-стек на том же EPYC-раннере биндит их на `10.9.0.1:8765` (`compose.mcp-awg.yml`). Фикс: оверлей `docker-compose.ci.yml` снимает host-публикации (`ports: !override []` mcp-sql/rag/enrich), eval.yml прокидывает его во все три compose-команды. eval-app ходит в MCP по compose-сети → host-порты не нужны. **ЖИВАЯ ВЕРИФИКАЦИЯ:** триггернула full-eval `29297896263` на HEAD с фиксом — **ОБА `up`-шага прошли SUCCESS** (postgres/qdrant + app/worker/mcp-sql/mcp-rag), впервые eval прошёл коллизионную точку на self-hosted раннере и дошёл до шага «Run eval» (сейчас скорит кейсы, ~20-40м на full). **§3-блокер `up` устранён.** Итоговые метрики (citation/faithfulness = вопрос T-041/Q-22, отдельный от инфра-фикса) — читать оркестратору/след. сессии по завершении `29297896263`. — ЗАВЕРШЕНО, закоммичено+запушено (main=<см. журнал>). Зона СВОБОДНА.` Тронула ТОЛЬКО BACKLOG (сноска ²⁹ + маркер `[ ]`→`[~]`) + COORDINATION + память. НЕ трогала код/хотспоты (api.py/router.py/compose/prompts/graph/eval/web). Демо снова live после router-фикса оркестратора → закрыла отложенные live-критерии: §5 `/api/examples` demo:true из реального app-контейнера + баннер живьём; §3 security-заголовки + docs-gating (app+nginx) над публичным TLS; §4 healthz 200 (полный ран уже подтверждён тобой). **Остаток T-036 = ТОЛЬКО §1 визуальная проверка Grafana-панелей demo-отказов** (Grafana не публична → твой домен на demo-ноде); по её закрытию T-036 → `[done]`.

- `[регулярная] T-040 sync трекера DoD к реальности + secret-скан публичного репо — ЗАВЕРШЕНО, закоммичено+запушено (main=ec9f760). Зона СВОБОДНА.` Тронула ТОЛЬКО `docs/release/v1.0-dod.md`, `.gitignore` (+`awgbins/`), `BACKLOG.md`³⁴ + COORDINATION. НЕ трогала prompts/config/compose/api.py/graph/eval/web. Живьё проверено: GH Pages API (cname ledgerlens.space, cert approved+enforced, deploy `29282878530` зелёный), сайт отдаёт полный контент, G3 закрыт (`d783e0d`). Трекер: §6 🟡→✅ (гейт G3), §10 публичный URL ⛔→✅, итог 7/11 done, блокер B закрыт. **Secret-скан публичного репо (чисто):** `.env` не в истории, нет sk-/AIza/PEM/id_ed25519 в tracked-файлах, единственные «пароли» = ephemeral CI-throwaway (`ci-password`, `ci-a2a-token`). Действий не потребовалось.

- `[регулярная] T-040 §6: Known Limitations в README (dedicated section) — ЗАВЕРШЕНО кодом-доков, закоммичено+запушено (main=ed0af9d). Зона СВОБОДНА.` Тронула ТОЛЬКО `README.md`, `README.ru.md`, `docs/release/v1.0-dod.md` (блок D §6 → done), `BACKLOG.md` (T-040 footnote-аддендум). НЕ трогала prompts/config/compose/api.py/graph/eval/web. Промотировала 3-пунктовый список из «Project status» в отдельную секцию `## Known limitations` (EN канон) + `## Известные ограничения` (RU зеркало, симметрично) + добавила недостающие релиз-ограничения: частичное RU-покрытие (Q-02: MOEX ISS живой = pluggable-доказательство, e-disclosure/ГИР БО = каркасы вне v1.0), глубина цен (T-033 AV free-tier ~100 дней), исключение GPU (Q-07), скоуп публичного демо (US/EDGAR + лимиты). 7 честных пунктов. Внутренние якоря EN+RU проверены против заголовков; `benchmarks/inference/REPORT.md` резолвится. Docs-only, не-хотспот. Закрывает T-040 §6 (README-часть block D); T-040 остаётся `[ ]` (G3 не закрыт).

- `[регулярная] T-036 §1 defect-fix #3: /agui concurrency-slot leak (overlong thread_id) — ЗАВЕРШЕНО кодом+тестами, закоммичено+запушено (main=9bb0bd1). Зона СВОБОДНА.` Тронула ТОЛЬКО `orchestrator/api.py` (хотспот — коммичен сразу, +импорт `ValidationError`, оба `/agui`+`/api/chat`) + новый `tests/unit/test_agui_slot_leak.py` (2 теста) + `BACKLOG.md`²⁷ (footnote отдельным коммитом). НЕ трогала `demo_limits.py`/пороги/prompts/graph/eval/compose/nginx. Реальный public-demo availability-баг: `/agui` брал concurrency-слот в `_admit` и создавал ран ДО `ChatRequest(session_id=body.thread_id)`, а `session_id` капается `max_length=128` при неограниченном AG-UI `thread_id` → клиент с `thread_id`>128 симв. ронял ValidationError вне slot-release-guard → слот течёт навсегда + ран осиротевает в `running`; на демо (cap=2) два таких запроса вешают демо на «busy» 429 до рестарта. Фикс: валидировать весь запрос (ValidationError→422) ДО взятия слота/`create_run` + расширить guard на весь setup до `create_task` (оба эндпоинта). 2 теста гоняют реальный endpoint (`TestClient`), **git-stash-верифицированы: падают на старом коде** (`_active==1`). 333 non-slow зелёных, ruff+mypy чисто.

- `[регулярная] T-036 §1 defect-fix #2: spoofable demo rate-limit (X-Forwarded-For) — ЗАВЕРШЕНО кодом+тестами, закоммичено+запушено (main=2dd88d2). Зона СВОБОДНА.` Тронула ТОЛЬКО `orchestrator/api.py:_client_ip` (хотспот — закоммичен сразу) + `tests/unit/test_demo_limits.py` (заменён 1 тест на 2) + `BACKLOG.md`²⁷ (footnote, отдельным коммитом). НЕ трогала `demo_limits.py`/пороги/prompts/graph/eval/compose/nginx. Фикс = доверять только `CF-Connecting-IP` (неспуфабельно за Cloudflare-туннелем), вне CF — прямой TCP-peer; `X-Forwarded-For` больше не источник для rate-limit. 331 non-slow зелёных, ruff+mypy чисто. Опциональный defense-in-depth (nginx явно затирать XFF) — не блокер, не делала.

- `[регулярная] T-036 §1 defect-fix: demo rate-limit lockout — ЗАВЕРШЕНО кодом+тестом, закоммичено+запушено (main=ced750b). Зона СВОБОДНА.` Тронула ТОЛЬКО `orchestrator/api.py` (`_admit`-ordering, хотспот — закоммичен сразу) + `tests/unit/test_demo_limits.py` (+1 регресс-тест) + `BACKLOG.md`²⁷ (addendum). НЕ трогала `demo_limits.py` (публичный API сохранён), пороги/prompts/graph/eval/compose. Реальный public-demo дефект: `check_rate` (пишет hit) вызывался ДО `acquire_slot` → concurrency-отказ («busy» 429) жёг часовую rate-квоту → рекомендованные ретраи могли залочить пользователя на час без единого прогона. Фикс: слот берётся ДО записи rate-hit, при rate-reject отпускается. Регресс-тест падал на прежнем порядке (`rate_limited`), проходит на новом. 330 non-slow зелёных, ruff+mypy чисто.

> ⚠️ **НАХОДКА для владельца (T-041 закрытие БЛОКИРОВАНО инфраструктурой):** self-hosted раннер `epyc-home` = **OFFLINE** (`gh api .../actions/runners`). Запланированный eval `29229428059` (schedule, стартовал ~06:35 UTC) **завис в queued ~2.5ч** — некому подхватить. Пока EPYC-нода/раннер offline, T-041-ревалидация (citation на реальном железе, Q-22=вариант-1) НЕ пойдёт, и каждый следующий schedule-триггер будет копить queued-раны. **За владельцем: поднять `epyc-home` runner-сервис на ноде (и/или освободить диск EPYC — прошлый блокер).**

- `[регулярная] T-040 подготовка: release-readiness tracker (DoD §7 evidence-doc) — ЗАВЕРШЕНО, закоммичено+запушено (main=a9cf064). Зона СВОБОДНА.` Новый `docs/release/v1.0-dod.md` (11 пунктов DoD §7: 6 ✅ done, 5 code-ready, каждый code-ready с именованным live-блокером) + консолидированные G4-блокеры (A железо-EPYC / B сеть-VPS / C owner Q-05·Q-22 / D не-прогнанные локальные протоколы) + footnote-задел в BACKLOG T-040. Все 22 evidence-указателя проверены против дерева. НЕ трогала код/prompts/config/compose/eval/graph — только новый doc + аддитивная заметка BACKLOG. T-040 остаётся `[ ]` (G3 не закрыт).

- `[регулярная] T-035 живая верификация (crit 1,2,3) — ЗАВЕРШЕНО, закоммичено+запушено (main=<см. журнал>), задача `[wip]`→`[~]`. Зона СВОБОДНА.` БЕЗ правок кода (только BACKLOG²⁵ + COORDINATION). crit 2 через реальный `/api/monitor/ingest-events` (new_count 1→0); crit 1,3 полный summarize-конвейер (реальный кэш-fetch NVDA-10K + DeepSeek + guardrail) с впрыснутым dry-run-alerter — Telegram владельцу НЕ слался. Тестовая строка `monitored_events` убрана за собой. Остаток `[~]`: crit 4 (n8n restore — образ ~1 ГБ vs диск C: 2.4 ГБ своб. = риск Q-21) + реальный Telegram-send (разрешение владельца; канал уже доказан оркестратором `312b5ae`).

- `[регулярная] T-038 fresh-reader doc-review — README.md, README.ru.md, BACKLOG.md³¹ — ЗАВЕРШЕНО, закоммичено+запушено (main=aad948f). Зона СВОБОДНА.` Тронула ТОЛЬКО README.md/README.ru.md (`4099447`) + footnote BACKLOG³¹ (`aad948f`, отдельно). Сверила каждый claim против as-built; исправила 2 stale-неточности EN↔RU симметрично: (1) Quick Start «6 сервисов» + enumeration пропускал 3 MCP-сервера (mcp-sql/rag/enrich, T-027) которые `make demo --profile local` реально поднимает → «full local stack» + MCP добавлены; (2) статус «T-001…T-037 delivered» при T-031/035 wip, T-036/037 [ ] → перепривязано к T-001…T-030+T-032…T-034 + явный остаток фазы 4. Проверено: все README-ссылки резолвятся, порты (3000/8000/3001) и faithfulness=non_blocking точны. НЕ трогала prompts/config/compose/api.py/eval/graph. Частично закрывает остаток T-038 (б) fresh-review; T-038 остаётся `[~]` (чистая-VM + `[OPEN]`(T-037) + GIF(→T-039)).

- `[регулярная] T-041 робастность: rerank soft-timeout + RRF-fusion-fallback — ЗАВЕРШЕНО кодом+тестами, закоммичено+запушено (main=16b9e43). Зона СВОБОДНА.` Тронула ТОЛЬКО `tools/rag/core.py` (config-gated `rerank_timeout_s` + fusion-fallback), `config/rag.yaml` (+ключ `search.rerank.timeout_s: null`, документирован), новый `tests/unit/test_rag_rerank_timeout.py` (4 теста), `BACKLOG.md`²⁰ (addendum). НЕ трогала пороги гейта / eval.yml / eval-thresholds.yaml / worker / prompts / graph.py. **Default `timeout_s: null` = поведение байт-в-байт как было** (dev/eval не тронуты, закреплено тестом). 329 non-slow зелёных, ruff+mypy чисто. **Остаток:** включение (`timeout_s`>0 в demo/constrained-профиле) + живая ревалидация citation на EPYC-раннере = домен оркестратора, тот же runner/disk-блокер.

- `[регулярная] T-036 §4 (SSE-heartbeat для CF-прокси) — ЗАВЕРШЕНО кодом+юнит-тестами, закоммичено+запушено (main=9cd204c). Зона СВОБОДНА.` Тронула ТОЛЬКО `orchestrator/api.py` (`_iter_with_heartbeat`, `_sse_headers`, `_drain_events`, оба эндпоинта; хотспот — закоммичен сразу), `orchestrator/agui.py` (`SSE_HEARTBEAT` + None-тик в `stream_agui_run`), новый `tests/unit/test_sse_heartbeat.py`, `BACKLOG.md`³³. НЕ трогала graph.py/worker/prompts/config/*.yaml/docker-compose/eval.yml/nginx.conf. Реальный пробел: оба SSE-эндпоинта блокировались на `queue.get()` без таймаута → долгий шаг оркестратора → CF idle-timeout рвал стрим; heartbeat в коде ОТСУТСТВОВАЛ. Фикс: тик каждые 10 c → SSE-комментарий `: keep-alive` (игнорится @ag-ui/client) + `X-Accel-Buffering: no` на ответах. 8 тестов гоняют реальные генераторы = байты на проводе; 325 non-slow зелёных. **Остаток §4 (T-036 остаётся `[ ]`):** живой end-to-end через РЕАЛЬНЫЙ туннель — демо на EPYC 530 (диск-блокер), тот же остаток что публичный URL+TLS (Q-05).
- `[регулярная] T-036 §1 остаток (Grafana-наблюдаемость demo-отказов) — ЗАВЕРШЕНО кодом+тестами+живой DB-проверкой, закоммичено+запушено (main=4a2d9ad). Зона СВОБОДНА.` Тронула: новый `db/versions/004_demo_rejections.py`, `orchestrator/persistence.py` (+`record_demo_rejection`), `orchestrator/api.py` (ТОЛЬКО обработчик `_demo_limit_handler` — пишет отказ best-effort), `orchestrator/demo_limits.py` (+`kind` в `DemoLimitError`), `observability/dashboards/operations.json` (+2 панели, минимальный diff в стиле файла), новый `tests/unit/test_demo_rejection_recording.py`, `tests/unit/test_demo_limits.py`, `BACKLOG.md`²⁷. НЕ трогала graph.py/worker_client.py/prompts/config/*.yaml/docker-compose/eval.yml. **⚠️ Применила `alembic upgrade head` на ОБЩЕМ dev-стеке → DB теперь на `004`** (таблица `demo_rejections` пустая, аддитивно, ничего не сломано). **Остаток §1 (T-036 остаётся `[ ]`):** только живая визуальная проверка панелей на `BUDGET_PROFILE=demo`-стеке (EPYC, ребилд app) + §4 CF-туннель (Q-05).

- `[регулярная] T-039 (сайт-презентация) — ЗАВЕРШЕНО кодом+браузер-проверкой, закоммичено+запушено (main=9c75e27), помечено `[~]`. Зона СВОБОДНА.` Тронула ТОЛЬКО НОВЫЙ `site/` (index.html+styles.css+app.js+assets 4 png+.nojekyll), новый `.github/workflows/site.yml` (GitHub Pages), `.claude/launch.json` (+static `site`-сервер), `BACKLOG.md`³² (отдельным коммитом). НЕ трогала prompts/, config/*.yaml, docker-compose.yml, orchestrator/*, web/, benchmarks/, eval.yml, deploy/. **Остаток (почему `[~]`):** (а) публичный Pages-URL — workflow готов, ждёт включения Pages владельцем в настройках репо + 1-го прогона (локально проверено `python -m http.server`); (б) 60–90-сек gif/видео самокоррекции — пока статические скриншоты (нужен живой стек+capture); (в) live-демо ссылка `app.ledgerlens.space` = 530 (EPYC диск-блокер — твой домен), заработает по возврату демо.

- `[регулярная] T-038 (README + финализация доков) — ЗАВЕРШЕНО доковой частью, закоммичено+запушено (main=eb7930a), помечено `[~]`. Зона СВОБОДНА.` Тронула: `README.md` (переписан RU-only→канонический EN), новые `README.ru.md`+`CHANGELOG.md`, `ARCHITECTURE.md`+`IMPLEMENTATION_PLAN.md` (синк ADR/[OPEN] к as-built), `BACKLOG.md`³¹ (закоммичен отдельно сразу). НЕ трогала prompts/, config/*.yaml, docker-compose.yml, orchestrator/api.py, graph.py, eval.yml. **Остаток T-038 (почему `[~]`):** (а) Quick Start на чистой VM не воспроизведён; (б) внешний читатель/свежий-LLM-ревью (только self-review сделан); (в) 1 осознанный `[OPEN]` в ARCHITECTURE §3.4 (локальная CPU-модель) снимется вместе с local-частью T-037 (ждёт EPYC-ноду); (г) GIF-анимации фич → вынесены в T-039 (пока статические скриншоты).

- `[регулярная] T-037 §1 (инференс-бенчмарк CPU-vs-API, API-часть) — ЗАВЕРШЕНО кодом+живым API-прогоном, закоммичено+запушено (main=9549315). Зона СВОБОДНА.` Тронула ТОЛЬКО `benchmarks/inference/` (новые `bench.py`,`prompts.py`,`REPORT.md`,`results.json`,4 PNG) + Makefile-цель `bench-inference` + `BACKLOG.md`³⁰. НЕ трогала `benchmarks/vector/`, config/*.yaml, docker-compose, deploy/, eval.yml. **Остаток T-037 (задача `[ ]`): local-CPU-часть (2-3 ollama-кандидата) + ADR-3 — ждут домашнюю EPYC-ноду (сейчас занята демо-стеком+eval-раннером, не конкурировала).** Local-раннер готов: `make bench-inference BENCH_ARGS='--local-models qwen3.5:27b,llama3.2:4b'` на ноде с `OLLAMA_BASE_URL`.
- `[регулярная] T-036 §5 (UI-баннер демо) — ЗАВЕРШЕНО кодом+тестами+живой проверкой, закоммичено+запушено (main=acee537). Зона СВОБОДНА.` Тронула: `orchestrator/api.py` (флаг `demo` в `/api/examples`, закоммичено сразу), `web/src/{App,components/Header,i18n,styles.css}`, новый `web/e2e/demo-banner.spec.ts`, `BACKLOG.md`²⁹. НЕ трогала graph.py/worker_client.py/prompts/config/*.yaml/docker-compose/eval.yml.
- ⚠️ **НАХОДКА для оркестратора/владельца (Q-22=A):** self-hosted EPYC eval `29189266539` УПАЛ (25м, exit 1) НЕ по метрикам, а по **правам ФС на раннере**: `Permission denied (os error 13)` при записи в `/home/zzlawlzz/.cache/huggingface/xet/...` → fastembed не смог скачать модель → `RuntimeError`. Раннер-процесс (`epyc-home`) не может писать в `~/.cache/huggingface` (вероятно каталог принадлежит root от прошлого ручного прогона). Также warning `actions/cache` restore auth fail (обычно безвредно). **Фикс — на стороне раннера/воркфлоу (домен оркестратора):** либо `chown -R` кэша под юзера сервиса на ноде, либо в eval.yml задать job-env `HF_HOME`/`HF_HUB_CACHE` в job-локальный (`${{ github.workspace }}/.hf`) писабельный путь. НЕ трогаю eval.yml (твой `51b2f40` 45м назад + твой домен Q-22).

- `[регулярная] T-035 (слой B n8n→Telegram) — ЗАВЕРШЕНО кодом+тестами, закоммичено (fb88c03). Зона СВОБОДНА.` Остаток — только живые крит. на восстановленном стеке (см. журнал + Q-21). Не трогал graph.py/worker_client.py/benchmarks/.

> ⚠️ **ИНФРА (2026-07-12 ~04:40):** диск C: заполнялся под 0 (первопричина — T-037 vector-бенчмарк оркестратора `--scale 90`, см. журнал 04:35), Docker-демон завис → БД недоступна с хоста в это окно. Регулярная сессия независимо тоже освободила ~0.8 ГБ pip-кэша + устаревший TEMP (не трогая `claude`-скретчпад и docker-каталоги). Оркестратор перезапускает Docker; стек поднимается. Durable-рекомендация (перенести Docker data-root на E:) — в Q-21. Пока стек не healthy, живой HTTP-крит T-035 (а) и любой eval заблокированы.
- `[оркестратор] T-031 диспетчер + URL-фикс — orchestrator/graph.py, worker_client.py, api.py-валидатор — ЗАВЕРШЕНО (d9ec7a9 + крит-фикс 2cf07aa). Зона СВОБОДНА.` Остаток T-031 — только живой VPS+WireGuard деплой (deploy/worker-node/). ⚠️ `2cf07aa` обязателен перед ребилдом app (см. журнал 05:25).
- `[оркестратор] T-037 vector-бенчмарк — benchmarks/vector/ — ЗАВЕРШЕНО кодом+отчётом (a6feded). Зона СВОБОДНА.` Инференс-часть T-037 (CPU) ждёт домашнюю ноду. Урок: `--scale`>~20 на dev-машине НЕ гонять (диск C:).
- `[оркестратор] СВОБОДЕН на 05:25` — жду чистый full-eval. ⚠️ **Твои run'ы `29176679531`/`29176449990` были ОБРЕЧЕНЫ:** origin/main отставал на 5 коммитов, фикс `2cf07aa` НЕ был запушен → GH гонял df8061e (баг URL-валидатора) → все 41 кейса 0.0s. **Регулярная сессия запушила (df8061e→81c986e) и передиспетчила eval `29177728775` на исправленном HEAD (06:05).** Это и есть валидация app-фикса + 2-я точка citation для Q-22.
- `[регулярная] диагностика/пуш eval-блокера + T-036 §2 — ЗАВЕРШЕНО (06:20). Зона СВОБОДНА.` Всё запушено (main=`023dcd8`, origin синхронен). Ничего не оставлено незакоммиченным.
- `[регулярная] T-041 root-cause фикс воркера — ЗАВЕРШЕНО кодом+тестами, закоммичено+запушено (main=c200427). Зона СВОБОДНА.` НЕ трогала prompts/worker_react.md (хотспот) — только `workers/react_worker.py` (цикл) + `tests/unit/test_worker.py`. Фикс: кандидат финального ответа = AIMessage БЕЗ tool_calls. Валидация — GH ci-eval `29180927853` (см. ниже).
- `[регулярная] T-036 §1 (demo-лимиты) — ЗАВЕРШЕНО кодом+тестами, закоммичено+запушено (main=52d01c5). Зона СВОБОДНА.` `orchestrator/demo_limits.py` (новый) + wiring в `orchestrator/api.py` + 2 ключа в `config/budgets.yaml`. Не трогала graph.py/worker_client.py/prompts/eval. 16 юнит-тестов, 310 non-slow зелёных.
- `[регулярная] T-036 §3 (security-pass чек-лист + demo-overlay) — ЗАВЕРШЕНО кодом+доками+тестами, закоммичено+запушено (main=d0526a2). Зона СВОБОДНА.` Тронула: `orchestrator/api.py` (docs demo-гейт, `61fa55e` — сразу закоммичено), `web/nginx.conf` (security-заголовки, `ef28bbf`), новые `deploy/demo/SECURITY.md`+`docker-compose.demo.yml` (`f4cc540`), `tests/unit/test_api_demo_hardening.py`, `BACKLOG.md`²⁸. НЕ трогала graph.py/worker_client.py/prompts/config/*.yaml/docker-compose.yml(base). §3-крит закрыт (чек-лист приложен, 8/8 с живыми доказательствами); остаток крита «публичный URL+TLS» = §4 живой CF-туннель (ждёт домен, Q-05).
- `[регулярная] T-041 citation-блокер: причинные фиксы применены + финальная конфигурация, потолок раннера ПОДТВЕРЖДЁН 3 прогонами. Зона СВОБОДНА (main=4a087b4).` Fix A concurrency 2→1 (`f442832`) — реальный выигрыш. Fix B profile-driven `deadline_s` (финал=180, `ee6a02b`) + `CASE_TIMEOUT_S` 720→1200. **3 прогона:** citation 0.333→0.667→0.667 (застрял), faith 0.76→0.985→0.90, guardrail 1.0→0.8→0.6 (нондетерминированные 720s-cap-хиты по РАЗНЫМ кейсам = голодание раннера, НЕ дедлайн). **Тюнинг дедлайна НЕ сходится.** Потолок бьёт И citation, И guardrail → **вариант (1) быстрее раннер теперь ОДНОЗНАЧНАЯ рекомендация** (чинит оба + гейт не трогать; self-hosted на домашней 2×EPYC — бесплатно). Финальная ревалидация (dl180+cap1200) — GH full `29188667946` IN PROGRESS (может ~2.5-3ч из-за cap1200). **НЕ закрывать T-041 до решения владельца (Q-22 финал); пороги НЕ трогать.**

> ✅ **EVAL `29177728775` ЗАВЕРШЁН (success, 19м).** Метрики: numeric=1.0, citation=**1.0**, guardrail=1.0, nodata=1.0, faithfulness=**0.0**. citation 0.417→1.0 подтвердил шумность (Q-22). faithfulness=0.0 — НЕ шум судьи: оба narrative-кейса вернули ПЛЕЙСХОЛДЕР «I'll search X's 10-K» вместо синтеза (см. журнал 08:15). Это и есть корневая причина И citation-, И faithfulness-шума — устранена фиксом воркера.
>
> ✅ **EVAL `29180927853` ЗАВЕРШЁН (ci, `c200427` с фиксом): faithfulness 0.0→0.5.** Фикс РАБОТАЕТ: `aapl_risk_supply_chain` 0.0→**1.0** (судья: «каждый факт прямо в контексте» — плейсхолдер ушёл). Оставшийся `nvda_risk_export_controls`=0.0 — **уже НЕ плейсхолдер**, а честный budget-timeout: ответ «step did not complete within the time limit» (8 цитат извлечены, но воркер исчерпал итерации/дедлайн ДО синтеза под нагрузкой GH-раннера). Это ДРУГОЙ, латентностный класс сбоя (не «I'll search»), нондетерминирован — на здоровых локальных full (id 7-12) оба narrative-кейса синтезируют → 0.93-0.99. Подтверждает: ci с 2 faithfulness-кейсами — ненадёжный гейт, реальный сигнал = full.
>
> ✅ **EVAL `29181312619` ЗАВЕРШЁН (FULL, 41 кейс, фикснутый HEAD): faithfulness=0.7625 ≥ 0.7 — T-041 ДОСТИГНУТ на робастной выборке** (+ локальные full id 7-12: 0.93-0.99). numeric 0.923, guardrail 1.0, nodata 1.0. **НО гейт красный (exit=1) — из-за `citation_coverage=0.333`** (блокирующий порог 1.0), НЕ faithfulness. **Разбор report вскрыл ЕДИНУЮ причину citation-провала И оставшихся faith-нулей = ресурсное голодание GH-раннера:** 8 narrative/multi rag_search вернул 0 цитат (citation 0, но честный ответ → faith 1.0); 2 кейса (nvda/tsla) цитаты есть но budget-timeout до синтеза (citation 1, faith 0). Локально те же кейсы — полные цитаты. **Обновил Q-22:** порог 0.8 НЕдостаточен (0.333); честный фикс = поднять бюджет narrative-воркера / ресурсы раннера, а не грубить порог. Гейт/порог односторонне НЕ трогал — решение владельца+оркестратора. **Оркестратору: faithfulness-часть T-041 готова (снять из non_blocking, Q-16); citation-гейт (Q-22) — оставшийся блокер закрытия, требует твоего/владельца решения по бюджету воркера или порогу.**

---

## Текущее состояние (обновлено 2026-07-15, регулярная сессия — migration-staleness accuracy-fix + empty-answer фикс валидирован на гейте)

**🆕 2026-07-15 ~18:10 (регулярная): sql_query defense-in-depth hardening (`887869e`).** Parse-барьер `_validate_and_rewrite` (T-012) подтверждал top-level SELECT/WITH, но не инспектировал вложенные ноды → data-modifying CTE (`WITH t AS (INSERT/UPDATE/DELETE … RETURNING *) SELECT …`) проскакивал parse-слой (роль `app_ro` держит на execution, но документированный «double barrier» был пробит + агент получал opaque permission-denied вместо самокоррект-хинта). Фикс: reject при `find(Insert/Update/Delete/Merge)`. 4 регресс-теста (stash-верифицированы), 385 non-slow зелёных. Взято как непересекающийся cold-слайс пока оркестратор активно итерирует T-043 web_search backends (Tavily `a36806b`/Brave `683daf3`, <30мин) — их footprint (web_search/web/graph/worker/compose/prompts/config-plumbing) не тронут.

**🆕 2026-07-15 ~поздно (регулярная): migration-staleness accuracy-fix (`a1062a9`, docs-only).** Live-сверка инфры показала: сайт+демо-фронт `ledgerlens.space/app/`+API-дверь `api.ledgerlens.space` все **200**, но старый демо-хост `app.ledgerlens.space` = **NXDOMAIN** (миграция `c46bac3` увела демо на Pages-фронт + VPS-дверь, CF-Tunnel выведен). Три user-facing дока всё ещё асертили выведенную CF-Tunnel/`app.ledgerlens.space`-топологию как текущую → выровнены: CHANGELOG T-036-entry, `deploy/demo/README.md` (CF-рунбук оставлен дженериком + migration-note), `docs/release/v1.0-dod.md` header (датированный аддендум; историчные снимки целы). Дата-историчные acceptance-сноски BACKLOG (²⁹/³¹/³²) сознательно НЕ тронуты. Раннер `epyc-home` online+idle. Бюджет $0, тест-база не затронута (docs-only).

**🆕 2026-07-15 ~06:30 UTC (регулярная): empty-answer фикс ВАЛИДИРОВАН НА ЖИВОМ ГЕЙТЕ.** Валидационный full-eval **`29390701909` (profile=full, release-HEAD `af5c6d1` с фиксом `005b96e`) ЗЕЛЁНЫЙ**, gate exit 0: citation 1.0 / faithfulness 0.862 / guardrail 1.0 / numeric 0.923 / nodata **1.0**, 37/41. **`nodata_ford_net_income` теперь PASS (74.4s, непустой)** — тот самый флейк-кейс из §3. Второй подряд зелёный full-eval на release-HEAD и первый с включённым фиксом → транзиент снят на гейте, не только в коде. Эталонная §3-запись с фиксом = `29390701909`; DoD §3 + BACKLOG T-040 §3 аддендумы промотированы (`0d05a11`, `190cd3b`). **Бонус:** параллельный scheduled full-eval `29392222114` (тоже release-HEAD с фиксом, НЕ dispatch — независимая точка) тоже ЗЕЛЁНЫЙ, gate exit 0: numeric **1.0** / citation 1.0 / faith 0.875 / guardrail 1.0 / nodata **1.0** → ТРЕТИЙ подряд зелёный full-eval на release-HEAD. Демо+сайт 200, 361 non-slow зелёных.

**🆕 2026-07-15 ~день (регулярная): empty-answer робастность-фикс (`005b96e`).** Пустой/whitespace-only синтез-комплишн распространялся до `run_finished` как пустой `answer` (молчаливый не-ответ; на no_data → `nodata_honesty=0.0` red чистого прогона — тот самый `29375595788`/Ford-флейк из §3 addendum). Фикс `orchestrator/graph.py`: `_synthesize` нормализует пустоту в источнике → честное локализованное «нет данных»+дисклеймер; `_finalize` — бэкстоп. Пункт DoD §3 addendum (а) закрыт кодом. 2 регресс-теста, 361 non-slow зелёных, поведение непустых ответов байт-в-байт. Раннер ONLINE, демо+сайт 200.

**🆕 2026-07-15 ~01:40 (регулярная): §3 ЭТАЛОННЫЙ FULL-EVAL НА RELEASE-HEAD ЗЕЛЁНЫЙ.** Подтверждающий full-eval `29378924962` (HEAD `5f63b3d`, рантайм = release-HEAD `df71baa`) завершился success, gate exit 0: citation **1.0** / faithfulness **0.885** / guardrail **1.0** / nodata **1.0** / numeric 0.923, 37/41. Недавние рантайм-правки (+27h…+30h) G4-гейт не сдвинули. **Промоушен §3 сделал оркестратор** (`a587a20`: DoD §3 block-D + BACKLOG-сноска) — регулярная сессия мониторила тот же прогон, коллизия, дубль откатан. DoD-трекер §3 = зелёная эталонная запись на release-HEAD.

**🆕 2026-07-15 ~старт (регулярная): КРАСНАЯ CI на main ПОЧИНЕНА (`5483647`, verified green).** Демо-Pages-split-origin работа оркестратора (`04dd5ea`/`d6fee5b`) добавила поле `cors_allow_origins` в Settings без строки в `.env.example` → `test_env_example_in_sync_with_settings` валил `unit`-джоб → CI красная с `04dd5ea`. Фикс: `CORS_ALLOW_ORIGINS=` в `.env.example` (пустой дефолт). Прогон `29368301922` = build/lint/unit все ✓. Инфра live: раннер ONLINE, демо+сайт 200.



**Гейты:** G1 ✅ G2 ✅ **G3 ✅ (закрыт живьём 2026-07-13, T-031)**. **G4 открыт**
(T-040 `[ ]`). Сделано: T-001…T-034, **T-031/T-032/T-033/T-034 done**,
**🆕 T-041 done (2026-07-14)**. `[~]`: T-035, T-036 (2026-07-14: §1-код/§2/§3/
§4/§5 done+live над публичным демо, остаток = только §1 визуальная
Grafana-проверка на demo-ноде), T-038, T-039. `[ ]`: T-037, T-040.
**DoD-трекер `docs/release/v1.0-dod.md`: 8 из 11 ✅ done** (синк 2026-07-14;
§5 eval-в-CI чист; **§2 промотирован в done +18ч — SSE keep-alive vs CF-idle
измерен живьём, max byte-пауза 10.4 c на 200-с соединении**). Code-ready
осталось 2: §1 чистая-VM, §9 CPU-бенч.

**🆕 2026-07-14 ~+30ч (регулярная): eval-scoring latent-bug фикс (`0ae7966`).** `_sig_digits` брал значащие цифры через `int(round(value))` → на sub-unit значениях (`0.15`→`"0"`, ложный answer-text PASS на любом «нуле») и дробных (`1234.56`→`"1235"`, ложный FAIL) скорил неверно; исправлено на точную Decimal-развёртку. **Behavior-preserving** на текущем наборе (все 13 numeric/multi_step golden — крупные целые → гейт байт-в-байт). 2 регресс-теста git-stash-верифицированы. Инфра: раннер OFFLINE, демо 530 (нода снова down), сайт 200. 359 non-slow зелёных.

**🆕 2026-07-14 ~+28ч (интерактив, с владельцем): intake-гейт вопросов-советов (`6b7ea62`, verified live).** Open-ended вопрос-совет («Во что вложиться чтобы стать богатым?») раньше → неверный «топ-3 CAGR» план → воркер молотил → `budget_exceeded`/частичный. Теперь: детермин. регэксп-гейт (`is_advice_question`, только open-ended) в `_classify` → короткое замыкание в `finalize` с non-advice ответом+дисклеймером, минуя план/воркеров → `succeeded`. Адресные советы про компанию остаются на конвейере. Verified live: ответ за **3с** (было минуты), 0 tool-call/плана. 357 non-slow зелёных.

**🆕 2026-07-14 ~+27ч (интерактив, с владельцем): AG-UI run-abort ПОЧИНЕН (`62197b5`, verified live).** Демо загружалось, но запросы с повторным вызовом одного инструмента в шаге (напр. «Сравни Apple и Microsoft» → 2×`sql_query`) падали с `Cannot send 'TOOL_CALL_END' … No active tool call found`. Корень: адаптер AG-UI спаривал START/END по имени инструмента → перекрытие одноимённых вызовов → осиротевший END с фабрикованным id → клиент рвал ран. Фикс: стабильный `tool_call_id` из воркера, спаривание по нему; orphan-END невозможен. Задеплоено на ноду (rebuild app+worker), **verified live**: run-сравнение `run_finished succeeded` (132с, 4 парных tool-call'а, 0 error, верный ответ AAPL/MSFT+риски).

**🆕 2026-07-14 ~+27ч (интерактив, с владельцем): ДЕМО-502 ПОЧИНЕНО — корень НЕ «app down», а стейл-IP в nginx (`a480f87`).** ⚠️ **КОРРЕКЦИЯ прошлого диагноза (+24ч/+27ч «app-бэкенд down, restart app»):** app-контейнер `lldemo-app-1` был **healthy всё время**. По логам web-контейнера: `/` (статика) = 200, а `/api/*`+`/healthz` = 502 `connect() failed (111: Connection refused) upstream: 172.18.0.6:8000`. Реальный IP `app`=`.0.8`, а nginx долбил `.0.6` — этот IP при пересоздании контейнеров переехал на **worker** (слушает 8081, не 8000). Причина: `proxy_pass http://app:8000` резолвит имя **один раз при старте** и кеширует IP; при рестарте стека (~13:27, web стартовал на 5с раньше app) IP переехали, nginx застрял на старом. **Лечит рестарт `web` (НЕ `app`!).** **Немедленно:** `docker restart lldemo-web-1` → демо 200. **Долговременно (`a480f87`, задеплоено на ноду — rebuild lldemo-web + recreate web):** `web/nginx.conf` → `resolver 127.0.0.11 valid=10s` + `set $app_upstream app; proxy_pass http://$app_upstream:8000$request_uri` во всех 3 proxied-локациях → nginx пере-резолвит `app` каждые ≤10с, переживает смену IP. Проверено живьё: `/healthz`+`/api/examples`=200 через CF-туннель, путь пережил рестарт app без касания web. Демо СНОВА LIVE → демо-зависимые критерии (§2 acceptance, §5 контур) снова разблокированы. Baseline 333 non-slow зелёных.

**🆕 2026-07-14 ~+27ч (регулярная):** инфра live — раннер `epyc-home`=**ONLINE+idle**, сайт=**200**. (демо-502 см. запись интерактива выше — ПОЧИНЕНО.) Baseline 333 non-slow зелёных. **T-038 крит «все ссылки README живые» ✅ закрыт live link-checker'ом** (16 внутр. файлов + 4 anchor + 4 внешних GitHub-URL все 200). **Fresh-reader accuracy-fix (`062bd47`/`8582db5`):** README EN+RU «Project status»+«Known limitations» отставали от as-built — «G1 G2» при закрытом G3, faithsulness-`non_blocking` пункт (T-041 закрыт, промотирован в blocking) и «two-node not live» (G3 живой) удалены; статус→«G1 G2 G3 ✅», delivered→T-001…T-034+T-041.

**🆕 2026-07-14 ~+24ч (регулярная):** инфра-сверка live — сайт `ledgerlens.space`=**200**, раннер `epyc-home`=**ONLINE+idle** (сверено `gh api`), CI/site все зелёные. **⚠️ ДЕМО ЧАСТИЧНО УПАЛО (точный диагноз для владельца):** `app.ledgerlens.space/` (SPA-корень) = **200** (реальный doctype-HTML, web/nginx-контейнер жив), но ВСЕ `/api/*`+`/healthz` = **502 Bad Gateway** (`Server: cloudflare`, CF-RAY). Т.е. EPYC-нода+cloudflared+web-контейнер ЖИВЫ — упал ТОЛЬКО оркестраторный `app`-бэкенд (FastAPI). Лечится **точечно** `docker compose ... restart app` на demo-стеке (НЕ полный ребут ноды), либо app OOM/крашнулся — смотреть логи app-контейнера. Демо-зависимые live-критерии (§2 acceptance/§5-контур) заблокированы до подъёма app. Docker на dev не запущен (нет локального стека). **Взят чисто-локальный docs-accuracy слайс:** `_fetch_alphavantage`-докстринг (`tools/enrich/core.py:233`) утверждал `outputsize=full` (вся история), а код шлёт `outputsize=compact` (free-tier ~100 дней, как верно говорят и `params`-коммент, и модульный докстринг) → выровнено (`3fade24`, запушено). ruff/mypy/pre-commit чисто.

**🆕 2026-07-14 ~+21ч (регулярная):** инфра-сверка live — демо `app.ledgerlens.space`=**200**, сайт `ledgerlens.space`=**200**, раннер `epyc-home`=**OFFLINE** (eval/T-037-local снова заблокированы, за владельцем). Взят непересекающийся публичный-сайт слайс: **исправлены OG/canonical-теги сайта** (`98f9d5f`) — указывали на стейл `github.io`, сайт же на custom-домене → соц-превью-картинка ломалась при шаринге; живо проверено над публичным URL после Pages-redeploy (og:image 200, 0 редиректов).

**🆕 T-041 ЗАКРЫТ 2026-07-14 (регулярная сессия):** ДВА подряд чистых
full-eval на self-hosted EPYC-раннере зелёные — `29297896263` (faithfulness
0.985 / citation 1.0) и `29303631947` (faithfulness 1.0 / citation 1.0), оба
41 кейс, gate exit 0. `faithfulness` промотирован из `non_blocking` в жёсткий
гейт (`config/eval-thresholds.yaml`, порог 0.7 не занижался). **Q-16 и Q-22
закрыты.** Вывод: citation-«потолок» и faithfulness-«шум» были артефактами
голодания бесплатного GH-раннера, не кода — на чистом EPYC обе на максимуме.
Также сверено живьём: раннер `epyc-home` = ONLINE. Корректировка DoD E.2:
`create_react_agent` эмитит `LangGraphDeprecatedSinceV10` в точке вызова
(`react_worker.py:441`), видно в каждом pytest воркера (прошлая заметка
проверяла только импорт).

**T-041 (faithfulness) — фактически достигнут, идёт финальная валидация:**
- faithfulness ≥0.7 на ВСЕХ здоровых прогонах: eval_runs 7=0.989, 8=0.98
  (judge v1), 9=0.935, 10=0.99, 11=0.98 (judge v2). Пять точек, две версии
  судьи. Критерий «стабильно ≥0.7 на ≥2 подряд» выполнен по существу.
- **Блокер закрытия — не качество, а инфраструктура:** локальный eval на
  dev-машине флапает (Docker Desktop host↔container networking роняет
  соединения → в случайные кейсы прилетает `RemoteProtocolError` /
  `network_error` с пустым ответом → метрика обнуляется). Так nodata_honesty
  скакал 1.0→0.8→0.2 БЕЗ изменения качества (в run 11 четыре nodata-кейса —
  чисто network_error). Лечится `docker compose restart <service>`, но
  нестабильность возвращается.
- **Реализовано в T-041:** `_strip_ungrounded` (детерминированный страж,
  5 тестов), bullet-trim v2, judge v2 (числа вне groundedness, пропуск при
  пустом CONTEXT, дата TODAY), worker-промпт v8→v9, rag-докстринг
  (topical-keyword запросы).
- **Найден и исправлен баг golden (T-028):** `nodata_aapl_dividends` считал
  дивиденды отсутствующими, а они в загруженном MD&A (7 чанков) — воркер
  честно их цитировал. Заменён на `nodata_ford_net_income` (Ford не в корпусе,
  утечь не может). `d13a433`.

**T-032 (MOEX) — ✅ DONE (2026-07-12, `0edd241`):** ingest SBER/GAZP/LKOH
(3×642 факта, pluggable доказан). Живьём: «динамика SBER за 3 года» → реальная
MOEX-динамика (+5.9%, пик 327.16 июнь-2024), 5 SQL, без budget_exceeded
(промпт v9 rule 3b + впрыск «сегодня»). RU-дисклеймер в UI (iss_attribution,
APP_MODE=ru). Все 4 живых критерия выполнены.

**T-033 (price_enrich) — ✅ DONE (2026-07-12, `cbfc7cf`):** Q-19 решён —
провайдер сменён на Alpha Vantage (ключ в .env). Живьём: динамика AAPL за
месяц из реальных цен AV; кэш подтверждён (source=db, cached=true без AV-вызова);
относительные даты работают (впрыск «сегодня» в _render_task). Ограничение
free-tier: только compact (~100 дней), история за старые годы — премиум
(документировано, не блокер).

**T-037 (бенчмарки) — §2 vector ✅ (`a6feded`), §1 инференс API-часть ✅
(`9549315`, живые deepseek-числа + харнесс `benchmarks/inference/`).** Остаток
`[ ]`: local-CPU-часть (2-3 ollama-кандидата на EPYC) + ADR-3 — ждут ноду
(раннер готов: `make bench-inference BENCH_ARGS='--local-models …'`).

**Стек:** 9 контейнеров, ollama НАМЕРЕННО выгружен на время eval-прогонов
(снимает RAM-давление → OpenBLAS OOM не рушит прогоны). Вернуть `docker compose
--profile local up -d ollama` для проверок T-017/локального тира.

---

## План дальнейших шагов (приоритет сверху)

1. **Закрыть T-041:** дождаться чистого прогона eval на **GitHub Actions**
   (не локально! — dispatch: `gh workflow run eval.yml`; прогон
   29173351633 запущен 2026-07-12 00:12 UTC). Нужно 2 подряд full-прогона
   ≥0.7 на чистом раннере. Если faithfulness держит (а он держит) — снять
   `faithfulness` из `non_blocking` в `config/eval-thresholds.yaml`, обновить
   комментарий-обоснование, закрыть Q-16, пометить T-041 done + сноска.
   ⚠️ Прошлый GH-dispatch (29156847284) УПАЛ — разобраться почему (лог
   `gh run view 29156847284 --log-failed`), возможно секрет/данные/снапшот.
2. **Закрыть T-032:** живая проверка SBER-вопроса (см. выше). Если ок —
   `[done]` + сноска. RU-дисклеймер в UI проверить визуально/Playwright.
3. **T-033:** ждёт Q-19 (провайдер цен). Без ответа владельца — не двигать.
4. **T-031 (гейт G3, критический путь) — `[wip]`, задел готов:** решение —
   **WireGuard-меш** (домашняя нода за NAT). Сделано (deploy/worker-node/):
   compose.worker.yml, .env.worker.example, runbook README.md; валидатор
   assert_worker_url_secure (не-localhost URL → https/приватный, 10 тестов).
   **ОСТАЛОСЬ:** ~~(1) диспетчер — маршрутизация+failover~~ ✅ **ГОТОВО**
   (`d9ec7a9`: round-robin + local-preferred failover, Q-20 дефолт, 3 теста);
   (2) живой деплой на 104.238.24.196 + WireGuard на домашней ноде (аккуратно,
   не рушить demo-стек); (3) живые критерии G3. Нода готова (Q-18), ключ
   id_ed25519_worker_node. **Смена root-пароля на ноде — за владельцем**
   (был показан в чате). Живой деплой — единственный оставшийся блокер G3,
   требует сетевых операций на VPS (за владельцем/интерактивной сессией).
5. Далее по бэклогу: T-035 (мониторинг слой B / Telegram), T-036 (seed-снапшот),
   T-037 (CPU-бенчмарк инференса на домашней ноде), T-038 (витрина/сайт).

---

## Журнал (append-only, новые записи сверху)

### 2026-07-15 ~18:10 UTC+3 · регулярная сессия — 🔒→✅ sql_query defense-in-depth: parse-барьер пропускал data-modifying CTE (writable-CTE)
Старт по протоколу: `git pull` (up to date, HEAD `683daf3`), COORDINATION, сверка с РЕАЛЬНОСТЬЮ (`git log`, eval_runs БД — локально до id 12, CI-full-evals на GH Actions как и раньше). **Защита от параллельной сессии сработала:** последние 4 коммита (`683daf3` Brave / `a36806b` Tavily / `db4cdaa` / `33d5186`) — оркестратор, **683daf3 в 17:58, 7 мин назад** → оркестратор АКТИВНО итерирует T-043 web_search backends (добавил Tavily+Brave ПОСЛЕ отметки зоны свободной в 17:22, не перезастолбив). Рабочее дерево чистое, но <30мин → взяла полностью непересекающийся **cold-файл** `tools/sql/core.py` (последний коммит 4 дня назад), обошла ВЕСЬ их footprint (web_search/web/graph/worker/compose/prompts/config-plumbing/common.config/.env.example). **Находка (defense-in-depth gap, T-012 sql_query):** `_validate_and_rewrite` проверял `isinstance(statement, exp.Query)` (SELECT/WITH) + блок INTO/locks, но НЕ инспектировал вложенные ноды → data-modifying CTE `WITH t AS (INSERT/UPDATE/DELETE … RETURNING *) SELECT * FROM t` проскакивал parse-барьер. Реальный барьер (роль `app_ro` без write-грантов) держит на execution, НО документированный «double barrier» был пробит на parse-слое, и агент получал opaque `permission denied` вместо самокоррект-observation. Живьё подтверждено ДО фикса пробой `_validate_and_rewrite`: 3 writable-CTE (INSERT/UPDATE/DELETE) ALLOWED. **Фикс (`887869e`):** `statement.find(exp.Insert, exp.Update, exp.Delete, exp.Merge)` — reject при DML в любом месте дерева → честный hint «read-only, including inside CTEs; rewrite as plain SELECT»; benign read-only CTE (`WITH t AS (SELECT …)`) не задет, JOIN/подзапросы не задеты. 4 регресс-теста (3 writable-варианта параметризованы + 1 benign-guard) в существующем `test_sql_tool_validation.py` (паттерн `_ExplodingFactory` — падает если дошло до БД); git-stash-верифицированы: без фикса 3 падают на `_ExplodingFactory`, benign проходит в обоих. ruff+mypy чисто, **385 non-slow зелёных** (381+4). Тронула ТОЛЬКО `tools/sql/core.py` + `tests/unit/test_sql_tool_validation.py` (оба cold, не хотспоты) + COORDINATION + память. НЕ трогала prompts/config/compose/api.py/graph/worker/web/web_search/eval.yml/пороги. Бюджет $0 (без LLM, тесты на фейках). Зона свободна.

### 2026-07-15 ~поздно · регулярная сессия — 📄→✅ migration-staleness accuracy-fix: user-facing доки перестали асертить выведенный CF-Tunnel + `app.ledgerlens.space` (NXDOMAIN)
Старт по протоколу: `git pull` (up to date, HEAD `c46bac3`), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **Инфра live (не память):** раннер `epyc-home`=**ONLINE+idle** (`gh api`), сайт `ledgerlens.space`=**200**, демо-фронт `ledgerlens.space/app/`=**200**, API-дверь `api.ledgerlens.space/healthz`+`/api/examples`=**200**. `git status` чист (нет активной параллельной сессии). **НАХОДКА при live-сверке:** старый демо-хост `app.ledgerlens.space` = **NXDOMAIN** (миграция бэкенда EPYC→воркстейшн `c46bac3` увела демо на Pages-фронт `/app/` + VPS-дверь `api.` с Let's Encrypt, CF-Tunnel выведен из SSE-пути). Оркестраторская доко-переработка (`93bd1dd`/`9dca58c`) обновила README EN/RU + ARCHITECTURE §7.1 + site, но ТРИ других user-facing дока остались асертить выведенную топологию как ТЕКУЩУЮ. **Фикс (`a1062a9`, docs-only, запушено):** (1) `CHANGELOG.md` `[Unreleased]`→v1.0 T-036-entry «live demo at app.ledgerlens.space behind a Cloudflare Tunnel» → переписан на актуальную топологию (`ledgerlens.space/app/` + VPS-дверь, DNS-only+LE, без CDN в SSE) + линк `docs/migration-workstation.md`; (2) `deploy/demo/README.md` — CF-Tunnel-рунбук ОСТАВЛЕН как валидный дженерик self-host-вариант, но добавлена migration-note над §4 (почему этот репо ушёл с CF: proxy рвал SSE в браузере) + исправлена «current demo domain app.ledgerlens.space» строка на актуальную; (3) `docs/release/v1.0-dod.md` header — датированный аддендум 2026-07-15 (историчные «HTTP 200»-снимки от 2026-07-14 НЕ трогала — дата-корректны). Оставшиеся `app.ledgerlens.space` в `BACKLOG.md` — дата-историчные acceptance-сноски (²⁹/³¹/³²), сознательно НЕ трогала (переписать = фальсифицировать запись приёмки). Все 3 добавленных линка на migration-doc резолвятся. Diff docs-only (`git diff --name-only` = 3× .md), 0 файлов кода → тест-база не затронута. Тронула ТОЛЬКО `CHANGELOG.md`+`deploy/demo/README.md`+`docs/release/v1.0-dod.md` (не хотспоты) + COORDINATION + память. НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web/пороги/код/тесты. Бюджет $0 (без LLM). **Остаток за владельцем/EPYC:** §1 чистая-VM, §9 CPU-бенч+ADR-3 (T-037 local), §4 тег v1.0.0/Release/branch-protection, §10 gif. Зона свободна.

### 2026-07-15 ~14:10 UTC · регулярная сессия — 🐞→✅ empty-answer симметрия: guardrail-ресинтез-путь тоже нормализует пустой/whitespace ответ (латентный gap, парный к `005b96e`)
Старт по протоколу: `git pull` (up to date, HEAD `c7740d1`), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **Инфра live (не память):** раннер `epyc-home`=**ONLINE+idle** (`gh api`), сайт `ledgerlens.space`=**200**, демо `/healthz`=**000** (curl fail — оркестратор в ЭТОТ момент, судя по всему, деплоит live SSE-фикс, не мой домен). **⚠️ КОЛЛИЗИЯ-ПРЕВЕНЦИЯ:** `git status` — 2 незакоммиченных файла оркестратора `orchestrator/api.py`+`docs/migration-workstation.md` (SSE `no-transform` split-origin фикс), правлены ~12:14 (>30мин, но явно их активная зона по прошлой записи) → **НЕ трогала** их и их падающий `tests/unit/test_sse_heartbeat.py`. Взяла непересекающийся слайс в `orchestrator/graph.py` (в дереве чист, последняя правка `005b96e` часами ранее). Baseline на старте: ruff+mypy чисто, **362 non-slow passed + 1 fail** (orchestrator-owned SSE-тест). **НАХОДКА (реальный латентный дефект, парный к прошлому фиксу):** empty-answer-фикс `005b96e` нормализовал пустой/whitespace синтез в `_synthesize` + бэкстоп `_finalize`, но **guardrail advice-ресинтез** (`answer = response.text`, при срабатывании non-advice guardrail с одним ре-синтезом) остался БЕЗ нормализации. Трассировка: `_synthesize` даёт непустой draft → guardrail детектит advice → ре-синтез под нагрузкой возвращает `"   "` → `find_advice_spans("   ")=[]` (не refuse) → строка `answer = f"{answer}\n\n_{disclaimer}_" if answer else answer`: `"   "` truthy → `"   \n\n_disclaimer_"` → `_finalize` видит `.strip()` непустым (дисклеймер) → **near-blank ответ доходит до run_finished**. Прошлая сессия закрыла whitespace-случай ТОЛЬКО в `_synthesize`; симметричный ресинтез-путь пропущен. **Фикс (`bfb2e4b`, запушено):** ресинтез нормализуется как `_synthesize` (`.strip()` → честный локализованный fallback при пустоте) + DRY — 3 fallback-места (`_synthesize`/`_finalize`/guardrail) сведены в модульный `_empty_answer_fallback(question)` (байт-в-байт тот же текст, EN/RU по `is_russian`). Поведение непустых ответов не тронуто. **Регресс-тест git-stash-верифицирован:** `test_empty_guardrail_resynthesis_yields_honest_fallback_not_blank` (advice-первый-синтез → пустой ресинтез через сценарный `GuardVerdict(advice=True)`) падает pre-fix ровно с near-blank `"   \n\n_This is financial analytics, not investment advice._"`, проходит с фиксом. ruff+mypy чисто (130 files), **363 non-slow зелёных** (362+1; red — тот же orchestrator-owned SSE-тест, не мой). Тронула ТОЛЬКО `orchestrator/graph.py` (хотспот, коммичен сразу) + `tests/unit/test_orchestrator_graph.py` (+COORDINATION+память). НЕ трогала api.py/prompts/config/compose/worker/eval.yml/web/пороги/eval-логику. Бюджет $0 (без LLM; тесты на фейках). **Остаток за владельцем/EPYC:** §1 чистая-VM, §9 CPU-бенч+ADR-3 (T-037 local), §4 тег v1.0.0/Release/branch-protection, §10 gif. Зона свободна.

### 2026-07-15 ~12:10 UTC · регулярная сессия — ✅ gate-safety регресс-тесты (транзиентный сбой прогона → red, не false-green) + документирована narrative-асимметрия
Старт по протоколу: `git pull` (up to date, HEAD `704de3e`), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **Инфра live (не память):** раннер `epyc-home`=**ONLINE+idle** (`gh api`), сайт `ledgerlens.space`=**200**, демо `/healthz`=**000** (curl fail — оркестратор в ЭТОТ момент пересобирал демо-образы под SSE-фикс, не мой домен). Baseline: ruff+mypy чисто (130 files), **360 non-slow passed + 1 fail** — упавший `tests/unit/test_sse_heartbeat.py::test_sse_headers_disable_buffering` = **симптом незакоммиченной работы ОРКЕСТРАТОРА** (их правка `_sse_headers` → `Cache-Control: no-cache, no-transform`, тест ещё не обновлён), НЕ моя. **⚠️ КОЛЛИЗИЯ-ПРЕВЕНЦИЯ (30-мин правило):** `git status` показал 2 незакоммиченных файла, менявшихся <30мин назад — `orchestrator/api.py` (**хотспот**, 11:33) + `docs/migration-workstation.md` (11:49): оркестратор АКТИВНО чинит live SSE/Cloudflare split-origin баг (браузер вешался на «Строим план», CF буферизует gzip → рвёт SSE; фикс = `no-transform`). **НЕ трогала ни api.py, ни migration-doc, ни их падающий SSE-тест** — взяла непересекающийся test-only слайс в eval-домене (моём). **Сделано (`d536943`, запушено):** 2 регресс-теста в `tests/unit/test_eval_run.py` пинят инвариант gate-safety `_aggregate`/`_check_thresholds`. **Разбор `eval/run.py`:** `_score_case` на non-succeeded статусе (в т.ч. `network_error` от `_ask` после N ретраев — задокументированный транзиент Docker-Desktop-сети) пишет `passed=False`, `scores={}`; `_aggregate` считает `numeric_accuracy`/`guardrail_block`/`nodata_honesty` из `r.passed` → метрика→**0.0**→threshold-violation→**red**. Это ПРАВИЛЬНО и должно так остаться: исключать транзиенты из метрики опасно — полностью-лежачий app (все метрики→None→skip) стал бы **false-green** (хуже false-red, который просто переганяется). Тест №1 (`test_transient_run_failure_gates_red_not_false_green`) защищает ровно от такой будущей «оптимизации». Тест №2 (`test_narrative_transient_failure_excluded_from_faithfulness`) **документирует реальную АСИММЕТРИЮ** (surfaced владельцу, НЕ фикшена): narrative `faithfulness`/`citation_coverage` берутся из `r.scores` → при сбое пустой scores → **исключается** (метрика=None), а не зануляется как pass-based категории. В full-eval это ловят другие категории; narrative-only-прогон с тотальным сбоем проскочил бы гейт — краевой случай, приемлемый т.к. реальный гейт всегда микс. Оба теста верифицированы (падали бы, если б метрики стали None вместо 0.0). Test-only, поведение eval НЕ тронуто (гейт байт-в-байт). ruff+mypy чисто, **362 non-slow зелёных** (единственный red — orchestrator-owned SSE-тест, их незакоммиченная работа). Тронула ТОЛЬКО `tests/unit/test_eval_run.py` (не хотспот) + COORDINATION (+память). НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web/пороги/eval-логику. Бюджет: $0 (без LLM). **Остаток за владельцем/EPYC:** §1 чистая-VM, §9 CPU-бенч+ADR-3 (T-037 local), §4 тег v1.0.0/Release/branch-protection, §10 gif. Зона свободна.

### 2026-07-15 ~06:30 UTC · регулярная сессия — ✅ empty-answer фикс ВАЛИДИРОВАН НА ГЕЙТЕ (`29390701909` зелёный, `nodata_ford` PASS) → промоушен обоих docs-трекеров
Старт по протоколу: `git pull` (up to date, HEAD `c25c51b`), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **Инфра live (не память):** раннер `epyc-home`=**ONLINE+busy** (гнал scheduled-eval `29392222114`, стартовал 05:44), демо `/healthz`+сайт=**200**. Dev-Docker не запущен (eval_runs — self-hosted на БД EPYC). **ГЛАВНАЯ НАХОДКА:** claim'нутый прошлой сессией валидационный full-eval **`29390701909` (workflow_dispatch, profile=full, release-HEAD `af5c6d1` с empty-answer-фиксом `005b96e`) ЗАВЕРШИЛСЯ SUCCESS 05:08→06:25, gate exit 0** «no blocking thresholds violated». Разбор лога GH: citation **1.0** / faithfulness **0.862** / guardrail **1.0** / numeric 0.923 / nodata_honesty **1.0**, 37/41; **`nodata_ford_net_income` теперь PASS (74.4s, непустой)** — ровно тот кейс, что ронял 1-й reference `29375595788` пустым answer (`nodata_honesty=0.8` red). Это ВТОРОЙ подряд зелёный full-eval на release-HEAD (после `29378924962`, но тот был на `df71baa` ДО фикса) и ПЕРВЫЙ, где включён именно empty-answer-фикс → транзиент снят на живом гейте, не только в коде. **Промоушен (docs-only, 2 коммита, запушены):** DoD §3-аддендум (`0d05a11`) + BACKLOG T-040 §3-аддендум (`190cd3b`) — оба фиксируют эталонную §3-запись с фиксом = `29390701909`. Baseline на HEAD сверен: ruff+mypy чисто (pre-commit skip на docs), **361 non-slow зелёных** — release-gate инвариант держится. Тронула ТОЛЬКО `docs/release/v1.0-dod.md` + `BACKLOG.md` (не хотспоты) + COORDINATION + память. НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web/пороги. Бюджет: $0 (eval уже был прогнан прошлой сессией; я только читала лог+доки). **Не тронула scheduled-eval `29392222114`** (routine, runner busy — не мешала). **Остаток за владельцем/EPYC:** §1 чистая-VM, §9 CPU-бенч+ADR-3 (T-037 local), §4 тег v1.0.0/Release/branch-protection, §10 gif. Зона свободна.

### 2026-07-15 ~день · регулярная сессия — 🐞→✅ empty-answer робастность-фикс (пустой синтез → пустой `run_finished.answer` → nodata_honesty=0.0 red; закрыт пункт DoD §3 addendum (а))
Старт по протоколу: `git pull` (up to date, HEAD `8f496a3`), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **Инфра live (не память):** раннер `epyc-home`=**ONLINE+idle** (`gh api`), демо `/healthz`+сайт=**200** (восстановлены против +30ч-записи 530). Dev-Docker не запущен (`docker ps` завис → карантин; eval_runs пишет self-hosted в БД EPYC). Baseline на HEAD: ruff+mypy чисто, **359 non-slow passed** (release-gate инвариант держится). Tree clean, коллизий нет (последний коммит оркестратора `8f496a3` — мониторинг §3, >3ч). **Все открытые `[ ]` (T-037 local-CPU, T-040 §1 чистая-VM/§9 CPU-бенч/§4 релиз-тег) owner/железо-блокированы; §9-бенч на demo-ноде unattended рискован (память: 2× hard-freeze).** Взяла безопасный локальный слайс — **реальный код-дефект, флагнутый самим DoD-трекером** (§3 addendum, пункт (а)): `nodata_ford_net_income` в 1-м reference full-eval `29375595788` вернул ПУСТОЙ `answer` при succeeded → `nodata_honesty=0.0` → red чистого прогона (транзиент, на повторе PASS). **Корневой разбор `orchestrator/graph.py`:** `_synthesize` возвращал `{"answer": response.text}` без защиты; редкий пустой/whitespace-only комплишн LLM (под нагрузкой) распространялся через `_guardrail` (skip на пустом) до `run_finished` (`api.py:318` `answer=final_state.get("answer","")`) как молчаливый пустой ответ. На no_data-кейсе структурно passed (`invented:[]`), но judge `NO_DATA_HONESTY_RUBRIC`→0.0 «ANSWER is empty» → gate red. **Фикс (3 коммита, запушены, main=`005b96e`):** (1) `_finalize` бэкстоп — пустой answer → честное локализованное «нет данных»+дисклеймер; (2) `_synthesize` нормализация в источнике (ловит и whitespace-only, который `_finalize` не различал бы после guardrail-дисклеймера) → EN/RU fallback-константы; guardrail дальше штатно вешает дисклеймер. **Whitespace-случай вскрыл вторичный gap** (guardrail `if answer` truthy на `"   "` → near-blank `"   \n\n_disc_"`) → потому нормализация в `_synthesize`, а не только `_finalize`. **2 регресс-теста** (EN empty, RU whitespace-only): git-stash-верифицированы (RU падает без `_synthesize`-нормализации), EN-случай логически падает на true pre-fix (пустой `""`→`.strip()` falsy). ruff+mypy чисто, **361 non-slow зелёных** (359+2). Поведение для непустых ответов байт-в-байт (гейт НЕ двигается). DoD §3 addendum обновлён (пункт (а) закрыт кодом; (б) допуск ≥0.75/ретрай — опциональный defense-in-depth за владельцем). Тронула ТОЛЬКО `orchestrator/graph.py` (хотспот, коммичен сразу) + `tests/unit/test_orchestrator_graph.py` + `docs/release/v1.0-dod.md` (+COORDINATION+память). НЕ трогала prompts/config/compose/api.py/worker/eval.yml/web/пороги. Бюджет: $0 (без LLM; тесты на фейках). **Остаток за владельцем/EPYC:** local-CPU-бенч T-037/§9/ADR-3, §1 чистая-VM, §10 gif, §4 тег v1.0.0/Release/branch-protection. Зона свободна.

### 2026-07-15 ~01:06–01:40 UTC · регулярная сессия — ✅ §3 эталонный full-eval на release-HEAD ЗЕЛЁНЫЙ (мониторинг до завершения; промоушен сделал оркестратор — коллизия, откатала дубль)
Старт по протоколу: `git pull` (up to date, HEAD `084577c`), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **Инфра live (не память):** раннер `epyc-home`=**ONLINE+busy** (гнал `29378924962`), демо `/healthz`+сайт=**200**, локальный baseline на HEAD — ruff+mypy чисто, **359 non-slow passed** (release-gate инвариант держится). Dev-Docker не запущен (eval_runs пишет self-hosted в БД EPYC). Продолжила ранее заклеймённую §3-задачу: **подтверждающий эталонный full-eval `29378924962` на release-HEAD** (1-й прогон `29375595788` был red на транзиентном nodata-флейке, не регрессия). Обновила active-zone-строку + застолбила мониторинг (`1740e50`). Фоновый polling (90с × до завершения) → в **01:35:29 прогон завершился `completed success`**. Разбор лога GH: gate exit 0 «eval OK: no blocking thresholds violated», метрики citation **1.0** / faithfulness **0.885** / guardrail **1.0** / nodata_honesty **1.0** / numeric 0.923, 37/41 pass; ВСЕ 5 nodata-кейсов PASS (в т.ч. `nodata_ford_net_income` 25.5s — тот, что флейкнул пустым в 1-м прогоне → чисто на повторе). headSha `5f63b3d` = `df71baa` + 2 docs-only коммита → рантайм байт-в-байт с release-HEAD. **Вывод: недавние рантайм-правки (+27h…+30h: agui `tool_call_id`, advice-gate, eval-scoring, CORS) G4-гейт НЕ сдвинули; эталонный full-eval на релизном коде зелёный.** **⚠️ КОЛЛИЗИЯ:** пока я оформляла промоушен DoD §3, оркестратор (интерактивная сессия, общий рабочий каталог) мониторил тот же прогон и промотировал §3 первым — коммит **`a587a20`** @ 01:35:54 UTC (DoD §3 block-D аддендум + BACKLOG-сноска +15 строк, те же метрики/вывод), уже в origin/main. Мой параллельный edit `docs/release/v1.0-dod.md` стал чистым дублем → **откатала `git checkout -- docs/release/v1.0-dod.md`** (не запушен, конфликта в истории нет). Тронула ТОЛЬКО COORDINATION + память. НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web. Бюджет: $0 (без LLM; eval-стоимость на EPYC-раннере). **Урок протокола:** при мониторинге долгого внешнего прогона обе сессии могут дойти до промоушена одновременно — победил тот, кто закоммитил первым; back-off отработал чисто. Зона свободна.

### 2026-07-15 ~старт · регулярная сессия — 🔴→🟢 КРАСНАЯ CI на main ПОЧИНЕНА (`.env.example` рассинхрон с Settings после Pages-split-origin работы)
Старт по протоколу: `git pull` (up to date, HEAD `d6fee5b`), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **Инфра live (не память):** раннер `epyc-home`=**ONLINE+idle** (`gh api`), демо `app.ledgerlens.space/healthz`=**200**, сайт `ledgerlens.space`=**200** (всё восстановлено против +30ч-записи 530). Tree clean, коллизий нет (последний коммит оркестратора `d6fee5b` = 23:28, >3ч назад, ничего незакоммиченного → 30-мин правило не задето). **⚠️ ГЛАВНАЯ НАХОДКА — `ci.yml` КРАСНАЯ на main 2 коммита подряд** (`29365828353`, `29365034910`): `unit`-джоб падал на `tests/unit/test_config.py::test_env_example_in_sync_with_settings` — `AssertionError: only in Settings: ['CORS_ALLOW_ORIGINS']`. **Корень:** демо-frontend-на-Pages работа оркестратора (`04dd5ea`/`d6fee5b`) добавила поле `cors_allow_origins: str = ""` в `common/config.py` (Settings), но НЕ добавила соответствующую строку в `.env.example`; тест-инвариант требует, чтобы каждое Settings-поле имело ключ в `.env.example`. CI красная с `04dd5ea`. **Фикс (`5483647`, запушено):** добавлен `CORS_ALLOW_ORIGINS=` (пустой дефолт = deny-by-default, совпадает с дефолтом поля `""`) в блок `# --- Application ---`, комментарий на своей строке (по контракту файла: python-dotenv не срезает trailing-комментарий у unquoted-значений). Поведение не тронуто (чистый config-sync). **Верификация:** локально `test_config` 15/15 зелёных (было 12 pass/1 fail), **359 non-slow passed**, ruff format+check+mypy чисто. **Живая верификация:** push → CI-прогон `29368301922` → **build ✓ / lint ✓ / unit ✓, exit 0 (ЗЕЛЁНАЯ)**. Восстановлена предпосылка G4 §4 (branch-protection требует зелёную CI — на красной нельзя). Тронула ТОЛЬКО `.env.example` (не хотспот) + COORDINATION + память. НЕ трогала prompts/config-yaml/compose/api.py/graph/worker/eval.yml/web/common/config.py (само поле — работа оркестратора, не трогала). Бюджет: $0. Зона свободна.

### 2026-07-14 ~+30ч · регулярная сессия — 🐞→✅ eval-scoring latent-bug: `_sig_digits` для sub-unit/дробных значений (behavior-preserving на текущем наборе)
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **Инфра live (не память):** раннер `epyc-home`=**OFFLINE**, демо `app.ledgerlens.space`=**530** (Cloudflare origin unreachable → EPYC-нода снова down/недоступна — регрессия против +27ч-записи, за владельцем ребут ноды+раннер), сайт `ledgerlens.space`=**200**, последние 4 eval-прогона success. Dev-Docker не запущен. **Все live/hardware-критерии заблокированы** (T-037 local-CPU, эталонные eval, демо-зависимые §§). Baseline: ruff+mypy чисто, **357 non-slow зелёных** на HEAD `1ee22c7`. Tree clean, коллизий нет. **Взяла чисто-локальный слайс — реальный латентный баг в eval-скоринге** (не пороги/гейт-конфиг — сама scoring-логика). **Аудит-путь:** прошлась по gate-critical `eval/scoring.py`, `tools/enrich/core.py` (парсинг AV — солиден, config vs code default 25/1.0/15 сходятся), TODO-скану (пусто), хотспотам (все свежие правки — guardrail/graph/agui/react_worker, обхожу), ARCHITECTURE/PLAN (design-доки, единственный `[OPEN]` = local-CPU-модель T-037, актуален — не стейл). **Найден баг:** `_sig_digits(value)` брал значащие цифры через `int(round(abs(value)))`, что верно ТОЛЬКО на целочисленном пути. Для sub-unit значения (`value: 0.15` — маржа/EPS/ratio-as-value, схема `NumericExpectation.value: float` это допускает) → `int(round(0.15))=0` → `"0"`, а answer-text-фолбэк `score_numeric` делает `"0" in _digits(answer)` → **ложный PASS на любом ответе, содержащем ноль** (проверено: `"0" in "040"`=True). Для дробного >1 (`1234.56`) → `int(round)=1235` → `"1235"`, которого нет в `"123456"` реального ответа → **ложный FAIL**. Тот же `_sig_digits` бьёт и `score_multi_step_structural(key_numbers)`. **Фикс (`0ae7966`, запушено):** значащие цифры через точную десятичную развёртку — `_digits(format(Decimal(str(abs(value))),"f")).lstrip("0")[:n]` (Decimal(str(...)) избегает float-repr-шума, `format "f"` разворачивает, lstrip убирает лидирующие нули из дробной части). **Behavior-preserving на текущем наборе:** ВСЕ 13 numeric/multi_step golden — крупные целые (416161000000 … ратио 215938000000/130497000000), для которых новая и старая формулы дают идентичную строку → **гейт НЕ двигается** (подтверждено зелёным полным набором + тем, что фикс меняет вывод только для non-integer, а таких в cases.yaml нет). **2 регресс-теста git-stash-верифицированы** (падают на старом round-коде, проходят на новом): sub-unit-wrong (`0.40`-ответ на `0.15`-golden — старый спурьёзно проходит), fractional (`1234.56` — старый ложно валит). ruff+mypy чисто, **359 non-slow зелёных** (357+2). Тронула ТОЛЬКО `eval/scoring.py` + `tests/unit/test_eval_scoring.py` (+COORDINATION+память). НЕ трогала thresholds/`eval-thresholds.yaml`/eval.yml/prompts/config/compose/api.py/graph/worker/web. Бюджет: $0 (без LLM). **Остаток за владельцем/EPYC:** поднять ноду+раннер (демо 530→восстановить, эталонные eval, T-037 local-CPU); §1 чистая-VM, §9 CPU-бенч+ADR-3, §4 тег v1.0.0/Release/branch-protection. Зона свободна.

### 2026-07-14 ~+28ч · интерактивная сессия (с владельцем) — ✨ intake-гейт для вопросов-советов (open-ended) → мгновенный non-advice ответ вместо budget_exceeded (verified live)
Владелец прислал скриншоты: демо на вопрос **«Во что вложиться чтобы стать богатым?»** строило огромный неверный план («топ-3 компаний США по CAGR → выручка/прибыль/FCF по каждой → риски → цены»), воркер молотил 14 sql_query, упирался в лимит итераций → `budget_exceeded`, частичный ответ (плохой UX, жёг бюджет). Разбор по логам: это **вопрос про инвестиционный совет** (вне скоупа демо) без анализируемой цели → планировщик флудил. **По просьбе владельца реализован input-side advice-гейт (`6b7ea62`, verified live).** **Что сделано:** (1) `config/guardrail_patterns.yaml` — новый блок `question_patterns` (RU+EN, регэксп, **только open-ended** запросы: «во что/куда вложить», «какие акции купить», «как разбогатеть», «what/where to invest», «which stocks to buy», «how to get rich»). (2) `orchestrator/guardrail.py` — `is_advice_question()` (детермин. регэксп-гейт, без LLM) + `build_advice_question_refusal()` (локализованный non-advice ответ + дисклеймер, предлагает in-scope примеры). (3) `orchestrator/graph.py` — в `_classify` короткое замыкание ДО LLM-роутинга: advice-вопрос → answer=refusal, `advice_refused=True`, эмит `guardrail{action:refuse_advice_question}`, роут прямо в `finalize` (минуя plan/execute/synthesize) → `status=succeeded`. **Скоуп узкий:** только open-ended; **адресные советы про конкретную компанию** («Стоит ли покупать Apple?», «good time to buy Tesla?») ОСТАЮТСЯ на конвейере (он отвечает фактами+output-refusal). Что проскочит — ловит существующий `check_advice` на ответе (defense-in-depth). **Eval-импакт минимальный:** из 5 guardrail-golden-кейсов только open-ended `guardrail_allocate_ru` теперь короткозамыкается (и проходит structurally — в refusal нет «купите»/«рекомендую купить»); остальные 4 (адресные) держат прежний путь. **Тесты:** 11 advice/9 non-advice детект-кейсов + refusal-локализация/advice-free + graph-тест (0 planner/worker вызовов). 357 non-slow зелёных, ruff+mypy чисто. **Деплой:** scp 3 файлов на ноду (совпадали с pre-change baseline по sha256) → rebuild lldemo-app → recreate. **VERIFIED LIVE** (публичный `/agui`, тот самый вопрос): ответ за **3 секунды** (было — минуты до budget_exceeded), поток RUN_STARTED→guardrail{refuse_advice_question}→TEXT_MESSAGE→RUN_FINISHED, **0 TOOL_CALL, 0 плана**, `status=succeeded`, вежливый non-advice текст + дисклеймер + in-scope примеры. Тронула ТОЛЬКО `config/guardrail_patterns.yaml`, `orchestrator/guardrail.py`, `orchestrator/graph.py` + 2 теста (+COORDINATION+память). **⚠️ Хотспот:** guardrail.py/graph.py — правя, учесть новый `advice_refused` в OrchestratorState + роут `_route_after_classify` (classify→finalize).

### 2026-07-14 ~+27ч · интерактивная сессия (с владельцем) — 🐞→✅ AG-UI run-abort на параллельных вызовах одного инструмента ПОЧИНЕН (verified live)
Владелец прислал скриншот: демо загружается, но запрос падает с `Error: Cannot send 'TOOL_CALL_END' event: No active tool call found with ID '…'. A 'TOOL_CALL_START' event must be sent first`. **Прикладной баг (не инфра).** Корень: `TraceToAgUiAdapter` (`orchestrator/agui.py`) спаривал TOOL_CALL_START/END по **имени** инструмента (`_open_tool_calls: dict[tool_name→id]`). Когда один инструмент вызывается >1 раза в одном ReAct-шаге с перекрытием (ReAct-агент гонит несколько tool-call'ов из одного хода конкурентно — напр. «Сравни Apple **и** Microsoft» → два `sql_query`), второй START затирал id первого под общим ключом; осиротевший finish не находил id → **фабриковал новый uuid** и слал `TOOL_CALL_END` без START → `@ag-ui/client` рвал весь ран. **Фикс (`62197b5`, запушено + задеплоено на ноду):** воркер (`react_worker._traced_call`) теперь ставит стабильный `tool_call_id` на каждый вызов, кладёт его И в `tool_call_started`, И в `tool_call_finished`; адаптер спаривает START/END по этому id → перекрытия одного инструмента больше не сталкиваются. Legacy-трейсы без id — name-fallback, но finish без пары теперь **отбрасывается**, а не фабрикуется (осиротевший END невозможен в принципе). **Регресс-тесты** (`tests/contract/test_agui_contract.py`: interleaved same-tool с id и без) — падают на старом коде («orphan TOOL_CALL_END»), проходят на новом; 335 non-slow зелёных, ruff+mypy чисто, pre-commit passed. **Деплой:** scp `agui.py`+`react_worker.py` на ноду (node-версии были байт-в-байт pre-fix) → rebuild `lldemo-app`+`lldemo-worker` (dep-слой из кэша) → recreate (nginx re-resolve фикс прикрыл смену IP app). **ЖИВАЯ ВЕРИФИКАЦИЯ (публичный `/agui`, тот самый запрос-сравнение):** run `c5068320` → `run_finished status=succeeded` (132с), в трейсе 4 tool-call'а все с `tool_call_id` (2×`sql_query` AAPL+MSFT, `schema_introspect`, `rag_search`), клиентский поток — 4 START/4 END идеально парные, **0 orphan, 0 RUN_ERROR**; верный ответ (AAPL $383→391→416 млрд vs MSFT $212→245→282 млрд FY23-25 + риски Apple с цитатами sec.gov), cost $0.0092. Тронула ТОЛЬКО `orchestrator/agui.py`, `workers/react_worker.py`, `tests/contract/test_agui_contract.py` (+COORDINATION+память). **⚠️ Хотспот-примечание для оркестратора/регулярной:** правила `agui.py` и `react_worker.py` — контракт TraceEvent теперь несёт `tool_call_id` в payload tool_call_started/finished (аддитивно; `persistence.py` name-keying не тронут, у него payload-latency fallback). Демо LIVE.

### 2026-07-14 ~+27ч · интерактивная сессия (с владельцем) — 🐞→✅ демо-502 ПОЧИНЕНО: стейл-IP upstream в nginx (не «app down»), + долговременный фикс re-resolve
Владелец: «почему демо 502?». Диагностика живьём (SSH на EPYC `zzlawlzz@192.168.1.115`): **прошлый диагноз (+24ч/+27ч «оркестраторный app-бэкенд down → restart app») ОКАЗАЛСЯ НЕВЕРНЫМ.** `docker ps`: ВСЕ 9 демо-контейнеров, включая `lldemo-app-1`, = **Up 4h (healthy)**. Лог web-контейнера: `/`=200, но `/api/*`+`/healthz`=502 с `connect() failed (111: Connection refused) while connecting to upstream: 172.18.0.6:8000`. `docker exec lldemo-web-1 wget http://app:8000/healthz` (свежий Docker-DNS lookup) = **работает** (`{"status":"ok"}`), а nginx на закешированный `.0.6` = refused. `docker inspect`: реальный IP `app`=**172.18.0.8**, а `172.18.0.6` теперь принадлежит **worker**-контейнеру (слушает 8081, не 8000) → refused → 502. **Корень:** `proxy_pass http://app:8000` (plain hostname) резолвится **один раз при старте nginx** и IP кешируется навсегда; при рестарте стека (~13:27, web стартовал на 5с раньше app по `StartedAt`) IP на compose-сети переехали, nginx застрял на старом `.0.6`. Docker-DNS отдаёт актуальный → прямой запрос из web работает, кешированный nginx — нет. **НЕМЕДЛЕННО:** `docker restart lldemo-web-1` (НЕ app!) → nginx пере-резолвил `app`→`.0.8` → публичный `/healthz`+`/api/examples`=**200**. **ДОЛГОВРЕМЕННО (`a480f87`, запушено + задеплоено на ноду):** `web/nginx.conf` — добавлен `resolver 127.0.0.11 valid=10s ipv6=off` (Docker embedded DNS) + `set $app_upstream app; proxy_pass http://$app_upstream:8000$request_uri` во всех 3 proxied-локациях (`/api/`, `/agui`, `/healthz`). Переменная в proxy_pass форсит пере-резолюцию каждый запрос (TTL 10с), `$request_uri` сохраняет точный путь+query. Rollout: scp конфига на ноду → `nginx -t` в throwaway-контейнере (syntax ok) → `docker compose -p lldemo -f docker-compose.yml -f deploy/demo/docker-compose.demo.yml -f deploy/demo/compose.epyc.yml build web` (pnpm-стадия cache-hit) → `up -d --no-deps web`. **Проверено живьё:** новый web-контейнер содержит переменную+resolver; после `restart app` публичный `/healthz`=`{"status":"ok","mode":"us"}` 200 БЕЗ касания web (доказывает пере-резолюцию). Тронула ТОЛЬКО `web/nginx.conf` (осознанно — по прямой просьбе владельца, это web/, обычно избегаемый регулярной сессией) + COORDINATION + память. Не трогала cloudflared/app/compose (память: cloudflared не дёргать). **Демо СНОВА LIVE**, демо-зависимые критерии разблокированы. **Урок для памяти:** «502 только на /api при 200 на /» = стейл-upstream-IP в nginx, лечить рестартом **web**, а не app; диагноз по `docker ps` статусу app недостаточен.

### 2026-07-14 ~+27ч · регулярная сессия — ✅ T-038 live link-checker + fresh-reader accuracy-fix (стейл Project-status/Known-limitations против as-built)
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **Инфра live (не память):** раннер `epyc-home`=**ONLINE+idle** (`gh api`, busy:false), сайт `ledgerlens.space`=**200**, демо `app.ledgerlens.space/healthz`=**502** (app-бэкенд по-прежнему down — тот же диагноз +24ч, за владельцем точечный `restart app`). Dev-Docker не запущен (postgres-контейнер стоит → нет локального DB/HTTP, кроме публичного). Baseline проверен: **333 non-slow зелёных** (34.7s), ruff/mypy чисто — release-gate инвариант держится на HEAD. Tree clean, коллизий нет (последний коммит — мой же +24ч). **Открытые `[ ]` все owner/железо-блокированы** (T-037 local-CPU/EPYC, T-040 §1 чистая-VM + §4 тег/Release/branch-protection), демо-зависимые критерии заблокированы 502. **Взяла непересекающийся локальный слайс — T-038 крит «все ссылки README живые (линк-чекер)» + fresh-reader accuracy-pass (README не хотспот):** **(1) Полный live link-check** обоих README (прошлый прогон проверял только локальные ссылки): 16 уникальных внутренних файлов/картинок — все существуют; 4 anchor-ссылки — резолвятся по GitHub-slug к реальным заголовкам; 4 внешних GitHub-URL/бейджа (ci.yml/eval.yml + badge.svg) — все **HTTP 200** (curl -L); bare `app.ledgerlens.space` честно обрамлён «may be offline» → 502 не делает README неточным. Крит ✅ закрыт живьём. **(2) Fresh-reader pass вскрыл РЕАЛЬНУЮ stale-неточность** (класс прошлых CHANGELOG/enrich-фиксов): «Project status» + «Known limitations» отставали от as-built — статус-строка «Gates G1 G2» при закрытом **G3** (T-031 done 2026-07-13); two-node в «remaining» хотя живой; в Known-limitations ДВА пункта устарели фактически: `faithfulness non_blocking «while T-041 closes»` (T-041 ЗАКРЫТ 2026-07-14, faithfulness промотирован в **blocking**, сверено `config/eval-thresholds.yaml non_blocking: []`) и `two-node not live yet` (G3 живой). Оба пункта удалены (более не ограничения); статус-строка → «G1 G2 G3 ✅», delivered → T-001…T-034 + T-041, remaining-список освежён (T-035/036/037/039/040); EN+RU строго симметрично. Docs-only, поведение не тронуто. **Коммиты:** `062bd47` (README EN+RU), `8582db5` (BACKLOG³¹ footnote), оба запушены. Тронула ТОЛЬКО README.md, README.ru.md, BACKLOG³¹ + COORDINATION + память. НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web. Бюджет: $0 (без LLM). **Осознанно НЕ тронула** (риск>польза в unattended): LangGraph `create_react_agent`→`create_agent` (контракт-меняющая, DoD E.2 явно откладывает до отдельного захода с full-eval); non-constant-time token-сравнения (за приватным WG, security-review не флагал). **Остаток за владельцем/EPYC:** точечный `restart app` демо-бэкенда (502); §1 чистая-VM Quick Start; §9 CPU-бенч+ADR-3 (ollama на EPYC); §10 gif/самокоррекция; §4 тег v1.0.0/Release/branch-protection. Зона свободна.

### 2026-07-14 ~+24ч · регулярная сессия — ⚠️ точечный диагноз демо-502 (app-бэкенд down, front жив) + docs-accuracy фикс enrich-докстринга
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **Инфра live (не память):** раннер `epyc-home`=**ONLINE+idle** (`gh api`, busy:false — регрессия-восстановление против +21ч offline-записи), сайт `ledgerlens.space`=**200**, CI/site все зелёные (последний eval `29309270369` success). Dev-Docker не запущен (нет локального стека → live-HTTP-верификация недоступна, кроме публичного демо). Baseline проверен: ruff+mypy чисто, **333 non-slow зелёных** (34.7s) — release-gate инвариант держится на HEAD. Tree clean, коллизий нет (последний коммит — мой же +21ч OG-fix). Открытые `[ ]`: T-037 (local-CPU, EPYC-домен), T-040 (G4-остаток — §1 чистая-VM + §9 CPU-бенч, оба железо/владелец). **⚠️ ГЛАВНАЯ НАХОДКА — точный диагноз демо-502 для владельца:** `curl` по эндпоинтам показал `app.ledgerlens.space/` (SPA-корень) = **200** (реальный `<!doctype html>` LedgerLens-SPA — web/nginx-контейнер отдаёт статику), но ВСЕ `/api/examples`, `/api/chat`, `/healthz` = **502 Bad Gateway** (`Server: cloudflare`, свежий CF-RAY, не кэш). **Вывод: EPYC-нода + cloudflared-туннель + web-контейнер ЖИВЫ; упал ТОЛЬКО оркестраторный `app`-бэкенд (FastAPI)** — nginx проксирует `/api`→app, app недоступен → 502. **Лечение точечное:** `docker compose -f … restart app` на demo-стеке (НЕ полный ребут ноды, как в прошлые падения где 502 отдавал и корень), либо app OOM/крашнулся → `docker compose logs app`. Демо-зависимые live-критерии (§2 acceptance-прогон, §5 контур-санити, T-036 §1 Grafana-визуал) заблокированы до подъёма app. **Локальный слайс (docs-accuracy, класс прошлых CHANGELOG/README-фиксов):** аудит `tools/enrich/core.py` вскрыл стейл-докстринг `_fetch_alphavantage` (строка 233): «returns the full history (`outputsize=full`)», тогда как запрос реально шлёт `outputsize=compact` (free-tier ~100 торговых дней) — что верно описывают И `params`-коммент (строка 251), И модульный докстринг (строка 21). Расхождение вводило бы майнтейнера в заблуждение о доступной глубине истории цен на free-tier. Выровнено на `compact` + указатель на `params`-заметку (`3fade24`, запушено). ruff/mypy/pre-commit чисто, поведение не тронуто (pure docstring). Тронула ТОЛЬКО `tools/enrich/core.py` (не хотспот) + COORDINATION + память. НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web. Бюджет: $0 (без LLM). **Осознанно НЕ тронула** (риск > польза в unattended-сессии): (1) A2A-bearer/monitor-token сравнения non-constant-time — но за приватным WG-мешем, security-review не флагал, менять auth-крит на спекулятивных timing-грунтах = ровно риск против протокола; (2) LangGraph `create_react_agent`→`create_agent` — раннер online → revalidation возможна, но контракт-меняющая правка + full-eval, DoD E.2 явно откладывает. **Остаток за владельцем/EPYC:** поднять `app`-контейнер демо (точечно, см. диагноз выше); §1 чистая-VM Quick Start, §9 CPU-бенч+ADR-3 (ollama на EPYC), §10 gif/самокоррекция, §4 тег v1.0.0/Release/branch-protection. Зона свободна.

### 2026-07-14 ~+21ч · регулярная сессия — ✅ Site OG/canonical fix (соц-превью стейл-домен), живо проверено над публичным URL
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **Инфра live (не память):** демо `app.ledgerlens.space/healthz`=**200**, сайт `ledgerlens.space`=**200**, раннер `epyc-home`=**OFFLINE** (`gh api` — регрессия против +18ч-записи; eval/T-037-local снова заблокированы, за владельцем ребут). Dev `platform-postgres-1` не запущен (eval_runs недоступен, но self-hosted пишет в БД EPYC — не источник тут). CI зелёная. Tree clean, коллизий нет (последний коммит — мой же +18ч DoD §2). **Открытые `[ ]`:** T-037 (local-CPU — EPYC-домен), T-040 (G4-остаток). Локальный код чист (единственный TODO — non-blocking MOEX follow-up). **Взяла непересекающийся публичный-сайт слайс (T-039/T-040 deliverable, site/ не хотспот):** аудит сайта вскрыл РЕАЛЬНЫЙ release-баг — соц-превью/canonical-метаданные (`og:url`/`og:image`/`twitter:image`) указывали на СТЕЙЛ `https://zzlawlzz.github.io/ledgerlens/`, хотя сайт с 07-13 канонически отдаётся с custom-домена `https://ledgerlens.space/` (CNAME enforced, `https_enforced=true`). Побочно сверила: числа бенчмарков сайта (flash 0.74s/$0.02/4.80, pro 2.75s/$0.23/5.00) = дословно `benchmarks/inference/REPORT.md` (ok); live-демо-ссылка сайта уже на `app.ledgerlens.space` (ok). **Последствия бага:** `og:url` рекламировал не-брендовый redirect-URL как canonical; `og:image` тянулся с github.io, который 301-редиректит на custom-домен, а соц-краулеры (Slack/Twitter/Telegram) обычно НЕ следуют редиректам для og:image → превью-картинка сломана при шаринге публичного сайта. **Сделано (`98f9d5f`, запушено):** все три тега → `https://ledgerlens.space/…` + добавлен явный `<link rel="canonical" href="https://ledgerlens.space/">`. Grep подтвердил: иных `github.io`-ссылок в source нет (остались только в COORDINATION — описание DNS, корректно). **ЖИВАЯ ВЕРИФИКАЦИЯ над публичным URL:** push тронул `site/**` → Pages-workflow `29335200230` redeploy (watch → success); `curl https://ledgerlens.space` отдаёт исправленные теги; `og:image` резолвится **HTTP 200 с 0 редиректов** на custom-домене (раньше — 301 через github.io). Тронула ТОЛЬКО `site/index.html`+BACKLOG³²+COORDINATION+память. НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web. Бюджет: $0 (без LLM-вызовов). **Остаток за владельцем/EPYC:** поднять раннер `epyc-home` (eval/T-041-мониторинг/T-037-local-бенч), §1 чистая-VM Quick Start, §9 CPU-бенч+ADR-3, §4 тег v1.0.0/Release/branch-protection; T-039-остаток (gif/видео самокоррекции) — нужен живой capture. Зона свободна.

### 2026-07-14 ~+18ч · регулярная сессия — ✅ DoD §2 закрыт: SSE keep-alive vs CF-idle-timeout ИЗМЕРЕН живьём (§2 промотирован в done)
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **Инфра ВОССТАНОВИЛАСЬ против +15ч-записи** (сверено live, не память): демо `app.ledgerlens.space/healthz`=**200** (было 530), сайт=200, раннер `epyc-home`=**online+idle** (был offline) — владелец, видимо, перезагрузил EPYC-ноду. Последние 4 eval-прогона success; eval НЕ идёт → чистое окно, демо-стек трогать безопасно. Открытые `[ ]`: T-037 (local-CPU-бенч, EPYC-домен — ollama на ноде, не драйвится отсюда), T-040 (G4-остаток). **Взяла единственный локально-исполнимый + сейчас-разблокированный живой критерий: DoD §2 остаток «SSE keep-alive на шаге >CF-idle-timeout»** — прошлая (+12ч) сессия видела keep-alive только на 15-с шаге, трекер явно держал открытым 100-с+ кейс. **Сделано (`aa8dc76`, docs-only, запушено):** 4 независимых тяжёлых multi-step запроса над публичным HTTPS-демо через реальный CF-туннель; финальный замер — curl (рабочий UA; чистый `Python-urllib` ловит CF-403-WAF) → python-таймштампер построчно = **клиентские** времена прибытия. **Результат: max интервал между любыми двумя строками = 10.4 c** на 200-с непрерывном соединении; крупные data-паузы (шаг плана `deepseek-v4-pro` — 22.6 c без событий, с 5.5 c до 28.1 c) **полностью перекрыты `: keep-alive`-комментариями** на 15.6 c и 25.6 c (каждые ~10 c). CF-idle-timeout (дефолт 100 c ≫ 10-с heartbeat) срабатывает лишь при byte-паузе больше таймаута → при max 10.4 c **не может сработать при любой длительности шага.** Во всех 4 прогонах соединение держалось 200–300 c и обрывалось КЛИЕНТСКИМ `--max-time` (`curl: (28)`), НИ РАЗУ не CF-ресетом/524. **§2 промотирован 🟡 почти done → ✅ done** (URL live + acceptance + SSE-idle измерены); DoD-итог 7→**8 из 11 done**; code-ready осталось 2 (§1 чистая-VM, §9 CPU-бенч). **Честное отклонение (в трекере):** не снят ОДИН трейс, одновременно >100 c И с `run_finished` — CPU-тир публичной демо-ноды не доводит multi-step до конца в разумном клиентском окне (все 4 обрезаны клиентом, не сервером); сквозная доставка `run_finished` уже доказана 38-с acceptance-прогоном `d73056f9`; открытый вопрос был именно про idle-устойчивость — измерен напрямую. Тронула ТОЛЬКО `docs/release/v1.0-dod.md` (+COORDINATION+память). НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web. Бюджет: 4 демо-запроса ≈ $0.03–0.05. **Остаток за владельцем/EPYC:** §1 чистая-VM Quick Start (тайминги), §9 local-CPU-бенч+ADR-3 (ollama на ноде), §10 gif/демо-ссылка, §4 тег v1.0.0/Release/branch-protection (релиз-действие). Зона свободна.

### 2026-07-14 ~+15ч · регулярная сессия — ✅ CHANGELOG accuracy-fix (стейл G3/T-041 in-progress) + верификация release-gate инварианта; ⚠️ демо/раннер снова упали
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **T-041 закрыт (прошлые сессии), G3✅/G4 открыт.** Открытые `[ ]`: T-037 (local-CPU-бенч, ждёт EPYC), T-040 (остаток G4). **Сверка инфры (live, не память):** раннер `epyc-home`=**OFFLINE**; последние 4 eval-прогона success (T-041 закрыт 2× зелёными full); **демо `app.ledgerlens.space`=530 (Cloudflare — origin unreachable → EPYC-нода снова упала/недоступна), сайт `ledgerlens.space`=200.** Т.е. регрессия против прошлой сессии (демо было 200): **EPYC-нода/туннель опять down — за владельцем (ребут ноды + поднять раннер).** `platform-postgres-1` на dev = Exited(0) 54м назад (dev-БД eval_runs = локальные, не источник по self-hosted). **Живая поверхность демо-зависимых критериев снова заблокирована.** **Взяла непересекающийся чисто-локальный docs-слайс:** нашла реальный дефект — `CHANGELOG.md` последний раз трогали `4f0ee1d` 2026-07-12, ДО закрытия G3 (07-13) и T-041 (07-14), поэтому `[Unreleased]` «In progress» **материально врёт**: T-031(гейт G3) и T-041 числились in-progress, а оба закрыты; секция `[G3]` повторяла стейл-оговорку «live two-node deploy is the remaining G3 item». Для v1.0-release-changelog (deliverable T-038/T-040) — реальная accuracy-ошибка, с которой владелец бы зарелизился. **Сделано (2 коммита, запушено):** оба перенесены в `[G3] Added` (депт-маркеры Phase 3, формулировки сверены против git/BACKLOG); «In progress» подрезан до реально открытых (T-037 local-CPU, T-040/G4); добавлен уже опубликованный сайт T-039 (`ledgerlens.space`). BACKLOG³¹ addendum. **Побочно (реальная верификация release-gate инварианта на HEAD `c5cc66e`):** `ruff format --check`+`ruff check` чисто, `mypy` чисто (130 файлов), **333 non-slow зелёных** (36.6s) — инвариант T-040 держится. Подтвердила live-факт DoD E.2: `create_react_agent` эмитит `LangGraphDeprecatedSinceV10` в точке вызова `react_worker.py:441` (видно в pytest воркера). Тронула ТОЛЬКО `CHANGELOG.md`+`BACKLOG.md`³¹+COORDINATION+память. НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web. **Остаток по-прежнему за владельцем/EPYC:** поднять ноду+раннер (демо 530→восстановить, T-037 local-CPU-бенч, эталонные eval); T-040 §4 (v1.0.0-тег/Release/branch-protection) = релиз-действие владельца, CHANGELOG теперь turnkey-готов. Зона свободна.

### 2026-07-14 ~+12ч · регулярная сессия — ✅ DoD §2 acceptance-прогон незнакомцем + §5 perimeter-sanity (live над публичным демо)
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **T-041 закрыт (прошлые сессии), G3✅/G4 открыт.** Открытые: T-037 `[ ]` (local-CPU-бенч — ждёт EPYC), T-040 `[ ]` (остаток G4). Сверка GH/инфры: раннер `epyc-home`=**online, не busy**; последний eval `29309270369` (schedule, profile=**ci** по дефолту, 9/10 pass, success); демо `app.ledgerlens.space`=**200**, `/healthz` ok. Eval не идёт → чистое окно, чтобы взять единственный локально-исполнимый живой критерий: **DoD §2 «формальный acceptance-прогон незнакомцем через публичный URL»** + **DoD блок-D §5 «санитарная проверка публичного контура»** (оба явно в остатке трекера). **Сделано (`a65ad90`, docs-only, запушено):** (§2) анонимный `POST /api/chat` `{"question":"What was Apple's revenue in fiscal year 2025?","mode":"us"}` → полный SSE-цикл: route `deepseek-v4-flash` → plan simple → `sql_query` (AAPL revenue = 416 161 000 000 USD @ period_end 2025-09-27) → synthesize `deepseek-v4-pro` → guardrail `pass` → `run_finished succeeded`; ответ «$416,161,000,000 (USD)» + дисклеймер «This is financial analytics, not investment advice.», `cost_usd=0.001517`, latency 38.2s, run_id `d73056f9-2f65-4cce-957c-f03bc3bce1a0`. **Побочно доказано:** §4 SSE-heartbeat `: keep-alive` пришёл на проводе через РЕАЛЬНЫЙ CF-туннель дважды (в т.ч. на 15-сек synthesize-шаге) — механизм работает end-to-end; dealias-конфиг (блок E) — оба cloud-тира `deepseek-v4-flash`/`deepseek-v4-pro` ответили вживую. (§5) perimeter-sanity: (1) `/api/examples`→`demo:true`,`mode:us`,5 примеров; (2) question 600 симв.→**HTTP 413** (T-036 §1 cap ≤500 живьё); (3) security-заголовки на корне — CSP (frame-ancestors 'none', object-src 'none')/`X-Frame-Options: DENY`/`X-Content-Type-Options: nosniff`/`Referrer-Policy` все present (T-036 §3); (4) FastAPI-доки НЕ экспонированы — `/docs`+`/openapi.json` отдают SPA-fallback (`text/html`, `<div id="root">`), а не Swagger → docs-gating держится живьё; (5) `/healthz`→`{"status":"ok","mode":"us"}` 200. Тронула ТОЛЬКО `docs/release/v1.0-dod.md` (§2/§5/summary/блок-A/D) + `BACKLOG.md`²⁹ (аддендум). НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web. Бюджет: 1 демо-вопрос ≈ $0.0015. **Остаток §2 = только SSE на шаге >CF-idle-timeout; T-036 остаётся `[~]` (§1 Grafana-визуал не публичен); T-040 `[ ]`.** Зона свободна.

### 2026-07-14 ~+9ч · регулярная сессия — ✅ T-040 release-hygiene: Makefile (стейл `make eval` + .PHONY) + сверка онбординг-пути §1
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ — `eval_runs` до id 12 (локальные dev-прогоны; self-hosted пишет в БД EPYC — источник истины по ним = логи GH), tree clean, HEAD `af6da18`. **T-041 закрыт прошлой сессией (2× зелёных full-eval на EPYC), G3/G4-статусы актуальны.** Открытые задачи: T-037 `[ ]` (local-CPU-бенч — ждёт EPYC), T-040 `[ ]` (остаток G4 = железо/owner). **Локальный слайс без блокеров:** DoD §1/§5 «когерентность онбординга на чистой машине» — release-hygiene-аудит Makefile+Quick Start. **Найдено и починено (`815b701`+`701190b`, запушено):** (1) цель `make eval` = стейл-заглушка `@echo "TODO(T-029): eval harness"` при давно готовом+CI-зелёном (eval.yml) харнессе `eval.run`; заголовок Makefile обещает «TODO-stubs are implemented by their backlog tasks» → это был последний невыполненный стаб (grep подтвердил). Прошита на реальную CLI `uv run python -m eval.run --profile $(EVAL_PROFILE) --base-url $(EVAL_BASE_URL) --concurrency $(EVAL_CONCURRENCY)` (дефолты ci/localhost:8000/1 = как в eval.yml). (2) `up-no-local`+`reindex` = реальные цели вне `.PHONY` → добавлены. **Верификация:** `eval.run --help` = дословная сигнатура `--profile {ci,full} --base-url --concurrency`; рецепт tab-индентирован (`cat -A` → `^I`); эта CLI зелёная в CI (`29288707116` + self-hosted full-eval); comm-проверка — все Makefile-цели теперь phony-declared; 333 non-slow зелёные (baseline не тронут). **Сверка онбординг-пути §1 по РЕАЛЬНОСТИ (расхождений НЕ найдено):** env-переменные Quick Start (DEEPSEEK_API_KEY/POSTGRES_PASSWORD/POSTGRES_RO_PASSWORD/A2A_TOKEN) все в `.env.example`; scripts (smoke_test/eval_snapshot/fetch_demo_snapshot/eval_baseline_compare) + флаг `--auto-ingest` + модуль `ingestion.reindex` — все существуют. **`make` в dev Git-Bash отсутствует** (нельзя гонять цели локально; проверка велась статически + через прямой вызов харнесса). Тронула ТОЛЬКО `Makefile` (+BACKLOG³⁴ + COORDINATION + память). НЕ трогала prompts/config/compose/api.py/graph/worker/eval.yml/web. **T-040 остаётся `[ ]`** (остаток G4 — железо/owner: §1 чистая-VM с таймингами, §2 acceptance-прогон, §4 v1.0.0-тег/Release+branch-protection, §9 CPU-бенч EPYC, §10 gif/демо-ссылка). Блокирующих owner-вопросов по eval-оси нет. Зона свободна.

### 2026-07-14 ~+6ч · регулярная сессия — ✅✅ T-041 ЗАКРЫТ (два подряд зелёных full-eval на EPYC), Q-16+Q-22 закрыты
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ. **БД `eval_runs` = локальные dev-прогоны (max id 12)** — self-hosted eval пишет в БД EPYC-ноды, не в dev; поэтому источник истины по self-hosted прогонам = логи GH Actions, не dev-БД. **Сверка GH вскрыла ГЛАВНОЕ:** прошлый §3-разблокировочный прогон `29297896263` (HEAD `7b894cd`, после MCP-фикса `198bdf9`) **завершился ЗЕЛЁНЫМ (success, 1h09m)** — первый ПОЛНОСТЬЮ чистый full-eval на реальном железе: `citation_coverage=1.0`, `faithfulness=0.985`, guardrail 1.0, nodata 1.0, numeric 0.923, 39/41 pass, «eval OK: no blocking thresholds violated» (exit 0). Раннер `epyc-home`=ONLINE (`gh api`). **Это доказало гипотезу Q-22: citation-«потолок» и faithfulness-«шум» = артефакты голодания БЕСПЛАТНОГО 2-vCPU GH-раннера, не кода.** Триггернула подтверждающий 2-й full-eval; **1-я попытка `29302691531` по умолчанию ушла профилем `ci`** (default `eval.yml` = ci, 10 кейсов — зелёный citation 1.0/faith 1.0, но ci слабый faithfulness-сигнал) → передиспетчила корректно `-f profile=full` → **`29303631947` ЗЕЛЁНЫЙ (faithfulness 1.0 / citation 1.0 / 38-of-41, 41 кейс, gate exit 0)**. **ДВА подряд чистых full-прогона ≥0.7 → критерий приёмки T-041 ВЫПОЛНЕН.** **Закрыла T-041:** `config/eval-thresholds.yaml` → `non_blocking: []` (faithfulness промотирован в blocking, порог 0.7 НЕ занижался, комментарий-обоснование переписан); гейт-логика проверена инлайн (`_blocking_violations` теперь ловит faithfulness → exit≠0); BACKLOG T-041 `[wip]`→`[done 2026-07-14]`+сноска²⁰; Q-16 закрыт; Q-22 закрыт (вариант-1 доказан); DoD-трекер header/§3/§5/block-C синкнут; память. Тесты 333 non-slow зелёные. **Побочно (memory-hygiene, live-факт):** корректировка DoD E.2 — `create_react_agent` ВСЁ-ТАКИ эмитит `LangGraphDeprecatedSinceV10`, но в точке ВЫЗОВА (`react_worker.py:441`, видно в каждом pytest воркера), не при импорте; прошлая заметка `e5d4aec` проверяла только импорт → «рантайм-предупреждения нет» была неполной. Слома нет (пин 1.2.8, removal V2.0), миграция отложена, но раннер online → revalidation-предпосылка восстановлена. Коммиты: coordination-claim, DoD-sync, E.2-correction, T-041-closure (`config/eval-thresholds.yaml`+BACKLOG+OWNER_QUESTIONS), DoD/Q-22-propagate — все запушены. **Тронула:** `config/eval-thresholds.yaml` (НЕ хотспот), BACKLOG, OWNER_QUESTIONS, docs/release/v1.0-dod.md, COORDINATION, память. НЕ трогала prompts/api.py/graph/worker/compose/eval.yml/web. Зона свободна. **Остаток G4 (T-040 `[ ]`):** §1 чистая-VM, §2 acceptance-прогон демо, §4 v1.0.0-тег/Release+branch-protection, §9 CPU-бенч (EPYC-нода), §10 gif/демо-ссылка. Q-05/Q-16/Q-22 закрыты → блокирующих owner-вопросов по этой оси нет.

### 2026-07-14 ~+3ч · регулярная сессия — ✅ РАЗБЛОКИРОВКА §3 эталонного full-eval (MCP host-port коллизия на EPYC-раннере), верифицировано живьём
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ — `eval_runs` до id 12 (последний full `a1761e4`: faith 0.98/citation 1.0/guardrail 1.0 — здоров), tree clean, последний коммит `84ff3d1` (оркестратор, network-bound demo-egress). **Раннер `epyc-home` = ONLINE** (`gh api`, busy:false). **Ключевая находка при сверке GH:** последний full-eval `29292555409` (dispatch оркестратора на HEAD с router-фиксом = §3 «эталонный full-eval») **упал через 4м42с не по метрикам**, а на `docker compose up`: `Bind for 10.9.0.1:8765 failed: port is already allocated` (в `eval_runs` новой строки нет, max id=12). **Диагноз:** base `docker-compose.yml` публикует MCP-серверы на host-портах 8765/8766/8767 («for dev tests / MCP inspector»), но на self-hosted EPYC-раннере eval-стек со-локализован с always-on demo/распределённым A2A-стеком, чей `deploy/worker-node/compose.mcp-awg.yml` биндит те же MCP на AmneziaWG-адрес `10.9.0.1:8765/…` → коллизия, `up` падает ДО оценки хоть одного кейса. Это ровно класс §3-блокера (журнал ~+1ч назад видел вариант с Postgres-5432; этот — MCP-8765). eval-app ходит в MCP по compose-сети по имени сервиса (`MCP_SQL_URL=http://mcp-sql:8765/mcp`) → host-публикации eval-харнессу не нужны. **Сделано (`198bdf9`+`4c1ee7d`, запушено):** новый оверлей `docker-compose.ci.yml` снимает host-публикации MCP (`ports: !override []` для mcp-sql/rag/enrich — `!override` заменяет, а не аппендит base-список; Compose ≥2.24, раннер на v5.x); `eval.yml` прокидывает его во все три compose-команды (`up postgres qdrant`, `up app worker mcp-sql mcp-rag`, `down -v`). Base compose НЕ тронут (dev сохраняет inspector-порты). **Проверено локально:** `docker compose -f docker-compose.yml -f docker-compose.ci.yml config` резолвится чисто; стартуемые eval-сервисы публикуют только 5432/6333/8000/8081 (все свободны на demo-ноде — демо remapped 15432/16333/18000, app не держит 8081), mcp-sql/rag — ничего. **ЖИВАЯ ВЕРИФИКАЦИЯ:** триггернула full-eval `29297896263` на HEAD с фиксом (push ПЕРЕД dispatch — урок соблюдён) → **ОБА `up`-шага прошли SUCCESS** (подтверждено `gh run view` — оба в списке успешных steps), впервые eval прошёл коллизионную точку на self-hosted раннере и дошёл до шага «Run eval» (скорит кейсы). **§3-блокер `up` устранён кодом-в-репо** (durable, версионируемо — не ручная правка ноды). Тронула ТОЛЬКО `docker-compose.ci.yml`+`eval.yml`+BACKLOG³⁴; НЕ трогала base compose/prompts/config/api.py/graph/deploy/eval-код. **Остаток:** итоговые метрики прогона `29297896263` (citation/faithfulness = вопрос T-041/Q-22, ОТДЕЛЬНЫЙ от инфра-фикса) — читать оркестратору/след. сессии по завершении (~20-40м на full). T-040 остаётся `[ ]` (§3-eval теперь может дать эталонный прогон; прочие G4-пункты §1/§4/§5/§9 + Q-05/Q-22 открыты). Зона свободна.

### 2026-07-14 ~+1ч · регулярная сессия — перепроверка «диск/раннер/§2/§3/Q-05» по запросу владельца + ФИКС красной CI (40+ прогонов)
Владелец: «освободить диск EPYC + поднять раннер epyc-home → §2/§3, Q-05 — должно быть уже сделано, перепроверься». **Перепроверила по РЕАЛЬНОСТИ (не памяти):**
- ✅ **Диск EPYC освобождён / §2 демо LIVE:** `https://app.ledgerlens.space/` → HTTP **200**, `/healthz` → `{"status":"ok","mode":"us"}`. Демо реально играбельно (память говорила 530 — устарела). §2 в трекере поднят 🟡→«почти done».
- ✅ **Q-05 решён** (2026-07-10 в OWNER_QUESTIONS: CF-туннель+домен), сейчас РЕАЛИЗОВАН — и сайт (`ledgerlens.space`), и демо (`app.ledgerlens.space`) живут на HTTPS.
- ❌ **Раннер `epyc-home` = OFFLINE** (gh api, total 1, offline). Был кратко online ~19:06 UTC 2026-07-13 (подхватил eval `29277164289`).
- ❌ **§3 эталонный full-eval НЕ сделан:** тот eval-прогон **упал через 43 мин** — НЕ по метрикам, а `Connect call failed ('127.0.0.1', 5432)` (Postgres недоступен из eval-джоба на self-hosted ноде) → каскад мгновенных фейлов кейсов → нет отчёта → `could not persist eval run to DB`. В `eval_runs` новой строки нет (max id = 12). Eval-инфра на ноде не поднята для job-контейнера.
- 🆕 **НАЙДЕНО И ПОЧИНЕНО: CI (`ci.yml`) была КРАСНОЙ 40+ прогонов подряд (>суток, с 2026-07-12T17:16).** `build`-джоб падал на `docker compose config --quiet`: `.env not found` (env_file на 5 сервисах; свежий docker compose трактует пропажу env_file как ошибку, раньше warning). lint+unit были зелёные — только build. **Фикс `042391e`:** `cp .env.example .env` в build-джобе (как в smoke). Проверено: `docker compose config --quiet` exit 0 с .env; **CI снова ЗЕЛЁНАЯ** (прогон `29288707116`: build/lint/unit ✅). Это снимает предпосылку для G4 §4 «main-branch protection = CI required» (нельзя требовать красную CI). Трекер §4 обновлён.
**Итог перепроверки:** владелец прав про диск/§2/Q-05 (сделано), но раннер offline и §3/citation-гейт (Q-22) ещё заблокированы — нужна (пере)подъём `epyc-home` + доступность Postgres/сервисов из eval-джоба. Трекер синкнут (`cd955aa`). Тронула ТОЛЬКО `.github/workflows/ci.yml` (`042391e`) + `docs/release/v1.0-dod.md` + COORDINATION. НЕ трогала prompts/config/compose/api.py/graph/eval.yml/web.

### 2026-07-14 ~старт · регулярная сессия — T-040 sync трекера DoD к реальности (G3 закрыт, сайт live) + secret-скан публичного репо
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ — `eval_runs` до id 12 (последний full `a1761e4`: faith 0.98, citation 1.0, guardrail 1.0 — здоров), tree clean (только untracked `awgbins/`), последний коммит `5a40cd9` (оркестратор, 2026-07-13 23:39). **Никакой активной коллизии** (ничего не менялось <30 мин). **Ключевое расхождение памяти vs реальность:** с прошлой сессии оркестратор (1) закрыл гейт **G3 живьём** (T-031 `[done]` `422d3bb`/`d783e0d` — распределённый шаг на `vps-fi`, failover, security) и (2) вывел **сайт в паблик** (T-039 (a) `[done]` `5a40cd9`+CNAME `b6bb1c5` → https://ledgerlens.space), репо стал публичным. **DoD-трекер `docs/release/v1.0-dod.md` устарел** на этих двух пунктах. **Сделано (`ec9f760`, запушено):** синк трекера — §6 (распределённый A2A) 🟡 code-ready → ✅ **done** (все критерии G3 живьё-доказаны, остаток снят); §10 (README+сайт) «публичный Pages-URL» ⛔ → ✅ (**живьё проверено 2026-07-14:** Pages API — `cname: ledgerlens.space`, `https_certificate: approved`, `https_enforced: true`, deploy-workflow `29282878530` зелёный; WebFetch — сайт отдаёт полный контент: хиро/How it works/Architecture/Depth/Benchmarks/Run it yourself); сводная таблица 6→**7 из 11 ✅ done**; блокер-группа B (G3-деплой + включение Pages) → закрыта, **G4 формально открыт** (порядок гейтов соблюдён). Плюс `.gitignore` +`awgbins/` (178МБ node-local AmneziaWG-бинарники `amneziawg-go`/`awg`/`awg-quick` + relayed `worker.tgz` от T-031 — защита от случайного коммита в публичный репо). BACKLOG footnote³⁴ на T-040. **Secret-скан (репо публичный — превентивно):** чисто — `.env` никогда не был в истории (`git log --all --diff-filter=A`), нет `sk-`/`AIza`/PEM-ключей/`id_ed25519` в tracked-файлах, единственные совпадения «password/token» = ephemeral CI-throwaway в `.github/workflows/*.yml` (`ci-password`, `ci-a2a-token` — одноразовые для disposable-контейнеров, безопасны). Действий не потребовалось. Docs-only + `.gitignore`, не трогала хотспоты. **T-040 остаётся `[ ]`** (оставшиеся G4: §1 чистая-VM Quick Start, §2 устойчивый демо-URL [EPYC-диск+Q-05], §3 эталонный full-eval, §4 тег v1.0.0+Release+защита ветки, §5 санитарная проверка контура, §9 CPU-бенчмарк [EPYC]; Q-05/Q-22 открыты). **Владельцу:** освободить диск EPYC (`/opt/trading-bot` 69ГБ) + поднять раннер `epyc-home` → разблокирует §2/§3/§9 + citation-гейт/T-041; решить Q-05/Q-22 → критерий G4 «ноль блокирующих вопросов».

### 2026-07-13 ~20:00 · оркестратор — 🎉 T-031 ГЕЙТ G3 ЗАКРЫТ ЖИВЬЁМ (распределённость доказана)
Владелец дал go на VPS. Развернул вторую ноду end-to-end, все критерии G3
проверены живьём (`422d3bb`):
1. **Живой шаг на VPS:** «Compare Apple vs NVIDIA FY2025 revenue» → step_2 отработал
   на `worker_node=vps-fi`, воркер на VPS дотянулся до MCP EPYC через туннель, вернул
   верный NVIDIA $130.497B.
2. **Failover:** стоп VPS-воркера → тот же вопрос успешно локально + trace
   `worker_failover`/`degradation=worker_unreachable` (мой диспетчер `d9ec7a9`).
3. **Security:** публичный `104.238.24.196:8081` → 000 (воркер слушает только AWG-IP);
   A2A-эндпоинт → 401 без токена (карта публична 200).
**Как:** AmneziaWG **userspace** (kernel-DKMS не собирается на обоих ядрах) —
`awg-quick`+`amneziawg-go`, mesh `10.9.0.0/24` (EPYC=.1, VPS=.2), UDP 51821 (≠ чужой
amnezia 45332), обфускация прошла DPI. Воркер на VPS = `docker run` образа
`lldemo-worker` (перенёс с EPYC через реле), entrypoint-миграции обойдён
(`--entrypoint uvicorn`). MCP EPYC открыт на туннель-IP (`compose.mcp-awg.yml`),
worker-vps зарегистрирован смонтированным `workers.vps.yaml`
(`compose.orchestrator-vps.yml`, дефолтный `config/workers.yaml` НЕ тронут → dev/CI
одно-нодовые). systemd `awg-ll.service` enabled на обеих нодах (durable). Runbook
as-built в `deploy/worker-node/README.md`. **G3 ✅ — критический путь к релизу открыт
(остаётся G4/T-040).**

### 2026-07-13 ~19:25 · регулярная сессия — ✅ T-040 §6: выделенная секция Known Limitations в README (EN+RU)
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ — `eval_runs` до id 12 (локальные full здоровы: faith 0.93–0.99, citation 0.92–1.0), tree clean, последний коммит `d3237e9` (оркестратор). **Раннер `epyc-home` = ONLINE** (EPYC ожил ~18:45, оркестратор освободил ~5.6 ГБ). **Оркестратор АКТИВНО гонит T-041-eval на EPYC:** self-hosted `29275558934` (offline-режим, `6bb826e`) **отменён монитором диска в 19:03** («The operation was canceled» — НЕ метрики, а disk-guard <3G; durable-блокер `/opt/trading-bot` 69 ГБ = владелец), затем оркестратор **передиспетчил `29277164289` (in_progress с 19:06)**. Эту зону (T-041/eval.yml/EPYC) НЕ трогала. Диск C: dev = 100% (2.3G) → ollama/bench-локально невозможны (T-037 local = EPYC-домен). **Взяла непересекающийся чисто-локальный docs-слайс T-040 §6** (в трекере v1.0-dod.md block D значился «не-прогнанным локальным протоколом»): существующий Known-Limitations-список был 3-пунктовым и **зарыт под «Project status»**, без обязательного по §6 пункта «судьба RU-адаптера (Q-02)». **Сделано (`ed0af9d`, запушено):** промотировала в отдельную секцию `## Known limitations` (EN канон) + `## Известные ограничения` (RU зеркало, симметрично) с 7 честными пунктами — добавлены недостающие: частичное RU-покрытие (Q-02: MOEX ISS живой = pluggable-доказательство SBER/GAZP/LKOH, e-disclosure/ГИР БО = каркасы осознанно вне v1.0), глубина истории цен (T-033 Alpha Vantage free-tier ~100 дней compact), исключение GPU-бенчмарка (Q-07), скоуп публичного демо (US/EDGAR + per-IP rate/concurrency/cost-лимиты). Внутренние якоря EN (`#known-limitations`, `#data-sources--licensing`) + RU (`#известные-ограничения`, `#источники-данных-и-лицензии`) проверены против заголовков; `benchmarks/inference/REPORT.md` резолвится. Обновила v1.0-dod.md (block D §6 → ✅ done) + BACKLOG T-040 footnote-аддендум. Docs-only, аддитивно, не-хотспот; НЕ трогала prompts/config/compose/api.py/graph/eval/web. **Отклонение (честно):** это docs-deliverable §6 — не код-фикс; §1·§3·§4·§5 T-040 + внешний ревью README требуют живых проверок/железа (не тронуты). T-040 остаётся `[ ]` (G3 не закрыт). Зона свободна. **Владельцу для реального прогресса:** освободить диск EPYC (`/opt/trading-bot` 69 ГБ → eval помещается рядом с демо, закроет citation-гейт/T-041), решить Q-05/Q-22.

### 2026-07-13 ~18:45 · оркестратор — EPYC ОЖИЛ (первопричина найдена) + self-hosted eval офлайн запущен
Владелец ребутнул EPYC → нода поднялась (~2 мин), демо-контейнеры авто-встали
(`restart: unless-stopped`), **демо `app.ledgerlens.space` стабильно 200** (флейк
530/502 был переходным осаживанием туннеля после ребута). **Первопричина
зависания EPYC = переполнение корня:** `/opt/trading-bot` = **69 ГБ** (данные
торгового бота владельца) → оставалось ~7.5 ГБ, мой демо-стек (~8 ГБ) добил до
~100% → sshd/Docker зависли. Почистил ~5.6 ГБ Docker-мусора → **18 ГБ своб.**
**Раннер `epyc-home` back online** (systemd авто-старт). **Твой блокер (раннер
offline) снят.** Запустил self-hosted full-eval `29275558934` с фиксом
**офлайн-моделей** (`6bb826e`: HF-CDN режется RU-сетью EPYC → eval.yml симлинкует
уже перенесённые модели демо + `HF_HUB_OFFLINE=1`, вместо HF-download). Слежу за
диском (монитор отменит при <3G). По зелёному citation=1.0 → закрою T-041.
**Диск EPYC durable-блокер: `/opt/trading-bot` 69 ГБ — за владельцем.**

### 2026-07-13 ~день (позже 8) · регулярная сессия — ✅ верификация + точное скоупирование LangGraph-миграции как НЕ-срочного tech-debt
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ — `eval_runs` до id 12 (локальные full здоровы: faith 0.93–0.99, citation 0.92–1.0, id12=all-green), tree clean, последний коммит `2233835` → оркестратор не активен. Раннер `epyc-home` = по-прежнему **offline** (`gh api` подтвердил `status:offline, busy:false`); **запланированный eval `29229428059` завис в queued ~10.5ч** (некому подхватить — накапливается). Диск C: = **100%** (2.3G своб.), E: 57%. **Весь верх плана целиком блокирован** (T-041 gate=Q-22+offline-раннер, T-031 live VPS=сеть, T-037 local=EPYC, T-036 §4 live=EPYC-туннель, T-038/T-039 остатки=owner/EPYC, T-040=G3). Прошлые 7 сессий сняли все локальные слайсы; сессия 7 честно отказалась манифактурить 4-й marginal api.py-фикс. **Вместо повтора паттерна — довела до факта единственный повторно-флагаемый tech-debt (`create_react_agent`→`create_agent`):** живьё проверила на запиненных версиях (`langgraph 1.2.8` / `langgraph-prebuilt 1.1.0` / `langchain-core 1.4.9`) — `create_react_agent` **полностью присутствует, DeprecationWarning при импорте НЕ эмитит** (`warnings.catch_warnings` — ноль), преемник `create_agent` живёт в meta-пакете `langchain`, который **в зависимостях отсутствует** (`langchain NOT INSTALLED`). **Вывод: рамка «eval-критичный deprecation» из заметок ЗАВЫШАЕТ срочность — имманентного слома нет, дедлайна нет.** Миграция форвард-луфинг и НЕ байт-в-байт (`create_agent` меняет контракт стрима `stream_mode="updates"`, на котором держится T-041-страж `react_worker.py:456-471`); требует (1) добавить `langchain`-dep в eval-критичный путь + (2) full-eval-ревалидацию T-041-инварианта — **невозможно вслепую без EPYC-раннера**, делать и пушить в main без eval = ровно риск, от которого предостерегает протокол. **Сделано (`e5d4aec`, запушено):** секция E.2 в `docs/release/v1.0-dod.md` с точным скоупом (версии/почему-не-срочно/что-влечёт/остаётся-владельцу). Docs-only, аддитивно, не-хотспот. НЕ трогала код/prompts/config/compose/eval/graph. **Отклонение (честно):** это верификация+скоупирование, не код-фикс — намеренно, ибо (а) миграция небезопасна без раннера, (б) манифактурить очередной marginal api.py-фикс = против духа «никаких хаков ради критериев» (сессии 4–7 подтвердили deep-mined). **Владельцу/оркестратору для реального прогресса нужны внешние действия:** поднять `epyc-home` раннер (+ отменить/подхватить зависший `29229428059`) + освободить диск EPYC (T-041/T-037/§4), решить Q-22 и Q-05. Зона свободна.

### 2026-07-13 ~день (позже 7) · регулярная сессия — ✅ живой де-риск DeepSeek 2026-07-24 alias-removal + запись в DoD-трекер
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ — `eval_runs` до id 12 (локальные full здоровы: faith 0.93–0.99, citation 0.92–1.0), tree clean, последний коммит `53e7213` → оркестратор не активен. Раннер `epyc-home` = по-прежнему **offline** (`gh api` подтвердил). Диск C: = 100% (2.4G своб.). **Верх плана целиком блокирован** (T-041 gate=Q-22+offline-раннер, T-031 live VPS=сеть, T-037 local=EPYC, T-036 §4 live=EPYC-туннель, T-038/T-039 остатки=owner/EPYC, T-040=G3). **Честный вывод сессии: локальная поверхность deep-mined** — прошла ревью demo-limits.py / SSE-heartbeat+admission (`/agui`+`/api/chat`, слот-гварды) / persistence — всё чисто и хорошо защищено прошлыми сессиями; baseline 333 non-slow зелёных; из 7 warnings единственные значимые (`create_react_agent`→`create_agent` deprecation в react_worker.py, и starlette-testclient httpx) — **eval-критичны/внешние → трогать вслепую без EPYC-раннера рискованно, оставила как задокументированный tech-debt для владельца.** **Нашла реальную time-sensitive находку в `docs/research/adr-notes.md:52`:** алиасы DeepSeek `deepseek-chat`/`deepseek-reasoner` удаляются **2026-07-24 (11 дней от сегодня)** — hard external deadline, ломающий деплой если конфиг на старых ID. **Проверила:** конфиг уже запинен на `deepseek-v4-flash`/`deepseek-v4-pro` (`config/router.yaml` + комментарий-предупреждение), repo-wide grep — ноль живых старых ссылок (тесты/дефолты/деплой чисты). **Живая проверка (scratchpad-скрипт, ≪$0.01):** оба cloud-тира прогнаны через реальную обвязку `RouterClient` против api.deepseek.com — flash (thinking=disabled)→`OK` (12 ток.), pro (thinking=enabled)→`OK` (41 ток., 28 reasoning корректно эмитятся) → **смена алиасов 2026-07-24 роутинг не сломает.** **Сделано (`bd6b350`, запушено):** добавила секцию «E. Внешние дедлайны/зависимости» в `docs/release/v1.0-dod.md` с этой находкой (✅ де-рискнуто, на радар владельцу) + освежила stale тест-строку (329→333, HEAD). Docs-only, аддитивно, не-хотспот. НЕ трогала код/prompts/config/compose/eval/graph. **Отклонение (честно):** это подтверждающая проверка (T-037-бенч `9549315` уже гонял эти ID живьём) + запись дедлайна на радар — не новый код-фикс; манифактурить 4-й marginal api.py-фикс не стала (well deep-mined, против духа «никаких хаков ради критериев»). **Владельцу/оркестратору для реального прогресса нужны внешние действия:** поднять `epyc-home` раннер + освободить диск EPYC (T-041/T-037/§4), решить Q-22 и Q-05. Зона свободна.

### 2026-07-13 ~день (позже 6) · регулярная сессия — ✅ T-036 §1 defect-fix #3: утечка concurrency-слота на `/agui` (availability)
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ — `eval_runs` до id 12 (локальные full здоровы: faith 0.93–0.99, citation 0.92–1.0), tree clean, последний коммит `2038f8a` 2ч назад → оркестратор не активен. Раннер `epyc-home` = по-прежнему **offline** (`gh api` подтвердил `status:offline`). Диск C: = 100% (2.4G своб.) → bench-vector-ревалидация (T-037 §2 крит) небезопасна (риск заполнения). **Весь верх плана целиком блокирован** (T-041 gate=owner-Q-22 + offline-раннер, T-031 live VPS=сеть, T-037 local=EPYC, T-036 §4 live=EPYC-туннель, T-038/T-039 остатки=owner/EPYC, T-040=G3). Прошлые сессии сняли все очевидные локальные слайсы. **Нашла новый реальный, чисто-локальный, тестируемый public-demo availability-дефект** ревью admission-пути (продолжение §1): `/agui` брал concurrency-слот в `_admit` и создавал ран (`create_run`) ДО построения `ChatRequest(session_id=body.thread_id)`. Проверила границы: `RunAgentInput.thread_id` = `str` без ограничения (`model_fields` metadata `[]`), а `ChatRequest.session_id` = `max_length=128` (подтвердила `ValidationError` на 200-симв. значении через `uv run python`). Построение `ChatRequest` стояло ВНЕ slot-release-guard (старый `except` оборачивал только `create_run`), а `/api/chat` защищён FastAPI-валидацией модели ДО `_admit` (там дыры нет). Следствие для `/agui`: клиент с `thread_id`>128 симв. → `ValidationError` в середине setup → слот НЕ освобождался (течёт навсегда) + ран осиротевал в `running` без finalize. На публичном демо (`max_concurrent_runs=2`) ДВА таких неаутентифицированных запроса намертво вешают демо на «busy» 429 до рестарта — ровно та availability-защита, ради которой §1 написан; триггер полностью клиент-контролируемый, без авторизации. **Сделано (`9bb0bd1`, запушено):** (1) в `/agui` валидировать весь запрос (`extract_question`+`ChatRequest`, `ValueError|ValidationError`→чистый 422) ДО `_admit`/`create_run` — overlong `thread_id` больше не жжёт слот и не плодит осиротевший ран; (2) расширила slot-release-guard на ОБОИХ `/agui`+`/api/chat`, чтобы любой сбой между `_admit` и `create_task` (включая `_subscribe_run_events`) освобождал слот. Тронула ТОЛЬКО `orchestrator/api.py` (+импорт `ValidationError`). 2 регресс-теста (`tests/unit/test_agui_slot_leak.py`) гоняют РЕАЛЬНЫЙ endpoint через `TestClient`: overlong `thread_id`→422 без утечки слота и без создания рана; сбой setup после `_admit` (subscribe кидает)→слот освобождён. **git-stash-верификация: оба падают на старом коде** (`_active==1` = слот утёк). 333 non-slow зелёных, ruff+mypy чисто. НЕ трогала `demo_limits.py`/пороги/prompts/graph/eval/compose/nginx. **Отклонение (честно):** живой end-to-end через реальный CF-туннель не прогнан (демо на EPYC = §4-блокер); фикс = чистая validation/guard-логика, юнит-доказана на реальном endpoint. T-036 остаётся `[ ]` (§4 живой туннель, Q-05). Зона свободна.

### 2026-07-13 ~день (позже 5) · регулярная сессия — ✅ T-036 §1 defect-fix #2: спуфабельный источник IP в rate-limit (security)
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ — `eval_runs` до id 12 (локальные full здоровы: faith 0.93–0.99, citation 0.92–1.0), tree clean, последний коммит `29c87b7` 2ч назад → оркестратор не активен. Раннер `epyc-home` = по-прежнему **offline** (подтвердила через `gh api`). **Верх плана целиком блокирован** (T-041 gate=owner+offline-раннер, T-031 live VPS=сеть, T-037 local=EPYC, T-036 §4 live=EPYC-туннель, T-038/T-039 остатки=owner/EPYC, T-040=G3). Прошлые сессии сняли все очевидные локальные слайсы. **Нашла новый реальный, чисто-локальный, release-critical security-дефект** ревью public-demo admission-пути (продолжение темы §1): `orchestrator/api.py:_client_ip` доверял ЛЕВОМУ значению `X-Forwarded-For` для per-IP rate-limit. Проверила топологию демо по коду: `web/nginx.conf` НЕ содержит `$proxy_add_x_forwarded_for` (grep: ни одного `proxy_set_header X-Forwarded-For`), значит клиент-присланный XFF проходит `Cloudflare → cloudflared → nginx → app` НЕТРОНУТЫМ → ЛЮБАЯ позиция XFF контролируется вызывающим. Следствие: посетитель шлёт `X-Forwarded-For: <произвольный>` и ротирует его → свежий rate-bucket (`hash_ip`) на КАЖДЫЙ запрос → полный обход `runs_per_hour_per_ip` — т.е. именно той защиты от исчерпания LLM-бюджета владельца, ради которой §1-лимитер и написан (его docstring: «A public stranger must not be able to exhaust the owner's LLM budget»). **Сделано (`2dd88d2`, запушено):** `_client_ip` доверяет ТОЛЬКО `CF-Connecting-IP` — Cloudflare переписывает его аутентично поверх любого клиентского значения, а единственный ingress демо = исходящий cloudflared-туннель (origin недостижим напрямую) → неспуфабельно; вне Cloudflare (dev) — прямой TCP-peer; XFF полностью выведен из security-контроля. Тест `test_client_ip_prefers_forwarded_for` (кодировал баг — ожидал левый XFF) заменён на 2: `test_client_ip_trusts_cf_connecting_ip` + `test_client_ip_ignores_spoofable_forwarded_for` (форжёный XFF игнорируется в пользу CF-заголовка; вне CF — прямой peer, спуфабельный XFF не трогается). Admission-тесты используют `host=` (прямой peer) → не задеты. 331 non-slow зелёных, ruff+mypy чисто. НЕ трогала `demo_limits.py` (публичный API)/пороги/prompts/graph/eval/compose/nginx. **Отклонение (честно):** живой end-to-end через реальный CF-туннель не прогнан (демо на EPYC = §4-блокер, туннель/диск за владельцем); фикс = чистая header-логика, юнит-доказана; поведение Cloudflare (CF-Connecting-IP аутентичен, XFF только append) — документированный контракт CF. Опциональный defense-in-depth (nginx явно затирать входящий XFF) не делала — app-фикс достаточен как граница доверия, а nginx.conf — общий §3-файл (не плодить diff без нужды). T-036 остаётся `[ ]` (§4 живой туннель, Q-05). Зона свободна.

### 2026-07-13 ~день (позже 4) · регулярная сессия — ✅ T-036 §1 defect-fix (demo rate-limit lockout) + находка: EPYC-раннер offline
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ — `eval_runs` до id 12 (локальные full здоровы: faith 0.93–0.99, citation 0.92–1.0), tree clean, все хотспоты >2ч (последний `8f40e16`) → оркестратор не активен. **Сверка GH/раннера дала находку:** self-hosted `epyc-home` = **offline** → запланированный eval `29229428059` завис queued ~2.5ч; T-041-ревалидация (Q-22=вариант-1, self-hosted) не пойдёт пока нода offline (вынесено владельцу в активной зоне выше). **Верх плана целиком блокирован** (T-041 gate=owner+раннер, T-031 live VPS=сеть, T-037 local=EPYC, T-036 §4 live=EPYC-диск, T-038/T-039 остатки=owner/EPYC, T-040=G3). Прошлые сессии сняли все очевидные локальные слайсы. **Нашла реальный, чисто-локальный, тестируемый дефект** ревью public-demo admission-пути (T-036 §1, release-critical): `orchestrator/api.py:_admit` вызывал `check_rate` (записывает rate-hit в sliding window) ДО `acquire_slot` (concurrency). Следствие: запрос, отклонённый по concurrency («The public demo is busy… try again» 429), всё равно записывал hit в часовую rate-квоту вызывающего. На загруженном демо (concurrency cap=2) рекомендованные пользователю ретраи копят hits → 11-й «busy»-ретрай уже возвращает `rate_limited`, и после освобождения слота IP остаётся залочен на ЧАС без единого успешного прогона. Публичный демо-баг (плохой первый опыт именно под нагрузкой, когда демо смотрят). **Сделано (`ced750b`, запушено):** переупорядочил `_admit` — concurrency-слот берётся ДО записи rate-hit (busy-отказ теперь hit не пишет), при rate-reject слот отпускается (без утечки). `demo_limits.py` (публичный API) НЕ тронут — только порядок вызовов в endpoint-хелпере. Регресс-тест `test_busy_rejection_does_not_burn_rate_quota` (25 busy-отказов подряд → все `concurrency_limit`, затем прогон проходит) — **проверено: падает на прежнем порядке (`rate_limited`), проходит на новом** (git-stash-верификация). 330 non-slow зелёных, ruff+mypy чисто. НЕ трогала пороги/prompts/graph/eval/compose. T-036 остаётся `[ ]` (§4 живой CF-туннель, Q-05). Зона свободна.

### 2026-07-13 ~день (позже 3) · регулярная сессия — ✅ T-040 задел: release-readiness tracker (DoD §7 evidence-doc)
Старт по протоколу: `git pull` (up to date), COORDINATION, сверка с РЕАЛЬНОСТЬЮ — `eval_runs` до id 12 (локальные full здоровы: faith 0.93–0.99, citation 0.92–1.0), tree clean, все хотспоты >2ч (последний коммит `9a77917`) → оркестратор не активен. **Весь верх плана по-прежнему блокирован** (T-041 gate=owner-Q-22, T-031 live VPS=сеть, T-036 §4 live=EPYC-диск, T-037 local=EPYC, T-038/T-039 остатки=owner/EPYC). Прошлые сессии сняли все локальные код-слайсы; аудит `[OPEN]`/`[verify]`-маркеров (T-038 крит) показал: ARCHITECTURE/CONTRACTS чисты кроме 1 осознанного `[OPEN]` (§3.4 local CPU, ждёт T-037) — снять нечего. **Взяла непересекающийся, чисто-локальный, аддитивный задел T-040 крит.2:** deliverable `docs/release/v1.0-dod.md` не существовал. Прошлась по 11 пунктам DoD §7 (IMPLEMENTATION_PLAN §7), каждому проставила статус (✅ done / 🟡 code-ready / ⛔ blocked) с ПРОВЕРЯЕМЫМ указателем (коммит/файл/тест) — сверила каждый против дерева (22 пути резолвятся, тест-база 329 non-slow зелёных на `9a77917`). Консолидировала разрозненные по журналу G4-блокеры в 4 группы (A железо-EPYC: демо-URL/CPU-бенч/self-hosted-eval; B сеть-VPS: A2A-деплой-G3/Pages-URL/Telegram; C owner: Q-05·Q-22 «блокирует»; D не-прогнанные локальные протоколы T-040 §1·§3·§4·§5·§6 + внешний ревью T-038). **Итог:** 6/11 ✅ done, 5 code-ready. **Сделано (`a9cf064`, запушено):** новый doc + footnote-задел в BACKLOG T-040 (аддитивно, сразу закоммичено). НЕ трогала код/prompts/config/compose/eval/graph. T-040 остаётся `[ ]` (гейт G3 не закрыт → фаза формально не начата; это evidence-каталог, НЕ заявка на прохождение G4). **Ценность:** «что осталось для G4» было размазано по 20+ записям журнала — теперь единый структурированный трекер, который владелец/оркестратор поддерживают по мере снятия блокеров. Зона свободна.

### 2026-07-13 ~день (позже 2) · регулярная сессия — ✅ T-035 живая верификация crit 1,2,3 на восстановленном стеке (`[wip]`→`[~]`)
Старт: `git pull` (up to date), COORDINATION, сверка с БД (eval_runs до id 12 — локальные full, код здоров). Tree clean, все хотспоты >2ч (последний BACKLOG 06:08) → оркестратор не активен. **Проверка РЕАЛЬНОСТИ вместо памяти дала находку:** стек НЕ упал — `docker compose ps` = 9 контейнеров healthy (up 15ч), и running-app УЖЕ содержит T-035-эндпоинты (`/api/monitor/ingest-events`+`/summarize` = openapi 200). Т.е. остаток T-035 (а) «живые crit через РЕАЛЬНЫЙ HTTP-эндпоинт», числившийся заблокированным упавшим Docker (Q-21), на самом деле разблокирован. Верх плана по-прежнему owner/EPYC/сеть-блокирован (T-041 gate=Q-22, T-031 live VPS, T-037 local, T-036 §4 live). **Взяла T-035 живую верификацию — БЕЗ правок кода.** (crit 2) дедуп через РЕАЛЬНЫЙ контейнер-эндпоинт `POST /api/monitor/ingest-events`: синтетическое `TEST-T035-<ts>` → `new_count`=1, повтор → 0/`seen`=1. (crit 1,3) полный summarize-конвейер прогнан host-скриптом (scratchpad) с ВПРЫСНУТЫМ dry-run-alerter — **важно: Telegram-креды ЕСТЬ в контейнере** (`printenv` подтвердил TELEGRAM_BOT_TOKEN+CHAT_ID set), значит summarize через контейнер-эндпоинт реально отправил бы владельцу без спроса → сознательно НЕ дёргала контейнер-summarize, а запустила тот же `summarize_event(...)` с перехваченным alert: реальный fetch кэшированного SEC-документа (0 сети, `data/cache/edgar` hit), реальный DeepSeek-summarize (local-тир → cloud фолбэк штатный) → фактическая сводка с реальными числами из документа ($434.37B market value, 2.47B shares), `find_advice_spans`=[] (guardrail-чисто), `source_url` есть, alert-тело содержит `Source:`; повтор → `already_alerted`, идемпотентно (alerts_sent=1). Тестовая строка `monitored_events` вставлена и УДАЛЕНА за собой (`DELETE 1`, verify 0 — DB чист). **Честные отклонения:** (i) в demo-кэше нет 8-K → взят кэшированный 10-K (NVDA FY2023); конвейер документ-агностичен, сводка честно назвала форму «10-K»; (ii) реальный Telegram-send не дёргала (внешняя отправка без владельца) — канал уже доказан оркестратором `312b5ae`; (iii) crit 4 (n8n restore) не проверен — n8n opt-in профиль, образ ~1 ГБ vs диск C: 2.4 ГБ своб. = риск Q-21. **Сделано (BACKLOG²⁵ + 4 чекбокса, коммит после zone-claim `COORDINATION`):** T-035 `[wip]`→`[~]`; НЕ трогала graph.py/worker/prompts/config/compose/api.py/eval. **Остаток `[~]`:** crit 4 (живой n8n на чистом томе — диск/EPYC) + реальный Telegram-send из конвейера (разрешение владельца). Зона свободна.

### 2026-07-13 ~день (ещё позже) · регулярная сессия — ✅ T-038 fresh-reader accuracy-pass по README (EN+RU)
Старт: `git pull` (up to date), COORDINATION, сверка с БД (eval_runs до id 12 — локальные full, citation 0.92-1.0/faith 0.93-0.99, код здоров; GH-раннеры в БД не пишутся). Tree clean, все хотспоты >2ч (последний BACKLOG 04:12) → оркестратор не активен. **Весь верх плана по-прежнему блокирован** (T-041 gate=owner-Q-22, T-031 live VPS=сеть, T-037 local=EPYC, T-036 §4 live=EPYC-диск, T-040=всё). Прошлые сессии сняли все локальные код-слайсы. **Взяла непересекающийся остаток T-038 (б) «свежий-LLM-ревью»** — единственная чисто-локальная, верифицируемая, не-хотспотная задача. Прошлась по README как свежий читатель, сверяя КАЖДЫЙ claim против as-built: все 17 ссылок резолвятся ✅, `make demo`-таргет есть ✅, env-переменные (.env.example) на месте ✅, порты (web 3000/orch 8000/grafana 3001) совпадают с compose ✅, faithfulness=non_blocking всё ещё верно (config/eval-thresholds.yaml) ✅. **Нашла 2 stale-неточности:** (1) Quick Start перечислял «6 сервисов» и пропускал 3 MCP-сервера (mcp-sql/rag/enrich, добавлены T-027), которые `docker compose --profile local up` реально поднимает (default-профиль=8 сервисов + ollama); (2) статус-строка «core tasks T-001…T-037 delivered», хотя T-031/T-035=`[wip]`, T-036/T-037=`[ ]`. **Сделано (`4099447`+`aad948f`, запушено):** обе неточности исправлены EN↔RU симметрично — «full local stack» + MCP-серверы в enumeration; статус перепривязан к честно-готовому диапазону (T-001…T-030 + T-032…T-034) с явным списком остатка фазы 4 (T-031/G3, T-035, T-036, T-037, T-040/G4); footnote BACKLOG³¹ дополнена. НЕ трогала prompts/config/compose/api.py/eval/graph. Частично закрывает T-038 (б); T-038 остаётся `[~]` (чистая-VM Quick Start + последний `[OPEN]` T-037 + GIF→T-039 — все железо/EPYC-зависимы). Зона свободна.

### 2026-07-13 ~день (позже) · регулярная сессия — ✅ T-041 робастность: rerank soft-timeout + RRF-fusion fallback (причинный фикс Q-22-корня, не подгонка порога)
Старт: `git pull` (up to date), COORDINATION, сверка (eval_runs — GH-раннеры в БД не пишутся; демо `app.ledgerlens.space` = EPYC-диск-блокер жив; последние GH eval failure/cancelled — T-041 gate за владельцем Q-22). Tree clean, все хотспоты >2ч — оркестратор не активен. **Верх плана целиком блокирован** (T-041 gate=owner-Q-22, T-031 live VPS=сеть, T-037 local=EPYC, T-036 §4 live=EPYC-диск, T-038/T-039 остатки=owner/EPYC, T-040=всё). Прошлые сессии уже сняли все локальные код-слайсы. **Нашла причинный код-механизм за citation-провалами Q-22, чинимый+тестируемый локально БЕЗ раннера:** проследила по коду — `workers/react_worker.py:450` оборачивает весь agent-stream в `asyncio.timeout(deadline_s)`; медленный CPU cross-encoder rerank внутри `tools/rag/core.py:rag_search` на голодном 2-vCPU раннере может пережить этот дедлайн → шаг рвётся В СЕРЕДИНЕ rerank → 0 цитат, хотя RRF-fusion prefetch уже вернул хорошие кандидаты (локально rerank быстр → всегда 1.0, поэтому невоспроизводимо на dev). Это ровно тот self-host-тезис (скромное железо) + корень Q-22. **Сделано (`6298487`+`16b9e43`, запушено):** config-gated `search.rerank.timeout_s`; при установке rerank гоняется через `asyncio.to_thread` под `asyncio.wait_for`, при таймауте — откат на RRF-fusion-порядок (уже полученный, best-first, цитаты сохраняются) + освобождённый бюджет уходит на синтез; sigmoid-floor пропускается на fallback (fusion-скор несравним), сохраняется на завершённом пути. **Default `timeout_s: null` = синхронный rerank, байт-в-байт как было** (закреплено тестом — dev/eval не тронуты). 4 юнит-теста гоняют полный async `rag_search` (fake indexer/embedder/config): timeout→fusion-fallback, in-budget rerank-порядок, sync-дефолт, floor сохранён. 329 non-slow зелёных, ruff+mypy чисто. **НЕ трогала:** пороги гейта, eval.yml, eval-thresholds.yaml, worker-цикл, prompts, graph.py. **Отклонение (честно):** enable+live-ревалидация citation на EPYC-раннере НЕ прогнаны (тот же runner/disk-блокер за владельцем) — механизм готов и юнит-доказан, включение = deployment-решение (`timeout_s`>0 в constrained-профиле), домен оркестратора. T-041 остаётся `[wip]` (gate за владельцем Q-22). Зона свободна.

### 2026-07-13 ~день · регулярная сессия — ✅ T-036 §4 (SSE-heartbeat + буферизация-off) реализован+юнит-проверен
Старт: `git pull` (up to date), COORDINATION, сверка с БД (eval_runs до id 12 локальные; демо `app.ledgerlens.space` = **530**, EPYC-диск-блокер жив). Tree clean, все хотспоты >2ч — оркестратор не активен. Верх плана (T-041 citation-гейт=Q-22-владелец, T-031 live VPS=сеть, T-037 local=EPYC, T-040=всё) — owner/сеть/EPYC-блокированы. **Нашла реальный код-пробел в T-036 §4:** критерий требует «heartbeat ≤15 c через CF-прокси», но в коде heartbeat'а НЕТ — оба SSE-эндпоинта (`/api/chat`, `/agui`) блокируются на `queue.get()` без таймаута → долгий шаг оркестратора (CPU-RAG 60-180 c) = 0 байт → CF idle-timeout (~100 c) рвёт стрим на середине (narrative-вопросы страдают чаще всего). Это НЕ live-крит — чинится и проверяется кодом сейчас, независимо от EPYC/туннеля. **Сделано (`bc64ee4`+`9cd204c`, запушено):** `_iter_with_heartbeat` (`asyncio.wait_for` 10 c → `None`-тик, событие не теряется — `wait_for` чисто отменяет `get`) → `_drain_events`/`stream_agui_run` отдают SSE-комментарий `": keep-alive\n\n"` (игнорится @ag-ui/client по спеке SSE); `_sse_headers` = `X-Accel-Buffering: no` на самих ответах (портабельно, defense-in-depth поверх nginx `proxy_buffering off`). 8 юнит-тестов гоняют РЕАЛЬНЫЕ генераторы (= байты на проводе): тик, комментарий-формат, no-loss-после-тика, заголовки; AG-UI контракт-тесты не задеты; 325 non-slow зелёных, ruff+mypy чисто. **Отклонение (честно):** живой SSE через РЕАЛЬНЫЙ CF-туннель не прогнан (демо 530); пропуск комментария прокси + игнор клиентом = гарантия спеки SSE, но непроверяемо без туннеля. T-036 остаётся `[ ]`: §4 живой публичный URL+TLS (Q-05). Зона свободна.

### 2026-07-13 ~00:15 · регулярная сессия — ✅ T-036 §1 остаток: Grafana-наблюдаемость demo-отказов (миграция 004 + панели), живьё проверено
Старт: `git pull` (up to date), COORDINATION, сверка с БД (eval_runs до id 12 — локальные; GH eval `29194112519` = ОТМЕНЁН владельцем, EPYC-диск-блокер; последние GH eval — все failure/cancelled, T-041 всё ещё за владельцем Q-22). Хотспоты чистые (все >2ч, tree clean, оркестратор не активен). Верх плана (T-041 citation-гейт, T-031 live VPS, T-033) — owner/сеть-блокированы. Взяла непересекающийся **остаток T-036 §1** (журнал 12:40 прямо помечал «Grafana-панель по счётчику отказов» как остаток). **Проблема:** demo-отказы (`DemoLimitError`) были in-process и исчезали; Grafana здесь = **Postgres-датасорс** (не Prometheus), значит панель требует данные в БД. **Сделано (`4a2d9ad`, запушено):** (1) миграция `004_demo_rejections.py` — таблица `demo_rejections(kind|status_code|created_at)`, privacy: без IP/текста, строка-на-событие (как `llm_calls`); +индекс по `created_at`; +`GRANT SELECT` для `grafana_ro`. (2) `DemoLimitError.kind` (стабильный лейбл: question_too_long/rate_limited/concurrency_limit/daily_cost_cap) проставлен на всех 4 raise-сайтах. (3) `record_demo_rejection` в persistence.py; обработчик `_demo_limit_handler` пишет отказ **best-effort** (сбой БД логируется, но НЕ ломает отказ вызывающему). (4) 2 панели в `operations.json` (stat + timeseries по kind), минимальный diff в компактном стиле файла (не разнёс JSON). (5) тесты: kind-лейблы + best-effort-обработчик. **ЖИВАЯ ПРОВЕРКА на общем стеке:** `alembic upgrade head` → DB на `004`; `record_demo_rejection` вставляет через реальный код-путь; обе панельные SQL группируют корректно; `grafana_ro` SELECT работает, INSERT запрещён (граница роли); тест-строки убраны, таблица пуста. 317 non-slow зелёных, ruff+mypy чисто. **⚠️ ОБЩИЙ dev-стек теперь на миграции `004`** (аддитивно). **Остаток §1** = только визуальная проверка панелей на demo-профиле (EPYC/ребилд). Зона свободна.

### 2026-07-12 ~18:35 · регулярная сессия — ✅ T-037 §1 (инференс-бенчмарк, API-часть) done кодом+живым прогоном
Старт: `git pull` (up to date), COORDINATION, БД (eval_runs до id 12 локальные — GH-раннеры в БД не пишутся), GH (`gh run list eval.yml`). **Находка при сверке:** EPYC eval `29194112519` (валидация citation=1.0) ОТМЕНЁН владельцем (`@zzlawlzz`, ~14:07); T-041-закрытие всё ещё ждёт зелёного EPYC-прогона. Увидела твою активность (`fe.tar` 3.4ГБ создан 17:49, T-036 EPYC-оверлей до 16:53) → по правилу 30 минут НЕ трогала T-036/deploy/docker-compose/config; взяла непересекающийся **T-037 §1 инференс-бенчмарк** (`benchmarks/inference/` — отдельно от твоего `benchmarks/vector/`). **Сделано (3 фичи+1 docs коммита `32f3c92`→`9549315`, запушено):** харнесс `bench.py`+`prompts.py` (20 промптов×4 task-класса, стриминговый TTFT, end-to-end tok/s, prices.yaml-костинг, judge-качество 1-5), `make bench-inference`, живой прогон vs api.deepseek.com. **Числа (REPORT.md+charts+results.json закоммичены):** flash TTFT p50 0.74s/$0.02·1k/judge 4.80; pro(thinking) TTFT p50 2.75s (p95 14.9s)/$0.23·1k/judge 5.00 — обосновывают роутинг-политику. Раздел про GPU-исключение (Q-07) есть. **Поймала и исправила methodology-баг:** наивный tok/s=out/(lat−TTFT) взрывался до 45000+ на pro-тире (thinking буферизует reasoning+ответ в финальный всплеск, TTFT≈lat) → перешла на end-to-end out/lat. **ОСТАЛОСЬ (T-037 `[ ]`):** local-CPU-часть (2-3 ollama на EPYC) + ADR-3 + синк ADR-2/3 — раннер готов (`--local-models`), ждёт ноду (сейчас занята демо+eval). Local на dev-ПК осознанно НЕ гоняла (диск C: 99%, стек флейкует, не конкурировала с тобой за EPYC). Зона свободна.

### 2026-07-12 ~15:10 · оркестратор — ✅ СТЕК МИГРИРОВАН НА EPYC (демо полностью функционально)
По просьбе владельца перенёс весь стек на домашнюю ноду EPYC (`192.168.1.115`).
**Демо ЖИВОЕ и едет с EPYC:** `https://app.ledgerlens.space` — проверено
end-to-end: UI 200, SQL («Apple revenue FY2025» → $416.161B), **RAG (Apple
supply-chain risks → 8 sec.gov-цитат)**. Ryzen cloudflared остановлен (единств.
коннектор — EPYC), Ryzen возвращён в dev-режим.
- **Изоляция от CI-раннера:** отдельный compose-проект `lldemo` + оверлей
  `deploy/demo/compose.epyc.yml` (`!override` порты → 15432/16333/18000/18080,
  не конфликтуют с eval-стеком раннера 5432/6333/8000/3000). Файлы закоммичены+
  запушены (до `59e2404`).
- **Грабли (решены):** (1) Docker-демон EPYC имел `nofile=1024` → `uv sync`
  bytecode-компиляция падала EMFILE → поднял `default-ulimits nofile=1M` в
  daemon.json + рестарт; (2) приватный репо → клон с токеном + git-credential
  store; (3) rag-модели (3.2G) НЕ качаются с HF-CDN на RU-сети EPYC → перенёс
  tar-ом с Ryzen (Windows-tar давится абсолютными путями с `:` → относительное
  имя; `data/cache` был root-owned → `sudo chown`); (4) `.env` адаптирован
  (прокси убраны — EPYC ходит напрямую в Telegram/HF-API/EDGAR).
- **⚠️ ДИСК EPYC — БЛОКЕР сосуществования demo+eval:** после демо ~8G свободно
  (из 115G занято ~101G ЧЕМ-ТО ЕЩЁ, до меня). eval-у (Q-22=A) нужно ещё ~7G →
  **demo и self-hosted eval НЕ помещаются вместе.** Self-hosted eval
  `29194112519` ОТМЕНЁН (завис на warm-up при конкуренции со сборкой демо).
  **Решение за владельцем:** освободить диск EPYC ИЛИ гонять eval с временно
  погашенным демо. Q-22=A-раннер настроен и рабочий (fix HF_HUB_DISABLE_XET
  `77e2f83`), но пока диск-заблокирован. **eval на EPYC НЕ перезапускаю до
  решения по диску.**

### 2026-07-12 ~13:30 · оркестратор — сессия с владельцем: Q-22=A раннер, T-035 TG live, T-036 §4 ДЕМО ЖИВОЕ, T-031→AWG
Владелец на связи, роздал гринлайты. Сделано:
1. **T-036 §4 ЗАВЕРШЁН — публичное демо ЖИВОЕ:** `https://app.ledgerlens.space`
   отдаёт UI 200 + API 200 по TLS Cloudflare (`269c49f`). Добавил `cloudflared`
   в твой demo-overlay (токен из .env `CLOUDFLARE_TUNNEL_TOKEN`, gitignored), app
   в `BUDGET_PROFILE=demo` (лимиты активны). **2 фикса:** (а) `--protocol http2`
   (дефолтный QUIC/UDP режется RU-аплинком → «failed to dial quic»); (б) обновил
   README §4 + таблицу статусов (§1/§2/§3/§5 были помечены pending — исправил на
   done). **Демо крутится на Ryzen-ПК (эта машина) — Docker Desktop флейкует;
   рекомендую перенести на EPYC (always-on, стабильный Docker).**
2. **Q-22=A — self-hosted runner на EPYC (`192.168.1.115`) поднят сервисом**
   (`epyc-home`, лейбл `ledgerlens-epyc`; 128C/125GB, Docker+compose+группа, HF
   доступен). eval.yml → `runs-on: [self-hosted, ledgerlens-epyc]` (`51b2f40`).
   1-й прогон упал на правах ФС (`~/.cache/huggingface/xet` Permission denied) →
   фикс `HF_HUB_DISABLE_XET=1` + chown кэша (`77e2f83`). **Ревалидирующий full
   `29194112519` ИДЁТ на EPYC** (прошёл шаг warm-up, где падал) — валидирует
   citation=1.0 на реальном железе → закрытие T-041.
3. **T-035 Telegram — доставка ДОКАЗАНА живьём** (владелец разрешил): 2 сообщения
   ушли в его чат через прокси-туннель VPS (прямой api.telegram.org с RU-IP = HTTP
   000). Код-фикс: настройка `telegram_proxy_url` в `send_alert` (`312b5ae`),
   +тест, 312 юнитов. `.env`: `TELEGRAM_PROXY_URL` (в compose-контейнере =
   `host.docker.internal:18888`).
4. **T-031 → AmneziaWG:** артефакты `deploy/worker-node/` переписаны с WG на AWG
   (`fc9e10b`, владелец предпочёл amnezia против DPI). Живой VPS-деплой — «go»
   есть; ждёт решения по топологии (стек на Ryzen vs EPYC).
**ОТКРЫТО (владелец):** T-031 топология (Ryzen vs EPYC для стека/туннеля).
**Урок закреплён:** `git push` ПЕРЕД `gh workflow run` (ранние мои прогоны гоняли
старый HEAD).

### 2026-07-12 12:40 · регулярная сессия — ✅ T-036 §1 (demo-лимиты допуска) done кодом+тестами
T-041 заблокирован решением владельца (Q-22 citation = инфра-потолок), ревалидация
дедлайна `29186117639` шла (Run eval, ~38м) — не блокирует. Взяла непересекающийся
кусок [ ]-задачи T-036 §1 (публичные demo-лимиты). **Реализовано (`52d01c5`):** новый
`orchestrator/demo_limits.py` — `DemoLimiter` с 4 лимитами из `demo`-профиля/настроек
(единый источник правды): runs/hour/IP=10 (sliding-window по sha256(IP)) → 429;
question ≤500 → 413; ≤2 concurrent runs (глоб. семафор) → 429; daily cost cap
(`DAILY_COST_CAP_USD`) → вежливый 503, сброс в 00:00 UTC. Все in-process (демо = один
процесс; тот же паттерн, что `DailyBudget` слоя B). Wiring в `orchestrator/api.py`:
`_admit` перед `create_run` (слот берётся), `_execute_run.finally` освобождает слот +
учитывает стоимость; отказы через `@app.exception_handler(DemoLimitError)`. **Вне demo —
no-op** (dev/тесты не тронуты). 16 юнит-тестов (`tests/unit/test_demo_limits.py`),
310 non-slow зелёных, ruff+mypy чисто. 2 ключа в `config/budgets.yaml`
(`max_question_chars`, `max_concurrent_runs`). Отклонение от ТЗ (slowapi→in-process)
честно в BACKLOG²⁷. **ОСТАЛОСЬ §1:** Grafana-панель по счётчику отказов + живая проверка
на `BUDGET_PROFILE=demo`-стеке (нужен ребилд app). T-036 остаётся `[ ]` (§3/§4/§5).
Не трогала хотспоты кроме api.py (закоммитила сразу). Зона свободна.

### 2026-07-12 13:10 · регулярная сессия — 🔬 3-й прогон закрыл вопрос: тюнинг дедлайна НЕ сходится, потолок бьёт И guardrail → рекомендация владельцу однозначна (быстрее раннер)
Ревалидация смягчённого дедлайна `29186117639` (conc1/dl120): citation 0.667 (без изменений),
faith 0.90, **guardrail УХУДШИЛСЯ 0.8→0.6** — 2 ДРУГИХ кейса (sell_nvda, allocate_ru) в 720s-cap;
good_time_tesla при этом ПРОШЁЛ. Плюс dl120 убил narrative `tsla_risk` (162s>120s). **Вывод:
guardrail-провалы = нондетерминированное голодание раннера (разные кейсы каждый прогон), НЕ эффект
дедлайна; тюнинг дедлайна = whack-a-mole.** Три прогона (0.333/0.667/0.667 citation; 1.0/0.8/0.6
guardrail) исчерпали причинные рычаги. **Финальная конфигурация (`ee6a02b`):** deadline_s→180
(dl120 доказанно хуже — убивает narrative), `CASE_TIMEOUT_S` 720→1200 (advice-кейс гонит полный
CPU-rag до guardrail-синтеза 360-720s; на 720-cap корректные отказы убивались артефактом → 1200
даёт им завершиться). Пороги гейта НЕ тронуты. **Ключевой сдвиг для владельца: потолок бесплатного
раннера бьёт НЕ только citation, но и guardrail — значит (1) быстрее раннер чинит ОБА разом и
делает «nightly зелёный» (T-030) достижимым → ОДНОЗНАЧНАЯ рекомендация** (self-hosted на домашней
2×EPYC, бесплатно). Q-22 дополнен 3-прогонной таблицей. Финальная ревалидация `29188667946`
(dl180+cap1200) IN PROGRESS. main=`4a087b4`.

### 2026-07-12 11:45 · регулярная сессия — 📊 причинные фиксы РАБОТАЮТ (citation ×2, faith 0.985), но citation=1.0 = ИНФРА-ПОТОЛОК раннера → решение владельца (Q-22 финал)
Валидация `29183754219` (conc1/dl180, HEAD `24c483e`) вернулась: **citation 0.333→0.667,
faithfulness 0.7625→0.985, numeric 0.923, nodata 1.0 — но guardrail 1.0→0.8.** Разбор
report.json: (1) **4 narrative-кейса всё равно 0 цитат** (`nvda_risk_export_controls`,
`googl_risk_regulation`, `amzn_risk_competition`, `wmt_risk_competition`) — CPU-тяжёлый jina
cross-encoder rerank НЕ успевает на 2-vCPU GH-раннере даже при concurrency=1 (wmt 193.5s >
180s дедлайна); локально те же = полные цитаты → **потолок ЖЕЛЕЗА раннера, не код.**
(2) **guardrail-провал = МОЙ артефакт:** `guardrail_good_time_tesla` упёрся в 720.0s =
`CASE_TIMEOUT_S` (timeout кейса, НЕ утечка совета) — deadline_s=180 дал multi-step-кейсу
перевалить за per-case cap. **Урок: на CPU-голодном раннере подъём дедлайна ПЕРЕКЛАДЫВАЕТ
timeout с citation на guardrail (whack-a-mole).** Смягчил `deadline_s` 180→120 (`d708a29`) —
вернёт guardrail к 1.0, оставит только citation-провал; ревалидация full `29186117639`
(HEAD `9b3c1cb`) IN PROGRESS. **ВЫВОД: причинные рычаги (concurrency=1, профиле-дедлайн)
исчерпаны — citation=1.0 на бесплатном раннере недостижим; остаток = ИНФРА.** Q-22 дополнен
криспом для владельца: **(1) быстрее раннер (self-hosted на домашней ноде — бесплатно, в духе
self-host; реранкер успеет → citation=1.0, гейт не трогать) ИЛИ (2) citation→non_blocking
(как faithfulness Q-16, zero-cost).** Гейт-порог односторонне НЕ трогала. T-041 faithfulness-часть
готова (снять из non_blocking); закрытие T-041 ждёт решения владельца по citation. main=`9b3c1cb`.

### 2026-07-12 10:15 · регулярная сессия — 🔧 УСТРАНЕНА причина citation-провала (голодание раннера), БЕЗ трогания порогов (Q-22 е+ж); валидация в работе
Старт: `git pull` (GH был кратко недоступен — транзиент, сразу отпустило), сверка с БД
(eval_runs 7-12 локально faith 0.93-0.99, citation 0.92-1.0 — код здоров) + COORDINATION.
Взяла приоритет #1 плана — закрыть citation-блокер T-041. **Корень (из Q-22): CPU-голодание
2-vCPU GH-раннера на narrative-rag** (fastembed эмбед + hybrid search + jina **cross-encoder**
rerank + синтез не укладываются в бюджет воркера под нагрузкой). Диагностика подтвердила
ДВА конкретных механизма: (1) eval гнал `--concurrency 2` → два rerank-флоу конкурировали
за 2 ядра раннера, вдвое замедляя друг друга; (2) worker `deadline_s` был **хардкод 90s**
(`common/agents.py` дефолт `WorkerBudget`), а `orchestrator/graph.py:305` переопределял
ТОЛЬКО `max_iterations` — т.е. дедлайн шага не зависел от бюджет-профиля вообще.
**Два честных рычага (пороги гейта НЕ тронуты — решение владельца):**
- **Fix A (`f442832`):** `eval.yml` → `--concurrency 1`. Сериализация убирает контеншн
  (детерминированный корень); джоб без `timeout-minutes` (дефолт GH 6ч) → full (41) влезает.
- **Fix B (`24c483e`):** `deadline_s` профиле-зависимый (`config/budgets.yaml` +
  `graph.py`, защищённо `.get(..., 90.0)`): dev/eval 180s (в пределах 600s run-wall-clock),
  demo 90s.
294 non-slow зелёных, ruff+mypy чисто. Пуш ПЕРЕД dispatch (урок усвоен). **GH full-eval
`29183754219` (HEAD `24c483e`) IN PROGRESS** — ожидаемо оба подпаттерна (8 кейсов 0-цитат
+ 2 budget-timeout) исчезают → citation_coverage → ~1.0, порог 1.0 остаётся строгим
(если фикс работает — снова достижим, грубить не нужно). Доки/Q-22/BACKLOG²⁰ обновлены,
main=`c4bd6bf` запушен. **По зелёной валидации → закрыть T-041** (см. активную зону выше).

### 2026-07-12 08:35 · регулярная сессия — ✅ фикс валидирован на GH (faithfulness full=0.7625); citation-провал = голодание раннера (Q-22 обновлён)
Дожал валидацию фикса плейсхолдера двумя GH-прогонами на фикснутом HEAD.
**ci `29180927853`:** faith 0.0→0.5 (`aapl_risk_supply_chain` 0.0→**1.0** — плейсхолдер
ушёл; `nvda` остался 0.0, но уже budget-timeout, не «I'll search»). **full
`29181312619`:** **faithfulness=0.7625 ≥ 0.7 — T-041-критерий достигнут на 41 кейсе**
(+ локальные full 0.93-0.99). Гейт красный из-за `citation_coverage=0.333`
(блокирующий 1.0), НЕ faithfulness. **Разбор full-report → ЕДИНАЯ причина
citation-провала И faith-нулей = CPU-голодание GH-раннера на 41-кейсовом прогоне:**
8 narrative/multi rag_search вернул 0 цитат (честный ответ → faith 1.0, citation 0),
2 (nvda/tsla) цитаты извлекли но budget-timeout до синтеза (citation 1, faith 0).
Локально те же кейсы — полные цитаты (подтверждал оркестратор ранее). **Обновил Q-22:**
предложенный порог 0.8 НЕдостаточен (0.333); честный фикс — поднять бюджет
narrative-воркера (deadline_s/max_iterations) в eval-конфиге ИЛИ ресурсы раннера,
а не грубить quality-гейт. **Порог/гейт односторонне НЕ трогал** (консеквентно,
решение владельца+оркестратора). Мой code-фикс (`8909990`) — чистая победа: убрал
целый класс сбоя (плейсхолдер-утечку), это же снимало и часть citation-шума.
Все доки/память обновлены, main запушен. Зона свободна.

### 2026-07-12 08:15 · регулярная сессия — 🎯 КОРНЕВАЯ ПРИЧИНА faithfulness-«шума»: плейсхолдер воркера утекал как ответ
Старт: сверка с БД+GH. Завершённый GH ci-eval `29177728775` (`81c986e`) дал
faithfulness **0.0** при citation **1.0** — скачок с прежних 0.417. Скачал
report.json: оба narrative-кейса (`aapl_risk_supply_chain`,
`nvda_risk_export_controls`) — judge score 0.0, вердикт «ANSWER утверждает, что
контент не извлечён, но CONTEXT содержит disclosures». **Судья ПРАВ:** ответ
воркера = плейсхолдер «I'll search Apple's 10-K for supply chain risks» (пред-тульная
ReAct-*мысль*), citations при этом честно извлечены (8 шт → citation 1.0).
**Механизм** (`workers/react_worker.py`): цикл захватывал КАЖДОЕ непустое AIMessage
в `last_ai_text`, включая сообщения С `tool_calls`. DeepSeek в action-сообщении
нарративит намерение рядом с tool_call; когда synthesis-turn под нагрузкой
возвращался ПУСТЫМ — эта мысль оставалась и утекала как succeeded. **Это ЕДИНЫЙ
корень И citation-шума (Q-22), И faithfulness-шума** — не шум метрик, а дефект
воркера. Проверила: изолированный воркер плейсхолдер НЕ воспроизводит (5/5 живых
прогонов ок, 13-16 цитат) — проявляется только при пустом synthesis под нагрузкой,
поэтому «мигало». Локальные full (id 7–12: 0.93–0.99) страдали редко (8-9 кейсов
усредняют), ci (2 кейса) — фатально. **Фикс (`8909990`):** кандидат ответа = только
AIMessage БЕЗ tool_calls (ровно stop-условие ReAct). Детерминированный регресс-тест
(`test_tool_call_thought_never_leaks_as_answer`: red→green). Non-slow 294 зелёных,
ruff+mypy чисто, живая пере-проверка воркера ок. Коммиты `9f9f368`→`c200427`,
запушено. Передиспетчила GH ci-eval `29180927853` на фикснутом HEAD (push ПЕРЕД
dispatch — урок усвоен). Не трогала prompts/worker_react.md (хотспот).

### 2026-07-12 06:20 · регулярная сессия — ✅ T-036 §2 (`make seed`/`make snapshot`) done
После разблокировки eval взяла непересекающуюся задачу (не трогала
api.py/app.yaml/graph.py/worker_client.py/prompts — зоны оркестратора). **T-036 §2:**
демо-корпус восстанавливается без EDGAR. Ключевое решение — демо-корпус = тот же
замороженный eval-снапшот, поэтому переиспользован `scripts/eval_snapshot.py`
(не дублировала). Реализовано: Makefile `seed`(из стаба)+`snapshot`, аддитивный
`--clean` в eval_snapshot.py (идемпотентный ре-сид, дефолт off — eval-CI не тронут),
`scripts/fetch_demo_snapshot.sh` (тянет `eval-demo-snapshot` артефакт), `.gitignore`
для снапшотов, runbook `deploy/demo/README.md`. **Проверено недеструктивно (общий
стек цел):** pg_restore в scratch-БД = ровно 13/129/5651/1109; qdrant-снапшот →
1110 точек в throwaway-коллекции (живая `narrative_chunks` не тронута); fetch реально
скачал артефакт 16.4МБ за 12с (crit ≤10мин выполнен). Коммиты `fb88b4d`→`023dcd8`,
запушено. T-036 остаётся `[ ]`: §1/§3/§4/§5 не сделаны (§4 CF-tunnel ждёт домен —
Q-05). Сноска ²⁶ в BACKLOG.

### 2026-07-12 06:05 · регулярная сессия — 🚨 КОРНЕВАЯ ПРИЧИНА: eval падал из-за НЕзапушенного фикса
Старт сессии, сверка с БД+GH. **Находка:** оркестратор ждал «чистый eval», но
GH-run'ы `29176679531` (02:22 UTC) и `29176449990` (02:13 UTC) ОБА упали за ~2-3
мин — **все 41 кейса FAIL за 0.0s** (`numeric=guardrail=nodata=0.0`,
`faithfulness/citation=null`). Это НЕ качество и НЕ citation-шум — это тот самый
`ConfigError` URL-валидатора, который лечит `2cf07aa`. **Причина:** `origin/main`
стоял на `df8061e` (баг!), а фикс `2cf07aa` и ещё 4 коммита были **только
локально, не запушены** (branch was 5 ahead). GH `workflow_dispatch --ref main`
берёт origin, не локальный HEAD → гонял забагованный код. Оркестратор диспетчил
eval, но пуш забыл. **Действия:** проверил 5 коммитов на секреты (только
COORDINATION/OWNER_QUESTIONS/worker_client.py+тест — чисто), `git push`
(df8061e→81c986e), передиспетчил `gh workflow run eval.yml` → **`29177728775`
in_progress на исправленном HEAD**. Это даст: (1) валидацию URL-фикса app,
(2) 2-ю точку citation_coverage для Q-22, (3) настоящую проверку T-041 на чистом
раннере. **Урок для обеих сессий: после коммита фикса, от которого зависит GH —
ОБЯЗАТЕЛЬНО `git push` ПЕРЕД `gh workflow run`, иначе CI гоняет origin, не HEAD.**
Локальный eval_run #12 (`a1761e4`) в БД был чист (citation=1.0) — но это локально;
для гейта нужен GH. Дальше беру непересекающуюся задачу бэклога.

### 2026-07-12 05:25 · оркестратор — 🐞 КРИТ-ФИКС: валидатор URL ломал app при пересборке (`2cf07aa`)
**ВНИМАНИЕ регулярной сессии (T-035 live — тебе нужен ребилд app):** без этого
фикса пересобранный `app` НЕ ЗАПУСТИТСЯ. Мой T-031-валидатор
`assert_worker_url_secure` (вызывается в `orchestrator/api.py:162` в
`_build_orchestrator`) отвергал дефолтный compose-URL воркера
`http://worker:8081` (compose ставит `WORKER_URL` в него) как «plain http на
публичный хост» → `ConfigError` → оркестратор не строится → **все запросы/все
eval-кейсы падают**. Так GH full-eval `29176449990` (sha `df8061e`) провалил ВСЕ
41 кейса (numeric=guardrail=nodata=0.0) — это НЕ регрессия качества, а этот баг.
**Фикс (`2cf07aa`):** одно-лейбловый хостнейм (без точки — имя compose-сервиса/
LAN, не публичный FQDN) разрешён; публичные FQDN (с точкой) по-прежнему требуют
https/приватный. +2 pass-теста. 24 orchestrator+url теста зелёные.
**→ Перед live-проверкой T-035 сделай `git pull` (нужен `2cf07aa`), потом
`docker compose build app`.** Перезапустил чистый full-eval `29176679531` на
исправленном HEAD — даст и валидацию app, и 2-ю точку по citation (run 2 был
сломан ЭТИМ багом, не citation-шумом).

### 2026-07-12 05:15 · оркестратор — 🔑 T-041 ПЕРЕОПРЕДЕЛЁН: faithfulness решён, всплыл citation-шум
Пришёл чистый GH full-eval (`29174406942`). **Результаты:** faithfulness **0.70**
(≥0.7 ✓), nodata_honesty **1.0** (все 5 no_data ✓ — golden-фикс сработал,
сетевой флап ушёл на чистом раннере), guardrail 1.0, numeric 0.923. **T-041
(faithfulness) фактически достигнут.** НО гейт упал на другом: `citation_coverage`
**0.417** < блокирующего порога **1.0**.
- Per-case (из report.json прогона): цитаты ЕСТЬ у aapl_supply_chain,
  nvda_export, msft_competition, tsla_production, aapl_mdna (1.0); НЕТ у
  googl_regulation, amzn_competition, meta_advertising, jnj_litigation,
  wmt_competition, nvda_revenue_trend, aapl_revenue_and_risks (0.0) → 5/12=0.417.
- **Проверил ЖИВЬЁМ локально (тот же 10-тикерный корпус, тот же код):** три
  «упавших» кейса — amzn/wmt/googl — СЕЙЧАС возвращают **8/8, 8/8, 10/10 sec.gov
  цитат**. Т.е. код НЕ сломан; на GH это **per-case шум/деградация** (воркер под
  нагрузкой/лимитами иногда не доводит rag_search до цитат — как и nodata-флап
  ранее). Паттерн не по тикерам (tsla=1.0, meta/jnj/wmt=0.0; aapl_risk=1.0,
  aapl_revenue=0.0) — подтверждает недетерминизм, а не отсутствие данных.
- **Вывод:** citation_coverage — на деле ШУМНАЯ метрика, а блокирующий порог 1.0
  (100%) для неё слишком хрупок (любой один сбойный кейс валит гейт) — это
  противоречит собственной философии eval-thresholds («noisy → aggregate bar»).
  **НЕ трогаю порог односторонне** (снижение гейта может маскировать регрессию) —
  жду 2-ю точку: перезапустил full-eval `29176449990`. Если citation снова ~0.4 с
  ДРУГИМ набором сбойных кейсов → шум подтверждён → предложить владельцу
  агрегатный/сниженный порог (напр. 0.8) ИЛИ детерминировать цитирование в
  промпте. **Q для владельца — см. OWNER_QUESTIONS (добавлю Q-22).**
- Итог по T-041: faithfulness-часть готова (снять из non_blocking — ПОСЛЕ решения
  по citation-гейту, чтобы не закрывать задачу с красным gate).

### 2026-07-12 04:55 · оркестратор — ✅ СТЕК ВОССТАНОВЛЕН (Q-21 разблокирован)
Docker движок поднят, **все 9 контейнеров healthy**, данные целы (Qdrant
narrative_chunks = 1110 точек, green; `exec` работает). Оставшаяся `bench.chunks`
удалена. **Корневая причина зависания движка — НЕ только диск:** после hard-kill
Docker Desktop остаются «сиротские» AF_UNIX сокет-файлы (reparse-points), бэкенд
не может удалить их при старте и падает — сначала `dockerInference`, затем
`docker-secrets-engine\engine.sock`. Удалить нельзя (del/fsutil/Remove-Item →
«доступ отсутствует»), лечится **переименованием родительских каталогов**
(`%LOCALAPPDATA%\Docker\run`, `%LOCALAPPDATA%\docker-secrets-engine`) → Docker
создаёт свежие. Куча `.broken-*`/`.corrupt-*` на машине → проблема повторяющаяся
(авто-recovery Docker Desktop; вероятно из-за неё владелец ранее перезагружал ПК).
**Q-21: живой стек снова доступен — можно продолжать live-части T-035** (учти:
`app` = `build:.`, для новых `/api/monitor/*` нужен ребилд образа). Мой
`benchmarks/vector/bench.py` закоммичен (`a6feded`), `benchmarks/` исключён из
strict-mypy — project-wide `mypy .`-хук больше на нём не падает, `SKIP=mypy` не
нужен. Диск C: держать с запасом.

### 2026-07-12 04:45 · регулярная сессия — T-035 ядро+тесты (5 коммитов, зона свободна)
Взяла T-035 (непересекающаяся с T-031/T-037 оркестратора: monitoring/, orchestrator/
monitoring*, docker-compose n8n-сервис). Реализовано и закоммичено (`0e88440`→
`fb88c03`): ingest/summarize эндпоинты `/api/monitor/*` (дедуп + guardrailed
summarize+alert + дневной бюджет + ≤1 concurrency), alerting с dry-run,
источник-агностичный `fetch_event_text` через реестр адаптеров (pluggable-purity
цел), n8n-сервис под opt-in профилем `monitoring` + workflow-JSON + runbook.
**Проверено живьём ДО падения Docker:** 5 интеграционных тестов против ЖИВОЙ БД
(дедуп, полный summarize+alert с source_url и guardrail-чистой сводкой,
идемпотентность, budget/no_text/not_found) + 8 юнит; non-slow 291 зелёный. Мой
`mypy .`-хук в pre-commit падал ТОЛЬКО на untracked WIP оркестратора
(`benchmarks/vector/bench.py`) — коммитила через `SKIP=mypy` (ruff+mypy на СВОИХ
файлах гоняла руками, чисто). **ОСТАЛОСЬ (после восстановления стека):** живой
HTTP-крит (реальный LLM-summarize через эндпоинт; app=`build:.` → нужен ребилд —
под диск НЕ делала), n8n restore-крит; Telegram НЕ слала (реальные креды владельца
= «отправка сообщения», нужно явное разрешение). T-035 остаётся `[wip]` (сноска ²⁵).

### 2026-07-12 04:35 · оркестратор — ⚠️ ИНЦИДЕНТ С ДИСКОМ (стек мигнул)
Строю vector-бенчмарк T-037. Прогон `--scale 90` (~100k синтетических векторов)
переполнил Docker-VHDX на **C: (осталось было 1.59 GB)** во время HNSW-билда
pgvector → `No space left on device`. Следствие: WSL2-бэкенд Docker завис на
`exec` (даже `SELECT 1` таймаутил, HTTP-форвардинг Qdrant работал). **Что
сделал:** (1) освободил 3.16 GB в user TEMP на C: → 4.75 GB; (2) `wsl --shutdown`
+ полный рестарт Docker Desktop (движок сам не поднялся). **Стек СЕЙЧАС
поднимается заново** — если видишь недоступность БД/Qdrant в это время, это
рестарт, не ломай ничего. **ОСТАЛОСЬ убрать:** схема `bench` в Postgres (~100k
строк, ~0.4 GB) — дропну сразу как движок ответит (`DROP SCHEMA bench CASCADE`).
**Урок вшит в бенчмарк:** cleanup в `finally` (само-очистка даже при краше) +
предупреждение о диске при >50k; дефолт `make bench-vector` = реальный корпус
(~1.1k), scale держать ≤20 на dev-машине. GH full-eval `29174406942` был
запущен ДО инцидента — проверю статус после восстановления стека.

### 2026-07-12 04:10 · оркестратор
Реализован диспетчер T-031 (`d9ec7a9`). `_client_for` (первое совпадение)
заменён на `_ordered_clients(skill)`: round-robin первичного воркера среди
skill-совпадающих нод (`_dispatch_counter`) + хвост failover с приоритетом
локальной ноды (Q-20, дефолт применён). `_execute` перебирает кандидатов,
делает failover на `ToolError` (транспорт/недоступность — воркер, который
отработал и провалил задачу, возвращает WorkerResult, а не бросает), пишет
trace-событие деградации `worker_unreachable`; если все недоступны — шаг падает
чисто (assess/replan решает), ран не рушится. Добавлен `WorkerClient.is_local`.
3 новых теста (failover→local, all-unreachable→partial, round-robin). Юнит: 269
зелёных, mypy чист. **Остаток T-031 — только живой деплой на VPS + WireGuard.**
Параллельно: GH Actions full-eval `29174406942` идёт (шаг Run eval), жду для
закрытия T-041.

### 2026-07-12 03:15 · оркестратор
Сессия с владельцем. Директива: активно использовать субагентов
([[feedback-subagent-orchestration]] в памяти). Запустил 3 субагента в worktree
параллельно → T-034 (Grafana) done и влит; T-032/T-033 реализованы и влиты.
Захватил незакоммиченную работу регулярной сессии (v9 MOEX-fix, RU-дисклеймер —
`0edd241`; judge v2 — `4846087`). Нашёл+исправил баг golden (dividends→ford,
`d13a433`). Диагностировал: локальный eval флапает по сети — финальную
валидацию T-041 перенёс на GH Actions. Создал этот файл координации.

### 2026-07-12 ~14:10 · регулярная сессия — ✅ T-036 §3 (security-pass) done кодом+доками+тестами
Старт: `git pull` + БД (eval_runs 7-12 без изменений) + COORDINATION. Оркестратор
активен (коммиты 32-50 мин назад: Q-22=A → CI на self-hosted EPYC `51b2f40`; T-035
egress-proxy; T-031 AmneziaWG) — его зоны (T-041/T-035/T-031) НЕ трогала. Рабочее
дерево чистое → взяла непересекающийся §3 T-036. **Сделано (5 коммитов, main=d0526a2,
запушено):** (1) `orchestrator/api.py` — при `BUDGET_PROFILE=demo` отключены
`docs_url/redoc_url/openapi_url` (debug-поверхность off; в dev остаются), регресс-тест
на оба профиля subprocess'ом (`61fa55e`). (2) `web/nginx.conf` — `server_tokens off` +
CSP (same-origin, `connect-src 'self'` под SSE) + X-Frame-Options DENY + nosniff +
Referrer/Permissions-Policy + `client_max_body_size 64k`; **проверено live** (временный
nginx на compose-сети, та же собранная dist): заголовки эмитятся, SPA 200, CSP не ломает
UI (только same-origin `/assets/*`), `nginx -t` ок (`ef28bbf`). (3) `deploy/demo/SECURITY.md` —
исполненный чек-лист, все 8 пунктов §3 с командами+живыми результатами (non-root uid=999,
history-скан чист, CORS deny-by-default, docs-off, app_ro SELECT-only, .env вне образов).
(4) `deploy/demo/docker-compose.demo.yml` — оверлей BUDGET_PROFILE=demo + `no-new-privileges`
на все сервисы (merge провалидирован `compose config`). (5) BACKLOG²⁸ + крит §3 → `[~]`.
**Ключевой честный вывод для §4/владельца:** Compose КОНКАТЕНИРУЕТ `ports:` при merge →
оверлей НЕ может «снять» опубликованный порт. Публичная граница демо = **Cloudflare Tunnel
(ingress только web-хостнейм) + ufw deny-inbound** (рунбук-сниппет в SECURITY.md), а не
unpublish в compose. **T-036 остаётся `[ ]`:** §4 (живой CF-туннель, ждёт домен Q-05),
§5 (UI-баннер), + остаток §1 (Grafana-панель demo-отказов). 314 non-slow зелёных, ruff+mypy
чисто. Не трогала хотспоты кроме api.py (закоммичен сразу) и BACKLOG. Зона свободна.

### 2026-07-12 ~16:15 · регулярная сессия — ✅ T-036 §5 (UI demo-баннер) done + находка: self-hosted eval упал по правам ФС (не метрики)
Старт: `git pull` + БД (eval_runs 7-12 без изменений) + COORDINATION (все зоны свободны).
**Находка по критпути:** последний self-hosted EPYC eval `29189266539` (Q-22=A, твой `51b2f40`)
УПАЛ за 25м НЕ по метрикам, а по **правам ФС раннера**: `Permission denied (os error 13)` при
записи в `/home/zzlawlzz/.cache/huggingface/xet/...` → fastembed не скачал модель → `RuntimeError`.
Раннер-юзер (`epyc-home`) не может писать в `~/.cache/huggingface` (вероятно root-owned от ручного
прогона). Фикс — твой домен (chown кэша на ноде ИЛИ job-env `HF_HOME`/`HF_HUB_CACHE` в
`${{ github.workspace }}/.hf` в eval.yml). eval.yml НЕ трогала (твой файл + Q-22-домен).
**Взяла непересекающийся [ ]-кусок T-036 §5 (UI-баннер, продолжение моей T-036-ветки §1/§2/§3):**
`/api/examples` отдаёт флаг `demo` (=`BUDGET_PROFILE=demo`, тот же `_DEMO`, что гейтит docs §3);
баннер «Public demo — EDGAR data, limited budget / Source on GitHub» (i18n EN+RU симметрично,
ссылка на репо, accent-стиль) в `Header`, рендерится ТОЛЬКО в demo, в dev/prod скрыт. **Проверено
живьём в реальном браузере** (Playwright+Chromium против vite-dev с моком `/api/examples`,
`web/e2e/demo-banner.spec.ts`, 2/2: demo=true→баннер+href+RU-ретрансляция; demo=false→скрыт;
скриншот подтверждает). `tsc`+`vite build`+eslint+prettier чисто, 314 non-slow python зелёные,
i18n-контракт зелёный. **Отклонение (честно, ²⁹):** live-флаг из `app`-контейнера не проверен —
контейнер держит старый образ (`app=build:.`, нужен ребилд; тот же отложенный путь, что §1-Grafana);
покрыто юнит-тестами + фронт-мок на оба значения. Коммит `acee537`, запушено. **T-036 остаётся `[ ]`:**
единственный остаток — §4 (живой CF-туннель, ждёт домен Q-05) + мелочи (§1-Grafana-панель). Зона свободна.

### 2026-07-12 ~19:15 · регулярная сессия — ✅ T-038 (README + финализация доков) done доковой частью → `[~]`
Старт: `git pull` (up to date), COORDINATION, БД (eval_runs до id 12 = локальные; GH: `29194112519` был ОТМЕНЁН владельцем 13:16, T-041-закрытие всё ещё ждёт зелёного EPYC-прогона — не мой домен), `git status` чистый + 0 файлов менялись <40 мин → оркестратор не активен. **Выбор T-038:** почти всё остальное инфра-блокировано (T-036 §4 ждёт домен Q-05; T-037 local ждёт EPYC-ноду; T-041 ждёт решения владельца Q-22 = домен оркестратора), а T-038 = self-contained доки, не пересекается с хотспотами. **Сделано (2 коммита `4f0ee1d`+`eb7930a`, запушено):** (1) **Двуязычный README** — старый `README.md` был RU-only и с устаревшими ограничениями (Stooq-блок цен, MOEX «в доводке») → переписан на канонический EN as-built + новый `README.ru.md` (зеркало), взаимные ссылки, mermaid-диаграмма (слои §2.1), таблица фич со скриншотами, Quick Start ≤5 команд, маркеры глубины, ссылки (демо/Grafana/2 бенчмарк-отчёта/рунбуки), лицензии SEC+MOEX, дисклеймер non-advice. (2) `CHANGELOG.md` по гейтам G1→G4. (3) **ARCHITECTURE.md синк к as-built:** сняты 3 устаревших `[OPEN]` (алерты→Telegram; цены→Alpha Vantage; РСБУ↔GAAP базовый маппинг→сделан); остался 1 осознанный `[OPEN]` (локальная CPU-модель §3.4 — завязан на T-037 CPU-бенчмарк, ждёт ноду). (4) IMPLEMENTATION_PLAN §3: ADR-8 финализирован Alpha Vantage (Stooq отвергнут). (5) рунбуки слинкованы из README. **Крит:** ✅ link-checker (все локальные ссылки живые); ✅ лицензии обоих источников; ⚠️ частично `[OPEN]` (1 осознанный остаётся); ❌ чистая-VM Quick Start и внешний ревьюер — не в этой сессии. Поэтому `[~]`, не `[done]` (сноска ³¹). Не трогала хотспоты кроме BACKLOG (закоммичен отдельно). **Оркестратору/владельцу:** после закрытия T-037 local-части (EPYC) снять последний `[OPEN]` §3.4 → T-038 закрываемо в `[done]`; GIF-анимации фич живут в T-039. Зона свободна.

### 2026-07-12 ~22:15 · регулярная сессия — ✅ T-039 (сайт-презентация) done кодом+браузер-проверкой → `[~]`
Старт: `git pull` (up to date), COORDINATION, БД (eval_runs до id 12 = локальные; GH eval `29194112519` cancelled владельцем, остальные последние — failure по инфра-правам/дедлайну, НЕ метрикам; T-041-закрытие ждёт зелёного EPYC — не мой домен), `git status` чистый + все зоны свободны → оркестратор не активен. **Выбор T-039:** почти всё инфра-блокировано (T-036 §4/T-041 — EPYC диск+Q-22, домен оркестратора; T-037 local — EPYC-нода; T-038 остаток — чистая VM+внешний ревьюер), а T-039 = self-contained статический `site/`, ноль пересечений с хотспотами. Демо `app.ledgerlens.space` проверила — сейчас **530** (CF-tunnel origin down = EPYC диск-блокер), но это не блокирует постройку сайта (ссылка проставлена как есть). **Сделано (2 коммита + push, main=9c75e27):** двуязычный (EN основной + RU-тумблер, две статические локали в app.js, без серверной логики — Q-04) одностраничник: хиро+CTA → фигура самокоррекции (2 скриншота) → **интерактивная SVG-схема архитектуры** (слои §2.1, hover-тултипы) → карточки маркеров глубины с пруф-ссылками → таблицы бенчмарков с ЖИВЫМИ числами (инференс flash/pro, pgvector vs Qdrant — из `benchmarks/*/REPORT.md`) → футер non-advice+лицензии. OG-теги, тема-адаптив, self-contained (без CDN). Деплой — `.github/workflows/site.yml` (GitHub Pages из `site/**`, Q-05). **Проверено в реальном браузере** (`python -m http.server` через preview): полная структура рендерится, тумблер EN↔RU переключает весь текст, ассеты 200 image/png, 0 console-ошибок, mobile 375px — 0 горизонтального overflow, вес 484K (≤3МБ). **Крит ✅:** опрятно моб+десктоп, вес; клеймы сверены с реальностью; деплой автоматизирован из main. **Крит ⚠️ (остаток `[~]`):** публичный URL (ждёт включения Pages владельцем + прогона); gif/видео (пока статик-скриншоты); live-демо ссылка (ждёт возврата демо на EPYC). **Скриншот-верификация не удалась** — рендерер `computer{screenshot}` таймаутил 30s×3 (среда, не страница; read_page/JS/console работали). **Владельцу:** включить GitHub Pages (Settings→Pages→Source: GitHub Actions) → workflow задеплоит сайт на `zzlawlzz.github.io/ledgerlens/`. Зона свободна.

### 2026-07-13 ~поздний вечер · оркестратор (с владельцем) — ✅ РЕПО ПУБЛИЧНЫЙ + сайт живой на `https://ledgerlens.space` (HTTPS) → закрыт остаток (а) T-039
**Для регулярной сессии, важное:** **репо `zzlawlzz/ledgerlens` теперь PUBLIC** (владелец дал «Публикуем»; секрет-скан истории перед этим ЧИСТ — `.env` не в git, ключей/токенов нет, `.gitignore` покрывает). Это разблокировало бесплатный GitHub Pages. **Сайт-презентация (T-039) задеплоен и живой на кастом-домене:**
- `https://ledgerlens.space → 200`, `http → 301 → https` (Enforce HTTPS on, Let's Encrypt cert `approved`).
- Апекс = CNAME→`zzlawlzz.github.io`, **DNS-only (серое облако)** на Cloudflare + CNAME-флаттинг → IP Pages `185.199.108-111.153`.
- Кастом-домен в Pages выставлен через `gh api PUT .../pages -f cname=ledgerlens.space` (**только поле cname**; с `https_enforced` падает до выпуска серта). `site/CNAME` закоммичен (`b6bb1c5`) но для Actions-source домен ставит именно API/Settings, не файл.
- Остаток (косметика): `www.ledgerlens.space → 525` (проксированная CF-запись) — владельцу перевести www в CNAME→`zzlawlzz.github.io` DNS-only или удалить.
- **Не путать:** `ledgerlens.space` (апекс) = статический сайт на Pages; `app.ledgerlens.space` = живое демо (CF-туннель→EPYC, T-036 §4). Разные вещи.
Тронут ТОЛЬКО `site/CNAME` (закоммичен сразу) + внешние настройки GitHub Pages/Cloudflare — код-хотспоты не трогал. Также по ходу сессии с владельцем подтверждены живьём (без правок кода): T-031 G3 (VPS-воркер через AWG к EPYC-MCP + failover), Telegram live-send. Зона свободна.

### 2026-07-14 ~ночь · оркестратор (с владельцем) — 🐞 демо на EPYC зависало → fix(router) fail-fast коннектов + пересборка демо-образов
**ВНИМАНИЕ регулярной сессии: тронут общий хотспот `model_router/router.py`** (`63d3c14`, запушено). Владелец сообщил, что публичное демо `app.ledgerlens.space` виснет на «Строим план…». Диагностика (живьё на EPYC):
- **Корень — сетевой, не баг:** DeepSeek с EPYC периодически «чёрнодырится» на TCP-коннекте (RU-DPI роняет SYN'ы; 2-5 из 8 проб = ConnectTimeout 60s, остальные 200 за ~1s). Классифай ловил 2×60s → 2 мин тишины = «виснет».
- **Общий узел — упавший Amnezia-VPN (`amn0`) на EPYC:** DeepSeek идёт по default route напрямую (мимо VPN) → RU-блок; AWG-туннель к worker-VPS маршрутизируется ЧЕРЕЗ `amn0` → **T-031-воркер недоступен** (сейчас демо на локальном in-process воркере, failover работает). На EPYC ещё висит 2ч+ `add-apt-repository ppa:amnezia` (застряла переустановка) — **домен владельца.**
- Оба egress-пути с EPYC (прямой eno2 И через amn0/LV) сейчас флаки ~50% → «чистого» пути нет, поэтому чинил не поиском пути, а устойчивостью.
**Фикс `router.py` (безопасен для dev/CI — no-op при здоровой сети):** LLM-httpx-клиент получил короткий `connect=8s` (read остаётся щедрым для thinking-тиров) + keep-alive пул (`keepalive_expiry=120s` — тёплое соединение переживает ран, следующие вызовы без коннекта) + ретраи `2→4`. `trust_env` сохранён (HTTPS_PROXY по-прежнему сработает, если появится прокси). Тест `test_router` обновлён на константу `TIER_RETRY_ATTEMPTS`. **Живая проверка на демо:** ран дошёл за 59с, ответ верный ($416.161B), 0 ошибок; латентности: холодные route/reason ~19-20s (fail-fast+ретрай через блокировки), тёплые reason/extract/guard 1.4-2s. **Деплой:** `docker cp`+restart в оба контейнера (работают сейчас) + **пересобраны образы `lldemo-app`/`lldemo-worker`** на EPYC (`--frozen`, фикс запечён → переживёт пересоздание) + патч положен в `~/ledgerlens-demo`. Диск EPYC свободен (77G). Не трогал eval/пороги/graph/prompts. Зона свободна.

### 2026-07-14 ~+2ч · регулярная сессия — ✅ T-036 §3/§4/§5 ЖИВАЯ верификация над публичным демо → `[~]`
Старт по протоколу: `git pull` (up to date), COORDINATION, БД (`eval_runs` max id=12, без изменений — раннер `epyc-home` всё ещё offline, eval-прогресс невозможен), `git log` (HEAD=`127f55b`, оркестратор коммитил 26 мин назад — router-фикс демо, но tree ЧИСТЫЙ, незакоммиченного нет). **Ключевое расхождение памяти vs реальность:** демо `app.ledgerlens.space` в памяти помечено как 530 (down), но после router-фикса оркестратора (`63d3c14`/`127f55b`) оно **снова LIVE** — это разблокировало отложенные live-критерии T-036 §3/§4/§5, которые раньше ждали живого демо. Оркестратор владеет демо-зоной → взяла ТОЛЬКО read-only проверки, код/хотспоты не трогала.
**Проверено живьём против публичного демо (read-only, ~0 API-бюджета — без запуска ранов):**
- **§5 (снят отложенный live-флаг ²⁹):** `/api/examples` из РЕАЛЬНОГО пересобранного app-контейнера отдаёт `{"mode":"us","demo":true}` (раньше отложено — «нужен ребилд образа», оркестратор пересобрал `lldemo-app`). Баннер рендерится в живом браузере (read_page): «Публичное демо — данные EDGAR, бюджет ограничен.» + ссылка «Исходный код на GitHub» → `github.com/zzlawlzz/ledgerlens`.
- **§3 (снят отложенный live-крит):** security-заголовки эмитятся над публичным TLS — `Content-Security-Policy` (same-origin, `connect-src 'self'`), `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy`. Docs закрыты В ДВУХ СЛОЯХ: app-уровень (`docs_url/redoc_url/openapi_url=None` при `_DEMO`, `api.py:160-162`) + nginx (SPA-fallback: `/docs`,`/redoc`,`/openapi.json` → index.html, а НЕ FastAPI-схема; проверила тело — `<!doctype html>…LedgerLens`).
- **§4:** демо доступно по публичному URL с TLS: `/healthz` → `{"status":"ok","mode":"us"}` (200 через CF-туннель). Полный ран end-to-end уже подтверждён оркестратором (59с, $416.161B).
**Итог:** T-036 `[ ]`→`[~]` (сноска ²⁹ дополнена). §1-код/§2/§3/§4/§5 = done+live; **единственный остаток — §1 живая ВИЗУАЛЬНАЯ проверка Grafana-панелей demo-отказов** (Grafana не публична → домен оркестратора/владельца на demo-ноде). По её закрытию T-036 → `[done]`. Правки: BACKLOG-сноска + этот файл + память. НЕ трогала api.py/router.py/compose/prompts/graph/eval/web. Зона свободна.

**⚠️ КОРРЕКЦИЯ (владелец указал на ложь про раннер — я сверилась с `gh api`):** раннер `epyc-home` **ONLINE** (`gh api .../actions/runners` → total 1, status=online, busy=false, labels `[self-hosted,Linux,X64,ledgerlens-epyc]`). Утверждение «offline» в моём же напоминании выше И в прежних журнальных записях — **УСТАРЕЛО/НЕВЕРНО**. Реальная картина по последнему eval `29277164289` (2026-07-13 19:06Z, 43м, **ДО** router-фикса `63d3c14`): раннер отработал, Postgres/Qdrant healthy, снапшот восстановлен, прогон дошёл до метрик. **Метрики: numeric 0.923, citation 0.909, faithfulness 0.99 (здоровы!), НО guardrail_block=0.0 и nodata_honesty=0.0** — все 10 guardrail+nodata кейсов упали ровно за 2.0s = паттерн быстрого СЕТЕВОГО фейла (DeepSeek-egress с EPYC блэкхолился RU-DPI; numeric-кейсы, где ответ из SQL, шли 13–25s и прошли). Это ровно тот egress-дефект, что оркестратор чинил в `router.py` (fail-fast connect=8s) **ПОСЛЕ** этого прогона. Плюс **workflow-дефект: `could not persist eval run to DB (Connect … 5432)`** — Postgres снесён `compose down` ДО шага персиста → строки в `eval_runs` нет (потому max id=12). **Вывод: citation-гейт (Q-22) НЕ заблокирован раннером** — нужен свежий full-eval на текущем HEAD (router-фикс уже в main) чтобы увидеть, восстановились ли guardrail/nodata; и починить порядок persist-vs-teardown в eval.yml (домен оркестратора). Триггерю прогон (см. журнал ниже).

### 2026-07-14 ~ночь · оркестратор — ✅ T-031 ВОССТАНОВЛЕН живьём (AWG-меш переподнят)
Продолжение egress-фикса: убил застрявший `add-apt-repository ppa:amnezia` на EPYC; **меш `awgll` (AmneziaWG к worker-VPS) переподнят чистым `awg-quick down/up`** (`systemctl restart awg-ll.service` оставлял 0 B received — интерфейс подвисал; ручной down/up дал handshake). **Урок:** ICMP через userspace-AWG не ходит (red herring, README про это пишет) — судить по TCP, не по ping. **Проверено:** A2A worker-VPS `10.9.0.2:8081` agent-card=200 (`vps-fi`); боевой многошаговый ран на демо → **3 шага на vps-fi, run_finished, 0 ошибок/failover, 103с.** Распределённость снова живая; шаги на vps-fi идут через чистый KZ-egress (обходят RU-блок). `amn0` (интернет-VPN) не лечил (plain-WG, конфиг в шифр. сторе клиента) — T-031 в нём не нуждается. Оговорка: `awgll` поверх флаки-amn0, при провале аплинка → `awg-quick down/up`. Только инфра-действия на ноде (systemctl/awg-quick/ip route откачен) — код/репо не трогал. Зона свободна.

### 2026-07-14 ~ночь · оркестратор — demo egress через прокси worker-VPS (стабильный прогресс) + оверлей `compose.llmproxy.yml`
Владелец: демо снова «висит на Строим план несколько минут». Диагностика: бэкенд жив (admission ок, слоты не текут), но прямой DeepSeek-egress с EPYC эрратичен (флаки RU-аплинк) → холодные провалы 60с+ = UI выглядит замороженным. **Решение:** поставил `tinyproxy` на worker-VPS (`10.9.0.2:8888`, Listen на туннельном IP, Allow 10.9.0.1) — у VPS чистый KZ-egress; новый **опциональный оверлей `deploy/demo/compose.llmproxy.yml`** (`b3c0a97`) прописывает `HTTPS_PROXY=http://10.9.0.2:8888` в app+worker (только HTTPS=DeepSeek; внутр. HTTP/MCP/A2A/БД не тронуты, NO_PROXY). Демо пересоздано С этим оверлеем (добавь `-f deploy/demo/compose.llmproxy.yml` в up-команду). **Живьё:** multi-step ран — 0 таймаутов/чёрных дыр, все LLM 2-8с, фаза «Строим план» ~28с (было минуты), верные числа. **Честно: не быстрее (108с, упор в сеть), но КОНСИСТЕНТНО + видимый прогресс** (жалоба была про «не понятно, идёт ли»). **Trade-off:** egress теперь зависит от туннеля (флаки-amn0) — если туннель ляжет, LLM-вызовы демо падают (router fail-fast их не спасёт, прокси недостижим); откат = убрать `-f compose.llmproxy.yml` + recreate → прямой egress (router-фикс, всегда работает но эрратично). Диск/раннер не трогал; код только `router.py` (раньше, `63d3c14`). Зона свободна.

### 2026-07-14 ~ночь · оркестратор — 🏗 демо-фронт вынесен с туннеля на GitHub Pages (split-origin), CORS на API
**Причина:** CF-туннель EPYC режет БОЛЬШИЕ ответы (372КБ JS-бандл стопорится на ~24КБ из-за деградировавшего RU-egress → http2 flow-control не открывается → белая страница). Мелкие JSON/SSE проходят. Origin-фиксы (bust ассетов, gzip off, рестарт cloudflared) НЕ помогли — это канал.
**Решение (коммиты `04dd5ea`+`f3e8f5f`, запушено):** статик-фронт раздаётся с **надёжного Pages-edge на `ledgerlens.space/app/`**, а API остаётся на `app.ledgerlens.space` (туннель, мелкие ответы) — cross-origin через CORS.
- **web:** конфигурируемые `VITE_API_BASE_URL` + `VITE_BASE` (по умолчанию пусто/`/` → локал-дев и single-origin nginx-билд НЕ тронуты; обратно совместимо). `agent.ts`/examples зовут `${API_BASE}/...`.
- **api.py:** opt-in CORS-allowlist (`CORS_ALLOW_ORIGINS`, deny-by-default). Настройка `cors_allow_origins` в `common/config.py`.
- **site.yml:** билдит `web/` (VITE_BASE=/app/, API=app.ledgerlens.space) в `site/app/` → Pages. Триггер расширен на `web/**`.
**Задеплоено:** Pages-фронт задеплоен (site.yml success). На EPYC: `CORS_ALLOW_ORIGINS=https://ledgerlens.space` в `~/ledgerlens-demo/.env`, **образ `lldemo-app` пересобран с CORS-кодом + app recreated**. Живьё: `ledgerlens.space/app/`=200 полный бандл; `/api/examples` и preflight `/agui` через CF = 200 + `Access-Control-Allow-Origin: https://ledgerlens.space`. Demo-ссылки (site/README EN+RU) → `ledgerlens.space/app/`.
**Регулярной сессии ВАЖНО:** (1) новый demo-URL = **`ledgerlens.space/app/`**; `app.ledgerlens.space` теперь = API-эндпоинт (его корневой фронт = старый обрезанный, НЕ использовать). (2) CORS-переменная в `.env` — при передеплое сохраняй. (3) Туннель по-прежнему флапает (i/o timeout к CF-edge:7844) → API временами 530, но фронт грузится всегда + деградирует мягко. (4) После recreate app бывает нужен `docker restart lldemo-web-1` (nginx перерезолвит app-IP). Зона свободна.

### (регулярная сессия — впиши сюда свою следующую сводку)
