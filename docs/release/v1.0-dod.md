# LedgerLens v1.0 — трекер готовности к релизу (Definition of Done, гейт G4)

> **Статус документа:** живой черновик (T-040, критерий 2). Консолидирует
> доказательства по 11 пунктам Definition of Done (IMPLEMENTATION_PLAN §7) и
> оставшиеся до гейта G4 блокеры. **Это evidence-каталог, а НЕ заявление о
> прохождении G4** — гейт закрывается владельцем/оркестратором после живых
> проверок (см. «Оставшиеся до G4 блокеры» внизу).
>
> Обновлено: 2026-07-13 (регулярная сессия). Тест-база на момент составления:
> `uv run pytest -m "not slow"` → **333 passed** (HEAD `53e7213`).

Легенда статусов:
- ✅ **done** — реализовано и проверено (код + тесты + при наличии живая проверка).
- 🟡 **code-ready** — код и юнит-доказательства готовы; недостаёт живой
  проверки/публичного URL (упирается в железо/сеть/владельца — см. блокер).
- ⛔ **blocked** — ждёт внешнего действия (владелец/инфраструктура).

---

## Чек-лист DoD §7 (11 пунктов)

### 1. `docker compose up` на чистой машине → рабочая система — 🟡 code-ready
- **Доказательства:** `docker-compose.yml` (8 сервисов ядра + `ollama` в профиле
  `local`); `Makefile` цели `up`/`demo`/`smoke`; **гейт G1** пройден
  (BACKLOG⁴: чистый volume → авто-миграции → demo-ingest 5 тикеров → smoke 3/3);
  **гейт G2** (T-026, полный MVP).
- **Остаток:** дословный прогон Quick Start на чистой VM с фиксацией таймингов
  ещё не выполнен (T-040 §1, критерий «≤ 30 мин от clone до UI»).

### 2. Публичный demo-режим (US/EDGAR) играбелен незнакомцем по ссылке — 🟡 code-ready
- **Доказательства:** `deploy/demo/` (`docker-compose.demo.yml`,
  `compose.epyc.yml`, `cloudflared`, `SECURITY.md` — 8/8 пунктов security-pass);
  demo-лимиты допуска `orchestrator/demo_limits.py` (rate/cost-cap/question-len/
  concurrency, T-036 §1); наблюдаемость отказов (таблица `demo_rejections`,
  миграция `004`, 2 панели, T-036 §1); баннер «публичное демо» в UI (T-036 §5);
  SSE-heartbeat для CF-прокси (T-036 §4). Демо **поднималось живьём** оркестратором
  на EPYC (`https://app.ledgerlens.space`, `269c49f`: UI 200 + SQL + RAG 8 цитат).
- **Остаток (⛔):** устойчивый публичный URL — упирается в диск EPYC (демо + eval
  не помещаются вместе) и CF-домен (**Q-05**); живой end-to-end SSE через реальный
  туннель (§4).

### 3. Многошаговый агентский анализ (план, шаги, ≥1 самокоррекция) — ✅ done
- **Доказательства:** оркестратор Plan-and-Execute `orchestrator/graph.py`
  (T-020); наблюдаемый сценарий самокоррекции `demo/self_correction.md` +
  `demo/screenshots/` (T-025); стрим плана/шагов в UI (T-024).

### 4. RAG с цитатами на первоисточник + guardrail против инвестрекомендаций — ✅ done
- **Доказательства:** `tools/rag/` — hybrid search + rerank + цитаты sec.gov
  (T-019); двухстадийный guardrail `orchestrator/guardrail.py` (regex RU+EN +
  LLM-классификатор, ре-синтез → template-refusal, дисклеймер, T-022).

### 5. Eval в CI + eval-скоры на дашборде — ✅ done (с нюансом гейта)
- **Доказательства:** `.github/workflows/eval.yml` (+`eval-snapshot.yml`);
  дашборд `observability/dashboards/quality.json`; исторические прогоны в таблице
  `eval_runs` (id 7–12: faithfulness 0.93–0.99, citation 0.92–1.0 на здоровых
  локальных full).
- **Нюанс (⛔ Q-22):** citation-гейт (порог 1.0) на бесплатном GH-раннере упирается
  в CPU-потолок (реранкер не успевает на 2-vCPU → провалы цитат) — переведён на
  self-hosted EPYC-раннер, ждёт зелёного прогона (диск EPYC / решение владельца).
  faithfulness-цель T-041 достигнута (full 0.7625 ≥ 0.7 с фиксом воркера).

### 6. Распределённость: воркер на удалённой ноде через A2A — 🟡 code-ready
- **Доказательства:** `deploy/worker-node/` (`compose.worker.yml`, runbook
  `README.md`, AmneziaWG-меш); диспетчер `orchestrator/graph.py` — round-robin +
  local-preferred failover (T-031 `d9ec7a9`); валидатор `assert_worker_url_secure`
  (не-localhost → https/приватный, 10 тестов).
- **Остаток (⛔):** живой VPS-деплой (`104.238.24.196`, Q-18) + AmneziaWG на
  домашней ноде + живые критерии G3 — единственный оставшийся блокер гейта G3
  (сетевые операции, домен владельца/оркестратора).

### 7. Инструменты как MCP-серверы; стрим в UI через AG-UI — ✅ done
- **Доказательства:** MCP-серверы `tools/{sql,rag,enrich}/server.py` + MCP-клиенты
  (T-027); AG-UI эндпоинт `orchestrator/agui.py` (T-023); web UI на AG-UI-стриме
  (T-024).

### 8. Наблюдаемость в Grafana (латентность, стоимость, качество) — ✅ done
- **Доказательства:** `observability/dashboards/` — `operations.json`,
  `quality.json`, `session.json` (T-034); Postgres-датасорс `grafana_ro` (SELECT,
  граница роли).

### 9. Бенчмарки (локальные CPU vs API; Qdrant vs pgvector; GPU исключён Q-07) — 🟡 частично
- **Доказательства:** `benchmarks/vector/REPORT.md` (§2, `a6feded` — pgvector vs
  Qdrant на реальном корпусе); `benchmarks/inference/REPORT.md` (§1 API-часть,
  `9549315` — живые deepseek-числа: flash TTFT 0.74s/$0.02·1k/judge 4.80, pro
  2.75s/$0.23·1k/judge 5.00); раздел об исключении GPU (Q-07) присутствует;
  `Makefile` `bench-vector`/`bench-inference`.
- **Остаток (⛔):** local-CPU-часть инференса (2–3 ollama-кандидата на EPYC 2×7551)
  + финализация ADR-3 (выбор локальной модели) — ждёт домашнюю ноду (сейчас занята
  демо + eval-раннером). Раннер готов: `make bench-inference BENCH_ARGS='--local-models …'`.

### 10. README + сайт-презентация; лицензии источников упомянуты — 🟡 code-ready
- **Доказательства:** двуязычный README (`README.md` EN канонический +
  `README.ru.md`, `eb7930a`); `CHANGELOG.md` по гейтам; статический сайт `site/`
  (EN+RU-тумблер, интерактивная SVG-архитектура, таблицы бенчмарков, `9c75e27`);
  `.github/workflows/site.yml` (GitHub Pages); `LICENSE`; лицензии данных (SEC fair
  use + User-Agent; MOEX ISS «только ознакомление») в README.
- **Остаток (⛔):** публичный Pages-URL (владельцу включить Settings→Pages→GitHub
  Actions); 60–90-сек gif/видео самокоррекции (пока статические скриншоты);
  живая ссылка на демо (`app.ledgerlens.space` = §4-блокер).

### 11. Pluggable-архитектура: второй источник за интерфейсом — ✅ done
- **Доказательства:** интерфейс адаптера `adapters/base.py` (T-007); RU-адаптер
  MOEX ISS `adapters/moex/` (T-032 `0edd241` — ingest SBER/GAZP/LKOH, живьё:
  динамика SBER, RU-дисклеймер `iss_attribution`); дополнительные RU-scaffolds
  `adapters/edisclosure/`, `adapters/girbo/`. Pluggable-чистота проверена
  grep-тестом (гейт G1).

---

## Сводка статусов

| # | Пункт DoD §7 | Статус |
|---|---|---|
| 1 | compose up на чистой машине | 🟡 code-ready (чистая-VM протокол) |
| 2 | публичное demo играбелно | 🟡 code-ready (⛔ EPYC-диск + Q-05) |
| 3 | многошаговый агент + самокоррекция | ✅ done |
| 4 | RAG-цитаты + guardrail | ✅ done |
| 5 | eval в CI + дашборд | ✅ done (⛔ citation-гейт Q-22) |
| 6 | распределённый воркер A2A | 🟡 code-ready (⛔ живой VPS/G3) |
| 7 | MCP-серверы + AG-UI | ✅ done |
| 8 | Grafana-наблюдаемость | ✅ done |
| 9 | бенчмарки | 🟡 частично (⛔ CPU-часть/EPYC + ADR-3) |
| 10 | README + сайт + лицензии | 🟡 code-ready (⛔ публичные URL) |
| 11 | pluggable второй источник | ✅ done |

**Итог:** 6 из 11 пунктов ✅ done; 5 — code-ready, каждый упирается в живую
проверку за пределами dev-машины.

---

## Оставшиеся до гейта G4 блокеры (сгруппировано)

**A. Железо / диск EPYC (домашняя нода 2×EPYC 7551):**
- Устойчивый публичный URL демо (§2/§4) — демо и self-hosted eval не помещаются
  вместе (~8 ГБ свободно; владельцу освободить диск).
- Local-CPU бенчмарк инференса + ADR-3 (§9).
- Зелёный self-hosted eval-прогон для закрытия citation-гейта (§5, Q-22).

**B. Сеть / VPS (за владельцем/оркестратором):**
- Живой A2A-деплой воркера на `104.238.24.196` + AmneziaWG → закрытие **гейта G3**
  (§6). Пока G3 не закрыт, G4 формально начинать нельзя (порядок гейтов).
- Включение GitHub Pages для публичного URL сайта (§10).
- Реальный Telegram-send из мониторинг-конвейера (T-035 crit — разрешение
  владельца; канал уже доказан `312b5ae`).

**C. Решения владельца (OWNER_QUESTIONS):**
- **Q-05** — CF-домен для публичного демо/сайта.
- **Q-22** — политика citation-гейта (self-hosted раннер vs non_blocking).
- Оба со статусом «блокирует» → критерий G4 «ноль блокирующих вопросов» пока НЕ
  выполнен.

**D. Не-прогнанные локальные протоколы (T-040 собственные пункты):**
- §1 — Quick Start на чистой VM с таймингами (≤ 30 мин).
- §3 — финальный эталонный full-eval → запись в `eval_runs` на Quality-дашборде.
- §4 — тег `v1.0.0` + GitHub Release с артефактами; защита ветки main (CI required).
- §5 — санитарная проверка публичного контура (демо/лимиты/Grafana/сайт).
- §6 — раздел Known Limitations в README (в т.ч. судьба RU-адаптера, Q-02).
- Внешний читатель / свежий-LLM-ревью README (T-038 крит.1) — сделан только
  self-review + fresh-reader accuracy-pass (`4099447`).

**E. Внешние дедлайны / зависимости (не блокеры, но на радар владельцу):**
- **DeepSeek: алиасы `deepseek-chat` / `deepseek-reasoner` удаляются 2026-07-24**
  (см. `docs/research/adr-notes.md:52`). **Статус: ✅ де-рискнуто.** Конфиг уже
  запинен на пост-алиасные ID `deepseek-v4-flash` (cloud_cheap) / `deepseek-v4-pro`
  (cloud_strong) (`config/router.yaml`, комментарий-предупреждение на месте);
  repo-wide grep не находит ни одной живой ссылки на старые ID (тесты/дефолты/
  деплой чисты). **Живая проверка 2026-07-13 (регулярная сессия):** оба тира
  прогнаны через реальную обвязку `RouterClient` против api.deepseek.com —
  `deepseek-v4-flash` (thinking=disabled) и `deepseek-v4-pro` (thinking=enabled,
  28 reasoning-токенов эмитятся корректно) отвечают `OK` (usage 12 / 41 токенов,
  ≪$0.01). Т.е. смена алиасов 2026-07-24 не сломает роутинг. **Остаётся владельцу:**
  просто держать на радаре (если DeepSeek изменит поведение/цены v4-pro thinking
  до релиза — пересверить бенч T-037 §1); действий в коде сейчас не требуется.

---

## Как поддерживать этот документ

Обновлять по мере снятия блокеров: менять статус пункта, добавлять указатель на
живое доказательство (commit/скриншот/запись прогона). Когда все 11 пунктов ✅ и
блокеры A–D закрыты — переносить содержимое в финальный `docs/release/v1.0-dod.md`
как эталонную запись гейта G4 и публиковать Release `v1.0.0`.
