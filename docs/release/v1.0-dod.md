# LedgerLens v1.0 — трекер готовности к релизу (Definition of Done, гейт G4)

> **Статус документа:** живой черновик (T-040, критерий 2). Консолидирует
> доказательства по 11 пунктам Definition of Done (IMPLEMENTATION_PLAN §7) и
> оставшиеся до гейта G4 блокеры. **Это evidence-каталог, а НЕ заявление о
> прохождении G4** — гейт закрывается владельцем/оркестратором после живых
> проверок (см. «Оставшиеся до G4 блокеры» внизу).
>
> Обновлено: 2026-07-14 (регулярная сессия; **гейт G3 закрыт живьём** →
> §6 ✅; **сайт live на HTTPS** → §10; **демо live** `app.ledgerlens.space`
> HTTP 200 → §2 почти done; **CI восстановлена** после 40+ красных прогонов
> [`042391e`]; ✅ **раннер `epyc-home` online + первый ПОЛНОСТЬЮ чистый
> self-hosted full-eval `29297896263` ЗЕЛЁНЫЙ** (после MCP host-port-фикса
> `198bdf9`): citation_coverage=**1.0**, faithfulness=**0.985**, guardrail 1.0,
> nodata 1.0, numeric 0.923, 39/41 pass, gate exit 0 «no blocking thresholds
> violated»; **подтверждающий 2-й full-eval `29303631947` тоже ЗЕЛЁНЫЙ**
> (citation 1.0 / faithfulness 1.0) → **T-041 ЗАКРЫТ**, faithfulness промотирован
> в blocking, Q-16/Q-22 закрыты).
> Прошлая правка: 2026-07-13.
> Тест-база на момент составления: `uv run pytest -m "not slow"` → **333 passed**
> (данная правка docs-only, тесты не затронуты).

Легенда статусов:
- ✅ **done** — реализовано и проверено (код + тесты + при наличии живая проверка).
- 🟡 **code-ready** — код и юнит-доказательства готовы; недостаёт живой
  проверки/публичного URL (упирается в железо/сеть/владельца — см. блокер).
- ⛔ **blocked** — ждёт внешнего действия (владелец/инфраструктура).

---

## Чек-лист DoD §7 (11 пунктов)

### 1. `docker compose up` на чистой машине → рабочая система — 🟡 code-ready
- **Доказательства:** `docker-compose.yml` (8 сервисов ядра + `ollama` в профиле
  `local`); `Makefile` цели `up`/`demo`/`smoke`; **гейт G1** пройден
  (BACKLOG⁴: чистый volume → авто-миграции → demo-ingest 5 тикеров → smoke 3/3);
  **гейт G2** (T-026, полный MVP).
- **Остаток:** дословный прогон Quick Start на чистой VM с фиксацией таймингов
  ещё не выполнен (T-040 §1, критерий «≤ 30 мин от clone до UI»).

### 2. Публичный demo-режим (US/EDGAR) играбелен незнакомцем по ссылке — 🟡 почти done (URL live)
- **Доказательства:** `deploy/demo/` (`docker-compose.demo.yml`,
  `compose.epyc.yml`, `cloudflared`, `SECURITY.md` — 8/8 пунктов security-pass);
  demo-лимиты допуска `orchestrator/demo_limits.py` (rate/cost-cap/question-len/
  concurrency, T-036 §1); наблюдаемость отказов (таблица `demo_rejections`,
  миграция `004`, 2 панели, T-036 §1); баннер «публичное демо» в UI (T-036 §5);
  SSE-heartbeat для CF-прокси (T-036 §4). **Демо live на публичном HTTPS-URL —
  проверено 2026-07-14:** `https://app.ledgerlens.space/` → HTTP 200,
  `/healthz` → `{"status":"ok","mode":"us"}` (диск EPYC освобождён владельцем,
  CF-туннель поднят). CF-домен = Q-05 (решён).
- **✅ Acceptance-прогон незнакомцем ВЫПОЛНЕН 2026-07-14** (регулярная сессия, live
  через публичный HTTPS): анонимный `POST /api/chat` `{"question":"What was Apple's
  revenue in fiscal year 2025?","mode":"us"}` → полный SSE-цикл `run_started` →
  route (`deepseek-v4-flash`) → `plan_created` (simple) → worker → `sql_query`
  (AAPL revenue = 416 161 000 000 USD, period_end 2025-09-27) → synthesize
  (`deepseek-v4-pro`) → guardrail `pass` → `run_finished status=succeeded`; ответ
  «$416,161,000,000 (USD)» + дисклеймер «This is financial analytics, not
  investment advice.»; `cost_usd=0.001517`, latency 38.2s (run_id
  `d73056f9-2f65-4cce-957c-f03bc3bce1a0`). **§4 SSE-heartbeat подтверждён на
  проводе через реальный CF-туннель** — в стриме дважды пришёл комментарий
  `: keep-alive` (в т.ч. на 15-сек synthesize-шаге). Побочно: оба cloud-тира
  (`deepseek-v4-flash`/`deepseek-v4-pro`) ответили вживую → dealias-конфиг
  (блок E) подтверждён на публичном контуре.
- **Остаток:** только устойчивый end-to-end SSE-heartbeat на шаге, ПРЕВЫШАЮЩЕМ
  CF-idle-timeout (наблюдённый keep-alive на 15-сек шаге демонстрирует механизм,
  но не 100-сек+ кейс). **Нюанс:** демо и self-hosted eval на одной ноде не
  уживаются устойчиво (см. §5/§3) — при eval-прогонах демо-стек трогать нельзя.

### 3. Многошаговый агентский анализ (план, шаги, ≥1 самокоррекция) — ✅ done
- **Доказательства:** оркестратор Plan-and-Execute `orchestrator/graph.py`
  (T-020); наблюдаемый сценарий самокоррекции `demo/self_correction.md` +
  `demo/screenshots/` (T-025); стрим плана/шагов в UI (T-024).

### 4. RAG с цитатами на первоисточник + guardrail против инвестрекомендаций — ✅ done
- **Доказательства:** `tools/rag/` — hybrid search + rerank + цитаты sec.gov
  (T-019); двухстадийный guardrail `orchestrator/guardrail.py` (regex RU+EN +
  LLM-классификатор, ре-синтез → template-refusal, дисклеймер, T-022).

### 5. Eval в CI + eval-скоры на дашборде — ✅ done (с нюансом гейта)
- **Доказательства:** `.github/workflows/eval.yml` (+`eval-snapshot.yml`);
  дашборд `observability/dashboards/quality.json`; исторические прогоны в таблице
  `eval_runs` (id 7–12: faithfulness 0.93–0.99, citation 0.92–1.0 на здоровых
  локальных full).
- **✅ Q-22 ЗАКРЫТ, citation-гейт зелёный (2026-07-14):** citation-гейт (порог
  1.0) на бесплатном GH-раннере упирался в CPU-потолок (реранкер не успевал на
  2-vCPU → провалы цитат). Q-22=вариант-1 (self-hosted EPYC-раннер) реализован и
  **доказан ДВУМЯ подряд чистыми full-прогонами: `29297896263`
  (citation 1.0 / faithfulness 0.985) и `29303631947` (citation 1.0 /
  faithfulness 1.0)**, оба 41 кейс, gate exit 0. Citation-«потолок» был
  артефактом голодания бесплатного раннера, а не кода. **T-041 закрыт**
  (`[done]`): `faithfulness` промотирован из `non_blocking` в жёсткий гейт
  (`config/eval-thresholds.yaml`, порог 0.7 не занижался), Q-16 закрыт.

### 6. Распределённость: воркер на удалённой ноде через A2A — ✅ done (гейт G3 закрыт)
- **Доказательства:** `deploy/worker-node/` (`compose.worker.yml`, runbook
  `README.md`, AmneziaWG-меш); диспетчер `orchestrator/graph.py` — round-robin +
  local-preferred failover (T-031 `d9ec7a9`); валидатор `assert_worker_url_secure`
  (не-localhost → https/приватный, 10 тестов). **Живой VPS-деплой выполнен и все
  критерии G3 проверены живьём 2026-07-13** (T-031 `[done]` `422d3bb`/`d783e0d`,
  **G3 ✅**): (1) шаг «Compare Apple vs NVIDIA FY2025 revenue» отработал на
  `worker_node=vps-fi` (воркер на VPS `104.238.24.196` дотянулся до MCP EPYC через
  AmneziaWG-туннель, вернул верный NVIDIA $130.497B); (2) failover — стоп
  VPS-воркера → тот же вопрос успешно локально + trace `worker_failover`/
  `degradation=worker_unreachable`; (3) security — публичный `:8081` → 000
  (воркер слушает только AWG-IP), A2A-эндпоинт → 401 без токена. Меш: AmneziaWG
  userspace (`awg-quick`+`amneziawg-go`), `10.9.0.0/24`, systemd `awg-ll.service`
  enabled на обеих нодах (durable). As-built runbook `deploy/worker-node/README.md`.
- **Остаток:** нет (гейт G3 закрыт).

### 7. Инструменты как MCP-серверы; стрим в UI через AG-UI — ✅ done
- **Доказательства:** MCP-серверы `tools/{sql,rag,enrich}/server.py` + MCP-клиенты
  (T-027); AG-UI эндпоинт `orchestrator/agui.py` (T-023); web UI на AG-UI-стриме
  (T-024).

### 8. Наблюдаемость в Grafana (латентность, стоимость, качество) — ✅ done
- **Доказательства:** `observability/dashboards/` — `operations.json`,
  `quality.json`, `session.json` (T-034); Postgres-датасорс `grafana_ro` (SELECT,
  граница роли).

### 9. Бенчмарки (локальные CPU vs API; Qdrant vs pgvector; GPU исключён Q-07) — 🟡 частично
- **Доказательства:** `benchmarks/vector/REPORT.md` (§2, `a6feded` — pgvector vs
  Qdrant на реальном корпусе); `benchmarks/inference/REPORT.md` (§1 API-часть,
  `9549315` — живые deepseek-числа: flash TTFT 0.74s/$0.02·1k/judge 4.80, pro
  2.75s/$0.23·1k/judge 5.00); раздел об исключении GPU (Q-07) присутствует;
  `Makefile` `bench-vector`/`bench-inference`.
- **Остаток (⛔):** local-CPU-часть инференса (2–3 ollama-кандидата на EPYC 2×7551)
  + финализация ADR-3 (выбор локальной модели) — ждёт домашнюю ноду (сейчас занята
  демо + eval-раннером). Раннер готов: `make bench-inference BENCH_ARGS='--local-models …'`.

### 10. README + сайт-презентация; лицензии источников упомянуты — 🟡 code-ready
- **Доказательства:** двуязычный README (`README.md` EN канонический +
  `README.ru.md`, `eb7930a`); `CHANGELOG.md` по гейтам; статический сайт `site/`
  (EN+RU-тумблер, интерактивная SVG-архитектура, таблицы бенчмарков, `9c75e27`);
  `.github/workflows/site.yml` (GitHub Pages); `LICENSE`; лицензии данных (SEC fair
  use + User-Agent; MOEX ISS «только ознакомление») в README. **Сайт live на
  публичном HTTPS-URL 2026-07-13** (T-039 (a) `[done]` `5a40cd9`): GitHub Pages
  включён, деплой-workflow зелёный (`29282878530`), custom-домен
  **https://ledgerlens.space** (CNAME `b6bb1c5`) — Pages API: `cname` verified,
  `https_certificate: approved`, `https_enforced: true` (проверено живьё
  2026-07-14: сайт отдаёт полный контент — хиро, How it works, Architecture,
  Depth, Benchmarks, Run it yourself). Репозиторий публичный.
- **Остаток (⛔):** 60–90-сек gif/видео самокоррекции (пока статические скриншоты,
  T-039 (b)); живая ссылка на демо со страницы (`app.ledgerlens.space` = §2/§4
  EPYC-диск-блокер).

### 11. Pluggable-архитектура: второй источник за интерфейсом — ✅ done
- **Доказательства:** интерфейс адаптера `adapters/base.py` (T-007); RU-адаптер
  MOEX ISS `adapters/moex/` (T-032 `0edd241` — ingest SBER/GAZP/LKOH, живьё:
  динамика SBER, RU-дисклеймер `iss_attribution`); дополнительные RU-scaffolds
  `adapters/edisclosure/`, `adapters/girbo/`. Pluggable-чистота проверена
  grep-тестом (гейт G1).

---

## Сводка статусов

| # | Пункт DoD §7 | Статус |
|---|---|---|
| 1 | compose up на чистой машине | 🟡 code-ready (чистая-VM протокол) |
| 2 | публичное demo играбелно | 🟡 почти done (URL live ✅; acceptance-прогон незнакомцем ✅ 2026-07-14; остаток только SSE на >CF-idle шаге) |
| 3 | многошаговый агент + самокоррекция | ✅ done |
| 4 | RAG-цитаты + guardrail | ✅ done |
| 5 | eval в CI + дашборд | ✅ done (T-041 закрыт: 2× чистых full-eval на EPYC зелёные, citation 1.0 / faith 0.985–1.0, faithfulness промотирован в blocking) |
| 6 | распределённый воркер A2A | ✅ done (гейт G3 закрыт живьём) |
| 7 | MCP-серверы + AG-UI | ✅ done |
| 8 | Grafana-наблюдаемость | ✅ done |
| 9 | бенчмарки | 🟡 частично (⛔ CPU-часть/EPYC + ADR-3) |
| 10 | README + сайт + лицензии | 🟡 code-ready (сайт live ✅; ⛔ gif + ссылка на демо) |
| 11 | pluggable второй источник | ✅ done |

**Итог:** 7 из 11 пунктов ✅ done; 3 — code-ready (§1 чистая-VM, §2 публичное
демо, §9 CPU-бенчмарк); §10 — сайт live, остаток только gif/демо-ссылка. Все
оставшиеся упираются в живую проверку за пределами dev-машины (диск EPYC / Q-05).

---

## Оставшиеся до гейта G4 блокеры (сгруппировано)

**A. Железо / диск EPYC (домашняя нода 2×EPYC 7551):**
- ~~Устойчивый публичный URL демо (§2) — демо и self-hosted eval не помещаются
  вместе.~~ ✅ **диск освобождён владельцем, демо live** (`app.ledgerlens.space`
  HTTP 200 + healthz ok, проверено 2026-07-14). ✅ **acceptance-прогон незнакомцем
  выполнен 2026-07-14** (SQL Q→A live через `/api/chat`, run_id `d73056f9…`, ответ
  верный, guardrail pass, cost $0.0015 — см. §2 выше); остаток §2 — только SSE на
  шаге >CF-idle-timeout.
- Local-CPU бенчмарк инференса + ADR-3 (§9) — по-прежнему ждёт ноду.
- ~~**⛔ Зелёный self-hosted eval-прогон для §3/citation-гейта (§5, Q-22).**~~
  ✅ **СДЕЛАНО 2026-07-14.** Прошлые падения были инфра-артефактами (Postgres
  недоступен из job / MCP host-port-коллизия с always-on demo/AWG-стеком). После
  фикса host-port-коллизии оверлеем `docker-compose.ci.yml` (`198bdf9`) первый
  ПОЛНОСТЬЮ чистый self-hosted full-eval **`29297896263` (HEAD=`7b894cd`) прошёл
  ЗЕЛЁНЫМ** (1h09m): citation_coverage=1.0, faithfulness=0.985, guardrail 1.0,
  nodata 1.0, numeric 0.923, 39/41 pass, «eval OK: no blocking thresholds
  violated» (exit 0). Раннер `epyc-home` = **online** (сверено `gh api`). Идёт
  подтверждающий 2-й прогон `29302691531`. Остаток по §3 — только формальное
  закрытие (2-й зелёный + запись на Quality-дашборд EPYC-БД).

**B. Сеть / VPS (за владельцем/оркестратором):**
- ~~Живой A2A-деплой воркера на `104.238.24.196` + AmneziaWG → закрытие **гейта
  G3** (§6).~~ ✅ **сделано 2026-07-13** (T-031 `[done]`, **G3 ✅**) — порядок
  гейтов соблюдён, **G4 формально открыт**.
- ~~Включение GitHub Pages для публичного URL сайта (§10).~~ ✅ **сделано
  2026-07-13** — сайт live на https://ledgerlens.space (cert approved+enforced).
- Реальный Telegram-send из мониторинг-конвейера (T-035 crit — разрешение
  владельца; канал уже доказан `312b5ae`).

**C. Решения владельца (OWNER_QUESTIONS):**
- ~~**Q-05** — CF-домен для публичного демо/сайта.~~ ✅ решён (домен
  `ledgerlens.space` / `app.ledgerlens.space` живой, cert enforced).
- ~~**Q-22** — политика citation-гейта (self-hosted раннер vs non_blocking).~~
  ✅ **ЗАКРЫТ 2026-07-14** — вариант-1 (self-hosted EPYC-раннер) реализован и
  доказан 2× зелёными full-eval (citation 1.0 / faithfulness 0.985–1.0); гейт-
  пороги не тронуты, T-041 закрыт, Q-16 закрыт.
- **Все блокирующие owner-вопросы закрыты** → критерий G4 «ноль блокирующих
  вопросов» по этой оси выполнен.

**D. Не-прогнанные локальные протоколы (T-040 собственные пункты):**
- §1 — Quick Start на чистой VM с таймингами (≤ 30 мин).
- §3 — финальный эталонный full-eval → запись в `eval_runs` на Quality-дашборде.
  **✅ достигнут 2026-07-14:** ДВА подряд чистых self-hosted full-eval —
  `29297896263` (citation 1.0 / faithfulness 0.985 / 39-of-41 pass) и
  `29303631947` (citation 1.0 / faithfulness 1.0 / 38-of-41), оба gate exit 0.
  T-041 закрыт на этой базе.
- §4 — тег `v1.0.0` + GitHub Release с артефактами; защита ветки main (CI required).
  **Предпосылка восстановлена 2026-07-14:** CI (`ci.yml`) была красной 40+
  прогонов подряд (>сутки, с 2026-07-12) — `build`-джоб падал на
  `docker compose config` из-за отсутствующего `.env` (env_file 5 сервисов;
  свежий compose трактует пропажу как ошибку). Фикс `042391e` (`cp .env.example
  .env` в build-джобе); CI снова **зелёный** (build/lint/unit ✅, прогон
  `29288707116`) → «CI required» теперь можно включать.
- §5 — санитарная проверка публичного контура (демо/лимиты/Grafana/сайт).
  **✅ live-проверка контура выполнена 2026-07-14** (регулярная сессия, curl над
  публичным HTTPS): (1) demo-баннер — `GET /api/examples` → `demo:true`, `mode:us`,
  5 примеров; (2) demo-лимит длины вопроса — `POST /api/chat` с question 600 симв.
  → **HTTP 413** (T-036 §1 cap ≤500 работает вживую); (3) security-заголовки на
  корне — `Content-Security-Policy` (default-src 'self', frame-ancestors 'none',
  object-src 'none'), `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`,
  `Referrer-Policy: strict-origin-when-cross-origin` — все присутствуют (T-036 §3);
  (4) FastAPI-доки НЕ экспонированы — `/docs` и `/openapi.json` отдают SPA-fallback
  (`text/html`, `<div id="root">`), а не Swagger/OpenAPI (app-`/docs` не
  проксируется nginx → docs-gating T-036 §3 держится вживую); (5) `/healthz` →
  `{"status":"ok","mode":"us"}` 200. **Остаток §5:** визуальная проверка
  Grafana-панелей demo-отказов (Grafana не публична — домен owner/demo-нода,
  = тот же остаток T-036 §1).
- ~~§6 — раздел Known Limitations в README (в т.ч. судьба RU-адаптера, Q-02).~~
  ✅ **done 2026-07-13** (регулярная сессия): выделенная секция «Known limitations»
  в `README.md` + зеркало «Известные ограничения» в `README.ru.md` (7 честных
  ограничений: faithfulness non_blocking, G3 не живой, медленный CPU-тир + ADR-3,
  частичное RU-покрытие Q-02 [MOEX ISS живой как pluggable-доказательство;
  e-disclosure/ГИР БО = каркасы вне v1.0], глубина цен AV free-tier, исключение
  GPU Q-07, скоуп публичного демо). Внутренние якоря проверены EN+RU.
- Внешний читатель / свежий-LLM-ревью README (T-038 крит.1) — сделан только
  self-review + fresh-reader accuracy-pass (`4099447`).

**E. Внешние дедлайны / зависимости (не блокеры, но на радар владельцу):**
- **DeepSeek: алиасы `deepseek-chat` / `deepseek-reasoner` удаляются 2026-07-24**
  (см. `docs/research/adr-notes.md:52`). **Статус: ✅ де-рискнуто.** Конфиг уже
  запинен на пост-алиасные ID `deepseek-v4-flash` (cloud_cheap) / `deepseek-v4-pro`
  (cloud_strong) (`config/router.yaml`, комментарий-предупреждение на месте);
  repo-wide grep не находит ни одной живой ссылки на старые ID (тесты/дефолты/
  деплой чисты). **Живая проверка 2026-07-13 (регулярная сессия):** оба тира
  прогнаны через реальную обвязку `RouterClient` против api.deepseek.com —
  `deepseek-v4-flash` (thinking=disabled) и `deepseek-v4-pro` (thinking=enabled,
  28 reasoning-токенов эмитятся корректно) отвечают `OK` (usage 12 / 41 токенов,
  ≪$0.01). Т.е. смена алиасов 2026-07-24 не сломает роутинг. **Остаётся владельцу:**
  просто держать на радаре (если DeepSeek изменит поведение/цены v4-pro thinking
  до релиза — пересверить бенч T-037 §1); действий в коде сейчас не требуется.
- **LangGraph: `create_react_agent` → `create_agent` (миграция, НЕ срочная).**
  `workers/react_worker.py:441` строит ReAct-агент через
  `langgraph.prebuilt.create_react_agent`. Версии (перепроверено 2026-07-14):
  `langgraph 1.2.8` / `langgraph-prebuilt 1.1.0` / `langchain-core 1.4.9`;
  `create_agent` живёт в meta-пакете `langchain`, который **в зависимостях
  проекта отсутствует** (`langchain NOT INSTALLED`). **КОРРЕКЦИЯ к прошлой
  заметке (2026-07-14, регулярная сессия):** прежний вывод «рантайм-предупреждения
  нет» был неполным — он проверял только *импорт*. На деле предупреждение
  эмитится в *точке вызова* `create_react_agent` (`react_worker.py:441`):
  `LangGraphDeprecatedSinceV10: create_react_agent has been moved to
  langchain.agents … Deprecated in LangGraph V1.0 to be removed in V2.0` —
  видно в КАЖДОМ прогоне pytest, который строит воркер-агента (напр.
  `tests/unit/test_worker.py`). Т.е. deprecation активен на рантайме, не
  отсутствует. **Имманентного слома по-прежнему нет** (запин V1.2.8, V2.0 не
  выпущена, жёсткого дедлайна нет), но сигнал устаревания реален и виден. Миграция —
  форвард-луфинг, и она **не байт-в-байт**: `create_agent` меняет контракт
  стрима (структуру узлов/сообщений `stream_mode="updates"`), а именно на этом
  контракте держится T-041-страж в цикле (`react_worker.py:456-471` — «финальный
  ответ = AIMessage БЕЗ tool_calls»). **Почему не сделано в этой сессии:**
  требует (1) добавить новую зависимость `langchain` в eval-критичный путь и
  (2) прогнать full-eval для подтверждения, что T-041-инвариант не регрессировал.
  Делать вслепую и пушить в main без eval-проверки = ровно тот риск, от которого
  предостерегает протокол координации. **Обновление 2026-07-14: раннер
  `epyc-home` снова ONLINE и чистый self-hosted full-eval теперь зелёный
  (`29297896263`) → предпосылка для revalidation ВОССТАНОВЛЕНА.** Миграция стала
  технически выполнимой, но остаётся сознательно отложенной (не делать в одной
  сессии с закрытием T-041, чтобы не смешивать контракт-меняющую правку с
  промоутом порога). **Остаётся владельцу/оркестратору:** запланировать миграцию
  отдельным заходом (добавить `langchain`-dep → переписать построение агента и
  адаптировать разбор стрима под новый контракт → full-eval ≥2 зелёных прогона,
  что T-041-метрики держатся → снять пин при желании). Действий в коде сейчас не
  требуется.

---

## Как поддерживать этот документ

Обновлять по мере снятия блокеров: менять статус пункта, добавлять указатель на
живое доказательство (commit/скриншот/запись прогона). Когда все 11 пунктов ✅ и
блокеры A–D закрыты — переносить содержимое в финальный `docs/release/v1.0-dod.md`
как эталонную запись гейта G4 и публиковать Release `v1.0.0`.
