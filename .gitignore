# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Env / secrets — never commit
.env
.env.*
!.env.example
!.env.worker.example

# Node / frontend
node_modules/
web/dist/
site/dist/

# Data, caches, models
data/cache/
*.gguf
*.dump
*.sql.gz
eval/reports/
.eval_logs/
# Demo/eval corpus snapshots (make snapshot / make seed) — shipped as release artifacts, not in git
snapshot/
*.pgdump
*.snapshot

# T-031 AmneziaWG mesh binaries + relayed worker image (large, node-local, not source)
awgbins/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
